---
phase: 01-storage-and-data-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - pnpm-workspace.yaml
  - vite.config.ts
  - tsconfig.json
  - tsconfig.node.json
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/styles.css
  - src-tauri/Cargo.toml
  - src-tauri/tauri.conf.json
  - src-tauri/capabilities/default.json
  - src-tauri/src/main.rs
  - src-tauri/src/lib.rs
  - src-tauri/src/error.rs
  - src-tauri/src/provider.rs
  - src-tauri/src/storage/mod.rs
  - src-tauri/src/storage/icloud.rs
autonomous: true
requirements:
  - SYNC-01
  - ADPT-03

must_haves:
  truths:
    - "Provider JSON files can be created, read, updated, and deleted in the iCloud Drive directory"
    - "Each provider is stored as an individual JSON file named by UUID"
    - "Provider data model includes protocol_type enum (Anthropic, OpenAI-compatible) and is extensible"
    - "Tauri 2 project scaffolds and builds successfully"
  artifacts:
    - path: "src-tauri/src/provider.rs"
      provides: "Provider struct, ProtocolType enum, ModelConfig struct"
      exports: ["Provider", "ProtocolType", "ModelConfig"]
    - path: "src-tauri/src/storage/icloud.rs"
      provides: "iCloud layer CRUD: list, save, get, delete providers"
      exports: ["list_providers", "save_provider", "get_provider", "delete_provider"]
    - path: "src-tauri/src/error.rs"
      provides: "AppError enum with Serialize impl for Tauri"
      exports: ["AppError"]
    - path: "src-tauri/Cargo.toml"
      provides: "Rust dependencies: serde, serde_json, uuid, dirs, thiserror, chrono"
      contains: "serde"
  key_links:
    - from: "src-tauri/src/storage/icloud.rs"
      to: "src-tauri/src/provider.rs"
      via: "uses Provider struct for serialization/deserialization"
      pattern: "use crate::provider::Provider"
    - from: "src-tauri/src/storage/icloud.rs"
      to: "src-tauri/src/error.rs"
      via: "returns AppError on failure"
      pattern: "use crate::error::AppError"
---

<objective>
Scaffold the Tauri 2 project and implement the Provider data model with iCloud storage layer.

Purpose: Establish the project foundation and deliver the core storage capability -- Provider CRUD in the iCloud Drive directory with per-file JSON persistence and atomic writes.

Output: A building Tauri 2 project with React frontend shell, Provider/ProtocolType data model, AppError type, and fully tested iCloud storage CRUD operations.
</objective>

<execution_context>
@/Users/kelin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-storage-and-data-model/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Tauri 2 project and configure dependencies</name>
  <files>
    package.json, pnpm-lock.yaml, vite.config.ts, tsconfig.json, index.html,
    src/main.tsx, src/App.tsx, src/styles.css,
    src-tauri/Cargo.toml, src-tauri/tauri.conf.json, src-tauri/capabilities/default.json,
    src-tauri/src/main.rs, src-tauri/src/lib.rs
  </files>
  <action>
    1. Run `pnpm create tauri-app` with the react-ts template to scaffold the project in the current directory.
       - If create-tauri-app asks for a project name, use "CLIManager".
       - If scaffolding into a subdirectory, move all files up to the repo root.
       - The goal is: `package.json` at repo root, `src-tauri/` at repo root.

    2. After scaffolding, add Rust dependencies to `src-tauri/Cargo.toml`:
       ```toml
       [dependencies]
       serde = { version = "1.0", features = ["derive"] }
       serde_json = "1.0"
       uuid = { version = "1", features = ["v4"] }
       dirs = "5.0"
       thiserror = "2.0"
       chrono = { version = "0.4", features = ["serde"] }
       log = "0.4"
       ```
       Keep whatever tauri dependencies the scaffold already added. Add `tempfile = "3"` under `[dev-dependencies]` for tests.

    3. Update `src-tauri/tauri.conf.json`:
       - Set `productName` to "CLIManager"
       - Set `identifier` to "com.climanager.app"
       - Set bundle identifier appropriately

    4. Set up the Rust module structure in `src-tauri/src/lib.rs`:
       ```rust
       mod error;
       mod provider;
       mod storage;
       ```
       Keep `main.rs` as the scaffold generated it (calling `lib::run()`).

    5. Verify the project builds: `pnpm install && cd src-tauri && cargo build`

    NOTE: The frontend is just the scaffold shell for now -- a minimal React app. Do not add any custom UI. The React frontend will be built in Phase 3.
  </action>
  <verify>
    <automated>cd /Users/kelin/Workspace/CLIManager && pnpm install && cd src-tauri && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>Tauri 2 project at repo root with React-TS frontend, all Rust dependencies in Cargo.toml, `cargo check` passes with no errors (warnings about unused modules are OK at this stage).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Provider model, AppError, and iCloud storage CRUD with tests</name>
  <files>
    src-tauri/src/error.rs,
    src-tauri/src/provider.rs,
    src-tauri/src/storage/mod.rs,
    src-tauri/src/storage/icloud.rs
  </files>
  <behavior>
    - Provider round-trip: serialize a Provider to JSON and deserialize back, all fields match
    - ProtocolType serde: "anthropic" serializes/deserializes correctly, "open_ai_compatible" likewise
    - ModelConfig optional fields: missing fields deserialize as None, present fields round-trip
    - schema_version: always present in serialized JSON, defaults to 1
    - iCloud CRUD - create: save_provider writes a JSON file named {id}.json in providers dir
    - iCloud CRUD - list: list_providers returns all saved providers sorted by created_at
    - iCloud CRUD - get: get_provider(id) returns the specific provider
    - iCloud CRUD - get missing: get_provider(nonexistent_id) returns NotFound error
    - iCloud CRUD - delete: delete_provider removes the file, subsequent get returns NotFound
    - iCloud CRUD - update: save_provider with existing id overwrites the file
    - Atomic write: file is written via temp-file + rename (not direct write)
    - iCloud dir creation: if providers directory doesn't exist, it's created automatically
  </behavior>
  <action>
    1. Create `src-tauri/src/error.rs` with AppError enum:
       - Variants: Io { path: String, source: std::io::Error }, Json(serde_json::Error), NotFound(String), ICloudUnavailable
       - Implement `serde::Serialize` manually (serialize as string via Display)
       - Use thiserror for Display/Error derives

    2. Create `src-tauri/src/provider.rs` with:
       - `ProtocolType` enum: Anthropic, OpenAiCompatible (with `#[serde(rename_all = "snake_case")]`)
       - `ModelConfig` struct: optional fields for haiku_model, sonnet_model, opus_model, reasoning_effort
       - `Provider` struct: id, name, protocol_type, api_key, base_url, model, model_config (Option), notes (Option), created_at (i64 millis), updated_at (i64 millis), schema_version (u32, default 1)
       - Include `#[cfg(test)] mod tests` with serde round-trip tests

    3. Create `src-tauri/src/storage/mod.rs`:
       - Declare submodules: `pub mod icloud;`
       - Add `pub fn atomic_write(path, data)` utility function (write to .tmp + rename)

    4. Create `src-tauri/src/storage/icloud.rs`:
       - `get_icloud_providers_dir() -> Result<PathBuf, AppError>`: resolves to `~/Library/Mobile Documents/com~apple~CloudDocs/CLIManager/providers/`, creates dir if missing. If `~/Library/Mobile Documents/` doesn't exist (iCloud unavailable), fall back to `~/.cli-manager/providers/` with a log warning.
       - `list_providers() -> Result<Vec<Provider>, AppError>`: reads all .json files, deserializes, sorts by created_at
       - `get_provider(id: &str) -> Result<Provider, AppError>`: reads single provider file
       - `save_provider(provider: &Provider) -> Result<(), AppError>`: atomic write to {id}.json
       - `delete_provider(id: &str) -> Result<(), AppError>`: removes file if exists
       - `#[cfg(test)] mod tests` using `tempfile::TempDir` to isolate test directory. Tests should use a helper function that overrides the providers directory to the temp dir. Use a module-level approach: create a `get_providers_dir` function that in tests can be pointed at a temp directory. One clean approach: add an internal function `list_providers_in(dir: &Path)`, `save_provider_to(dir: &Path, provider)`, etc., with the public functions delegating to these using the resolved iCloud path. Tests call the `_in` / `_to` variants directly with temp dirs.

    5. Run `cargo test` to verify all tests pass.
  </action>
  <verify>
    <automated>cd /Users/kelin/Workspace/CLIManager/src-tauri && cargo test 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Provider struct serializes/deserializes with all fields including protocol_type enum
    - iCloud storage CRUD operations work: create, list, get, update, delete
    - All Rust tests pass (`cargo test` green)
    - Atomic write uses temp-file + rename pattern
    - iCloud dir auto-created when missing
  </done>
</task>

</tasks>

<verification>
- `cargo check` passes with no errors in src-tauri
- `cargo test` passes all tests (provider serde + iCloud CRUD)
- Project structure matches the recommended layout from RESEARCH.md
- Provider JSON files use UUID naming and contain schema_version field
</verification>

<success_criteria>
1. Tauri 2 project builds (`cargo check` + `pnpm build` succeed)
2. Provider model round-trips through JSON with all fields intact
3. ProtocolType enum serializes as snake_case strings
4. iCloud CRUD operations pass all unit tests (create, read, update, delete, list)
5. Atomic write pattern used for all file writes
</success_criteria>

<output>
After completion, create `.planning/phases/01-storage-and-data-model/01-01-SUMMARY.md`
</output>
