---
phase: 15-handler
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - src-tauri/src/proxy/handler.rs
  - src-tauri/src/proxy/mod.rs
autonomous: true
requirements: [ROUT-01, ROUT-02, MODL-03]

must_haves:
  truths:
    - "OpenAiChatCompletions Provider 请求自动经请求转换 + 模型映射 + 响应转换完整链路"
    - "Anthropic Provider 请求完全透传，行为与 v2.1 相同"
    - "模型名按映射优先级替换：精确匹配 > upstream_model 默认 > 保留原名"
    - "非流式 OpenAI 响应正确转换为 Anthropic 格式"
    - "流式 SSE OpenAI 响应经 create_anthropic_sse_stream 转换为 Anthropic SSE 事件流"
    - "4xx/5xx 错误响应直接透传，不经转换处理"
  artifacts:
    - path: "src-tauri/src/proxy/handler.rs"
      provides: "协议路由分支 + apply_upstream_model_mapping 函数"
      contains: "apply_upstream_model_mapping"
    - path: "src-tauri/src/proxy/mod.rs"
      provides: "OpenAiChatCompletions 非流式和流式集成测试"
      contains: "test_openai_compatible_non_streaming_roundtrip"
  key_links:
    - from: "src-tauri/src/proxy/handler.rs"
      to: "src-tauri/src/proxy/translate/request.rs"
      via: "translate::request::anthropic_to_openai(body_value)"
      pattern: "anthropic_to_openai"
    - from: "src-tauri/src/proxy/handler.rs"
      to: "src-tauri/src/proxy/translate/response.rs"
      via: "translate::response::openai_to_anthropic(resp_value)"
      pattern: "openai_to_anthropic"
    - from: "src-tauri/src/proxy/handler.rs"
      to: "src-tauri/src/proxy/translate/stream.rs"
      via: "translate::stream::create_anthropic_sse_stream(bytes_stream, model)"
      pattern: "create_anthropic_sse_stream"
    - from: "src-tauri/src/proxy/handler.rs"
      to: "src-tauri/src/proxy/state.rs"
      via: "upstream.upstream_model / upstream.upstream_model_map"
      pattern: "upstream_model"
---

<objective>
在 handler.rs 中实现协议路由分支：OpenAiChatCompletions 走转换路径（请求转换 + 模型映射 + 响应转换），Anthropic 直接透传。包含模型映射纯函数 + 单元测试 + 集成测试。

Purpose: 这是 Phase 15 的核心交付——将 Phase 14 的三个转换模块接入代理 handler，实现端到端协议转换。
Output: handler.rs 含完整路由分支，3 个模型映射单元测试 + 2 个集成测试（非流式 + 流式 roundtrip）。
</objective>

<execution_context>
@/Users/kelin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-handler/15-RESEARCH.md
@.planning/phases/15-handler/15-01-SUMMARY.md

<interfaces>
<!-- Phase 14 转换函数签名 -->
```rust
// translate/request.rs
pub fn anthropic_to_openai(body: Value) -> Result<Value, ProxyError>;
pub fn build_proxy_endpoint_url(base_url: &str, endpoint_suffix: &str) -> String;

// translate/response.rs
pub fn openai_to_anthropic(body: Value) -> Result<Value, ProxyError>;

// translate/stream.rs
pub fn create_anthropic_sse_stream(
    upstream: impl Stream<Item = Result<Bytes, reqwest::Error>> + Send + 'static,
    model: String,
) -> impl Stream<Item = Result<Bytes, std::io::Error>> + Send;
```

<!-- UpstreamTarget 扩展后（Plan 01 产出） -->
```rust
pub struct UpstreamTarget {
    pub api_key: String,
    pub base_url: String,
    pub protocol_type: ProtocolType,
    pub upstream_model: Option<String>,
    pub upstream_model_map: Option<HashMap<String, String>>,
}
```

<!-- handler.rs 当前步骤 A-J 结构 -->
<!-- A: get_upstream  B: 提取请求信息  C: 读取 body  D: URL 拼接 -->
<!-- E-G: 构建 reqwest + 凭据注入  H: 发送请求  I: 响应头  J: 响应体透传 -->

<!-- 已有集成测试辅助函数（mod.rs） -->
```rust
fn test_client() -> reqwest::Client;
fn make_upstream(base_url: &str) -> UpstreamTarget;
async fn start_mock_upstream_router(handler: Router) -> (u16, oneshot::Sender<()>);
async fn start_mock_upstream(response_body: Value) -> (u16, oneshot::Sender<()>);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: handler.rs 协议路由分支 + 模型映射函数</name>
  <files>src-tauri/src/proxy/handler.rs</files>
  <behavior>
    - test_model_exact_match_wins_over_default: upstream_model_map 中有精确匹配条目时使用该映射值
    - test_model_fallback_to_upstream_model: 无精确匹配时退回 upstream_model 默认模型
    - test_model_preserved_when_no_mapping: upstream_model 和 upstream_model_map 均为 None 时保留原模型名
  </behavior>
  <action>
1. **新增 use 声明：**
   ```rust
   use std::collections::HashMap;
   use bytes::Bytes;
   use super::translate;
   ```

2. **新增 apply_upstream_model_mapping 私有函数：**
   按 RESEARCH.md Pattern 4 实现——优先级：精确匹配 upstream_model_map → upstream_model 默认 → 保留原名。纯函数，接收 `mut body: Value` 和 `&UpstreamTarget`，返回 `Value`。

3. **重构 proxy_handler 步骤 C-D-H-J，在步骤 C 之后插入协议路由分支：**

   ```rust
   // === 步骤 C 之后：协议路由分支 ===
   let (upstream_url, final_body_bytes, is_streaming) = match upstream.protocol_type {
       ProtocolType::OpenAiChatCompletions => {
           // 1. 解析请求体
           let body_value: Value = serde_json::from_slice(&body_bytes)
               .map_err(|e| ProxyError::TranslateError(format!("无法解析请求体: {}", e)))?;

           // 2. 提取 stream 标志（转换前读取，因为 anthropic_to_openai 会透传）
           let is_streaming = body_value.get("stream").and_then(|v| v.as_bool()).unwrap_or(false);

           // 3. 模型名映射（MODL-03，在转换前执行）
           let body_value = apply_upstream_model_mapping(body_value, &upstream);

           // 4. 请求转换 + 端点重写
           let openai_body = translate::request::anthropic_to_openai(body_value)?;
           let url = translate::request::build_proxy_endpoint_url(&upstream.base_url, "/chat/completions");
           let new_bytes = serde_json::to_vec(&openai_body)
               .map_err(|e| ProxyError::Internal(e.to_string()))?;

           (url, Bytes::from(new_bytes), is_streaming)
       }
       ProtocolType::Anthropic | ProtocolType::OpenAiResponses => {
           // 透传路径：URL 拼接与现有逻辑一致
           let url = format!("{}{}{}", upstream.base_url.trim_end_matches('/'), path, query);
           (url, body_bytes, false)
       }
   };
   ```

4. **步骤 D 删除原来的 upstream_url 拼接代码**（已在上面的 match 中处理）。

5. **步骤 E：req_builder 使用 upstream_url 而非旧变量名。**

6. **步骤 H：body 使用 final_body_bytes：**
   ```rust
   let upstream_resp = req_builder
       .body(final_body_bytes.to_vec())
       .send()
       .await
       .map_err(|e| ProxyError::UpstreamUnreachable(e.to_string()))?;
   ```

7. **步骤 J 改为按 protocol_type 分支处理响应体：**
   ```rust
   let body = match upstream.protocol_type {
       ProtocolType::OpenAiChatCompletions => {
           if !status.is_success() {
               // 4xx/5xx 直接透传（RESP-05）
               Body::from_stream(upstream_resp.bytes_stream())
           } else if is_streaming {
               // 流式：wrap 为 SSE 转换流
               let model = "claude-3-5-sonnet-20241022".to_string(); // model 名仅用于 SSE 事件中的 message_start
               Body::from_stream(translate::stream::create_anthropic_sse_stream(
                   upstream_resp.bytes_stream(),
                   model,
               ))
           } else {
               // 非流式：读完整响应，转换后返回
               let resp_bytes = upstream_resp.bytes().await
                   .map_err(|e| ProxyError::Internal(format!("读取上游响应失败: {}", e)))?;
               let resp_value: Value = serde_json::from_slice(&resp_bytes)
                   .map_err(|e| ProxyError::TranslateError(format!("响应解析失败: {}", e)))?;
               let anthropic_resp = translate::response::openai_to_anthropic(resp_value)?;
               let resp_bytes = serde_json::to_vec(&anthropic_resp)
                   .map_err(|e| ProxyError::Internal(e.to_string()))?;
               Body::from(resp_bytes)
           }
       }
       _ => {
           // Anthropic / OpenAiResponses：透传（现有行为）
           Body::from_stream(upstream_resp.bytes_stream())
       }
   };
   ```

8. **模型名提取优化：** 在步骤 3（模型映射）中，从 body_value 提取原始 model 名存入局部变量，用于步骤 7 流式路径的 `model` 参数（而非硬编码）：
   ```rust
   let request_model = body_value.get("model")
       .and_then(|m| m.as_str())
       .unwrap_or("unknown")
       .to_string();
   ```
   流式路径中使用 `request_model` 而非硬编码字符串。

9. **单元测试（#[cfg(test)] mod tests 内新增）：**
   - `test_model_exact_match_wins_over_default`: 构造含 upstream_model_map 和 upstream_model 的 UpstreamTarget，验证精确匹配优先
   - `test_model_fallback_to_upstream_model`: 无精确匹配时退回 upstream_model
   - `test_model_preserved_when_no_mapping`: 两者均为 None 时保留原模型名

**注意事项：**
- Anthropic 透传路径**完全不触碰**任何转换函数——ROUT-02 零回归
- is_streaming 变量仅在 OpenAiChatCompletions 分支内有语义，Anthropic 分支 is_streaming=false 不影响后续（因为 Anthropic 直接透传，不走 is_streaming 分支）
- content-length 头已在步骤 I 的过滤中被移除，非流式转换后无需额外处理
  </action>
  <verify>
    <automated>/Users/kelin/.cargo/bin/cargo test --manifest-path /Users/kelin/Workspace/CLIManager/src-tauri/Cargo.toml --package cli-manager proxy::handler 2>&1 | tail -5</automated>
  </verify>
  <done>
    - handler.rs 含完整协议路由分支（OpenAiChatCompletions 转换 + Anthropic 透传）
    - apply_upstream_model_mapping 函数实现映射优先级：精确匹配 > upstream_model > 保留原名
    - 3 个模型映射单元测试全部通过
    - 编译通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 集成测试 — OpenAiChatCompletions 非流式 + 流式 roundtrip</name>
  <files>src-tauri/src/proxy/mod.rs</files>
  <action>
在 proxy/mod.rs 的 `#[cfg(test)] mod tests` 中新增两个集成测试。需要额外引入 `use axum::http::header::HeaderValue;` 和 `use axum::routing::post;`。

1. **test_openai_compatible_non_streaming_roundtrip:**
   - 创建 mock 上游 Router，接收 POST 请求返回 OpenAI Chat Completions 非流式格式响应：
     ```rust
     let mock_resp = json!({
         "id": "chatcmpl-test",
         "object": "chat.completion",
         "choices": [{
             "index": 0,
             "message": {"role": "assistant", "content": "Hello from OpenAI!"},
             "finish_reason": "stop"
         }],
         "usage": {"prompt_tokens": 10, "completion_tokens": 5, "total_tokens": 15}
     });
     ```
   - 使用 `post` 路由绑定（mock 只响应 POST `/v1/chat/completions`）
   - 构造 OpenAiChatCompletions UpstreamTarget（base_url 含 mock 端口）
   - 启动 ProxyService，发送 Anthropic 格式请求到代理
   - 验证响应为 Anthropic 格式：
     - `resp["content"][0]["type"] == "text"`
     - `resp["content"][0]["text"] == "Hello from OpenAI!"`
     - `resp["stop_reason"] == "end_turn"`
     - `resp["usage"]["input_tokens"] == 10`
   - 清理

2. **test_openai_compatible_streaming_roundtrip:**
   - 创建 mock 上游 Router，返回 SSE 流式响应（手工构造 OpenAI SSE 事件）：
     ```
     data: {"id":"chatcmpl-test","object":"chat.completion.chunk","choices":[{"index":0,"delta":{"role":"assistant","content":"Hi"},"finish_reason":null}]}

     data: {"id":"chatcmpl-test","object":"chat.completion.chunk","choices":[{"index":0,"delta":{"content":" there"},"finish_reason":null}]}

     data: {"id":"chatcmpl-test","object":"chat.completion.chunk","choices":[{"index":0,"delta":{},"finish_reason":"stop"}],"usage":{"prompt_tokens":5,"completion_tokens":2,"total_tokens":7}}

     data: [DONE]
     ```
   - mock 上游响应 content-type: text/event-stream
   - 构造 OpenAiChatCompletions UpstreamTarget，请求体含 `"stream": true`
   - 发送 Anthropic 格式请求到代理
   - 读取完整响应 body 为 String
   - 验证响应包含 Anthropic SSE 事件关键标记：
     - 包含 `event: message_start`
     - 包含 `event: content_block_start`
     - 包含 `event: content_block_delta`
     - 包含 `event: message_stop`
     - 包含 `"Hi"` 和 `" there"` 文本
   - 清理

3. **test_openai_compatible_model_mapping_applied:**
   - 创建 mock 上游，capture 接收到的请求 body（使用 Arc<TokioMutex<Option<Value>>> 共享状态）
   - 构造 UpstreamTarget 含 upstream_model_map: {"claude-3-5-sonnet-20241022": "gpt-4o"}
   - 发送 Anthropic 请求（model: "claude-3-5-sonnet-20241022"）
   - 验证 mock 上游接收到的请求中 model 字段已被替换为 "gpt-4o"
   - 清理

**make_upstream 辅助函数扩展：** 新增 `make_upstream_openai(base_url: &str) -> UpstreamTarget`，protocol_type 为 OpenAiChatCompletions，upstream_model/upstream_model_map 为 None。

**注意事项：**
- mock 上游 handler 必须匹配 POST 方法（`axum::routing::post`），因为代理转发保留原始 HTTP 方法
- 流式测试中 mock 上游需通过 Body::from_stream 返回 SSE 字节流（不要使用 Json 返回）
- 确保 mock 上游设置 `content-type: text/event-stream` 响应头
  </action>
  <verify>
    <automated>/Users/kelin/.cargo/bin/cargo test --manifest-path /Users/kelin/Workspace/CLIManager/src-tauri/Cargo.toml --package cli-manager proxy::mod::tests 2>&1 | tail -10</automated>
  </verify>
  <done>
    - 非流式 roundtrip 测试通过：Anthropic 请求 → 代理转换 → mock OpenAI 上游 → 代理转换回 → Anthropic 格式响应
    - 流式 roundtrip 测试通过：SSE 事件流完整转换，包含 message_start/content_block_delta/message_stop
    - 模型映射集成测试通过：mock 上游确认收到映射后的模型名
    - 全部现有测试继续通过（Anthropic 透传路径零回归）
  </done>
</task>

</tasks>

<verification>
/Users/kelin/.cargo/bin/cargo test --manifest-path /Users/kelin/Workspace/CLIManager/src-tauri/Cargo.toml --package cli-manager 2>&1 | grep -E "test result|FAILED"
</verification>

<success_criteria>
- handler.rs 含 OpenAiChatCompletions 协议路由分支，Anthropic 透传不变
- apply_upstream_model_mapping 实现三级映射优先级
- 3 个模型映射单元测试通过
- 3 个集成测试通过（非流式 + 流式 + 模型映射验证）
- 全套测试通过（294+ 现有 + 6 新增）
- 零回归：所有 Anthropic 透传测试继续通过
</success_criteria>

<output>
After completion, create `.planning/phases/15-handler/15-02-SUMMARY.md`
</output>
