---
phase: 18-homepage-layout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/provider/EmptyState.tsx
  - src/components/provider/ProviderTabs.tsx
  - src/i18n/locales/zh.json
  - src/i18n/locales/en.json
autonomous: true
requirements: [HOME-03, HOME-04]

must_haves:
  truths:
    - "无 Provider 时页面展示精致的空状态：放大的 lucide 图标 + 品牌橙色淡底圆形装饰"
    - "空状态文案为「还没有 Provider」+「添加你的第一个 API Provider 开始使用」"
    - "空状态只有一个「创建 Provider」按钮"
    - "Tab 标签旁代理绿点从 size-2 加大到 size-2.5，启用时有脉冲动画"
    - "代理模式开关行旁有圆点指示：启用绿色，停用灰色，始终可见"
  artifacts:
    - path: "src/components/provider/EmptyState.tsx"
      provides: "精致空状态页面（品牌橙色装饰 + 优化文案）"
      contains: "brand-accent"
    - path: "src/components/provider/ProviderTabs.tsx"
      provides: "代理状态圆点优化（加大 + 脉冲 + 开关旁指示）"
      contains: "animate-pulse"
  key_links:
    - from: "src/components/provider/EmptyState.tsx"
      to: "CSS 变量 brand-accent"
      via: "bg-brand-accent/10 + text-brand-accent 装饰圆形"
      pattern: "brand-accent"
    - from: "src/components/provider/ProviderTabs.tsx"
      to: "status-success"
      via: "bg-status-success 代理绿点"
      pattern: "status-success"
---

<objective>
优化首页空状态页面视觉和代理状态指示的清晰度。

Purpose: 空状态引导用户创建 Provider 而非显示空白；代理状态指示让用户一眼看出代理是否运行
Output: 精致化的 EmptyState.tsx + 优化的代理状态指示
</objective>

<execution_context>
@/Users/kelin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-homepage-layout/18-CONTEXT.md
@.planning/phases/17-design-foundation/17-01-SUMMARY.md

<interfaces>
<!-- 空状态和代理状态涉及的接口 -->

From src/components/provider/EmptyState.tsx:
```typescript
interface EmptyStateProps {
  onCreate: () => void;
}
```

From src/components/provider/ProviderTabs.tsx:
```typescript
// 代理状态相关变量
const cliProxyActive = cliStatus?.active ?? false;
const globalEnabled = proxyStatus?.global_enabled ?? false;

// Tab 标签中的代理绿点（当前）:
// <span className="size-2 rounded-full bg-status-success" />

// 代理开关行（当前）:
// <Switch checked={cliProxyActive} ... />
```

CSS 变量（来自 Phase 17）:
- --brand-accent: 品牌橙色 oklch(0.702 0.183 56.518)
- --status-success: 成功/在线绿色
- Tailwind 类: bg-brand-accent, text-brand-accent, bg-status-success

已有翻译 key:
- empty.title: "暂无 Provider" / "No Providers"
- empty.description: "点击下方按钮创建你的第一个 Provider" / "Click the button below to create your first provider"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 空状态页面精致化</name>
  <files>src/components/provider/EmptyState.tsx, src/i18n/locales/zh.json, src/i18n/locales/en.json</files>
  <action>
**A. 重构 EmptyState.tsx（HOME-03）：**

1. 保持现有结构（垂直居中 flex 布局），在图标外层添加品牌橙色淡底圆形装饰：
   ```
   <div className="flex size-20 items-center justify-center rounded-full bg-brand-accent/10">
     <PackageOpen className="size-10 text-brand-accent" />
   </div>
   ```
   - 圆形装饰：size-20（80px），rounded-full，bg-brand-accent/10（橙色 10% 透明度背景）
   - 图标：从 size-12 加大到 size-10（在圆形内），颜色从 text-muted-foreground 改为 text-brand-accent

2. 文案部分保持结构不变，更新翻译 key 值

3. 按钮保持不变（已有的 Plus 图标 + t("actions.create")）

**B. 更新翻译文件：**

zh.json 修改：
- empty.title: "暂无 Provider" → "还没有 Provider"
- empty.description: "点击下方按钮创建你的第一个 Provider" → "添加你的第一个 API Provider 开始使用"

en.json 修改：
- empty.title: "No Providers" → "No Providers Yet"
- empty.description: "Click the button below to create your first provider" → "Add your first API provider to get started"
  </action>
  <verify>
    <automated>cd /Users/kelin/Workspace/CLIManager && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>
    - EmptyState 图标外层有品牌橙色淡底圆形装饰（bg-brand-accent/10）
    - 图标颜色为 text-brand-accent 而非 muted-foreground
    - 中英文空状态文案已更新
    - 只有一个「创建 Provider」按钮
  </done>
</task>

<task type="auto">
  <name>Task 2: 代理状态指示优化</name>
  <files>src/components/provider/ProviderTabs.tsx</files>
  <action>
**修改 ProviderTabs.tsx（HOME-04）：**

1. **Tab 标签旁代理绿点加大 + 脉冲动画：**
   找到 TabsTrigger 中的绿点 `<span className="size-2 rounded-full bg-status-success" />`，修改为：
   ```
   <span className="size-2.5 rounded-full bg-status-success animate-pulse" />
   ```
   - size-2 → size-2.5（从 8px 增大到 10px）
   - 添加 animate-pulse 产生呼吸/脉冲效果，传达「服务正在运行」的活跃感

2. **代理模式开关行旁添加状态圆点指示：**
   找到 `{/* CLI 代理开关行 */}` 部分，在 Switch 组件之后（或 span 标签旁）添加状态圆点：
   ```
   <span className={`size-2 rounded-full ${cliProxyActive ? 'bg-status-success' : 'bg-muted-foreground/40'}`} />
   ```
   - 启用时：绿色圆点（bg-status-success）
   - 停用时：灰色圆点（bg-muted-foreground/40），始终可见
   - 放在 Switch 组件右侧（开关后面）
   - 注意：需要将圆点放在 TooltipProvider 条件判断之外或之内均可，关键是在两种情况（switchDisabled 和正常状态）下都能看到圆点

   具体实现：在 Switch（包括 disabled 和正常两个分支）之后，追加圆点 span。可以把圆点提取到 Switch 条件外面，因为圆点的显示不依赖 switchDisabled 逻辑，只依赖 cliProxyActive 状态
  </action>
  <verify>
    <automated>cd /Users/kelin/Workspace/CLIManager && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>
    - Tab 标签旁代理绿点为 size-2.5（加大），启用时有 animate-pulse 脉冲动画
    - 代理开关行旁有状态圆点：启用时绿色，停用时灰色
    - 停用时灰色圆点始终可见（非隐藏）
    - npm run build 通过
  </done>
</task>

</tasks>

<verification>
- npm run build 编译通过
- EmptyState.tsx 包含 brand-accent 引用（圆形装饰）
- ProviderTabs.tsx 中代理绿点为 size-2.5 而非 size-2
- ProviderTabs.tsx 中包含 animate-pulse 类名
- ProviderTabs.tsx 中代理开关行有 bg-status-success 和 bg-muted-foreground 的条件圆点
- zh.json 空状态标题为「还没有 Provider」
</verification>

<success_criteria>
- 空状态图标有品牌橙色淡底圆形装饰，视觉精致不空洞
- 空状态文案简洁友好，引导用户创建 Provider
- Tab 标签旁代理绿点更大且有脉冲动画
- 代理开关旁状态圆点始终可见，绿/灰清晰区分
- 中英文翻译均已更新
- 构建通过
</success_criteria>

<output>
After completion, create `.planning/phases/18-homepage-layout/18-02-SUMMARY.md`
</output>
