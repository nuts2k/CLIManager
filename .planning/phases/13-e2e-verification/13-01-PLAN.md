---
phase: 13-e2e-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [".github/workflows/release.yml"]
autonomous: false
requirements: [CICD-01, CICD-02, CICD-03, SIGN-01, SIGN-02, SIGN-03, UPD-01, UPD-02, UPD-03, UPD-04, REL-01, REL-02, REL-03]

must_haves:
  truths:
    - "/ship patch 执行后 tag 成功推送，GitHub Actions 自动触发构建"
    - "CI 双架构 job（aarch64 + x86_64）均绿色完成"
    - "GitHub Release 自动发布（非 Draft），包含完整产物（DMG + .app.tar.gz + .sig + latest.json）"
    - "latest.json 可通过 releases/latest/download/latest.json 访问，且包含双架构信息"
    - "aarch64 DMG 可安装到 /Applications 并正常运行"
    - "v0.2.1 app 启动后检测到 v0.2.2 更新并弹出 UpdateDialog"
    - "点击更新后下载安装成功，新版本正常运行"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "releaseDraft: false 配置"
      contains: "releaseDraft: false"
    - path: "GitHub Release v0.2.1"
      provides: "基准版本 Release 及完整产物"
    - path: "GitHub Release v0.2.2"
      provides: "更新版本 Release，验证 updater 端到端"
  key_links:
    - from: "/ship patch"
      to: "GitHub Actions release.yml"
      via: "git tag v*.*.* 触发"
      pattern: "v[0-9]*\\.[0-9]*\\.[0-9]*"
    - from: "release.yml CI"
      to: "GitHub Release"
      via: "tauri-action@v1 上传产物"
      pattern: "releaseDraft: false"
    - from: "releases/latest/download/latest.json"
      to: "useUpdater.checkForUpdate()"
      via: "tauri.conf.json endpoints"
      pattern: "releases/latest/download/latest.json"
---

<objective>
端到端验证 Phase 12 全部产出：release script -> CI build -> updater check -> download -> install -> relaunch

目的：确认 v2.1 Release Engineering 里程碑的所有需求在真实环境中联通闭环。唯一代码变更为 release.yml releaseDraft 配置。采用"两次发版"模式：v0.2.1 建立基准版本，v0.2.2 测试完整更新流。

产出：两个已发布的 GitHub Release（v0.2.1 + v0.2.2），aarch64 完整端到端验证通过
</objective>

<execution_context>
@/Users/kelin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-e2e-verification/13-CONTEXT.md
@.planning/phases/13-e2e-verification/13-RESEARCH.md

@.planning/phases/12-full-stack-impl/12-02-SUMMARY.md
@.planning/phases/12-full-stack-impl/12-04-SUMMARY.md

<interfaces>
<!-- release.yml 当前配置（唯一需修改项） -->

From .github/workflows/release.yml (line 55):
```yaml
          releaseDraft: true
```
修改为 `releaseDraft: false`，使 CI 完成后 Release 自动发布。

<!-- /ship 技能调用方式 -->

从 .claude/commands/ship.md:
- 调用：`/ship patch`（从 0.2.0 bump 到 0.2.1）
- 流程：读取 Cargo.toml → bump 版本 → 更新 CHANGELOG.md → git commit → git tag → git push

<!-- updater endpoints 配置 -->

从 src-tauri/tauri.conf.json:
- endpoints: ["https://github.com/nuts2k/CLIManager/releases/latest/download/latest.json"]
- 只有非 Draft Release 才会被 releases/latest 路径解析到
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修改 releaseDraft 配置为自动发布</name>
  <files>.github/workflows/release.yml</files>
  <action>
将 `.github/workflows/release.yml` 第 55 行的 `releaseDraft: true` 改为 `releaseDraft: false`。

这是 Phase 13 唯一的代码变更。目的：Draft Release 不会成为 `releases/latest`，updater endpoint 无法解析 latest.json；改为 false 后 CI 完成即自动发布，latest.json 通过 releases/latest/download/ 路径可达。

修改后 commit 并 push（不带 tag，不触发 CI）：
```bash
git add .github/workflows/release.yml
git commit -m "ci: auto-publish release (releaseDraft false)"
git push
```
  </action>
  <verify>
    <automated>grep -n "releaseDraft: false" .github/workflows/release.yml</automated>
  </verify>
  <done>release.yml 中 releaseDraft 值为 false，变更已推送到 main</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 发布 v0.2.1 基准版本并验证 CI + 产物</name>
  <files>.github/workflows/release.yml</files>
  <action>
用户执行 /ship patch 触发首次真实发版，验证完整 CI/CD 流水线 + 产物 + DMG 安装。

覆盖需求：CICD-01（tag 触发）、CICD-02（双架构）、CICD-03（产物上传）、SIGN-01（ad-hoc 签名）、SIGN-02/03（Ed25519 签名）、REL-01（版本来源）、REL-02（/ship 技能）、REL-03（Gatekeeper 指引）

Claude 在此任务中协助用户执行以下验证命令，但 /ship 执行、CI 监控和 DMG 安装需要用户参与。
  </action>
  <verify>
    <automated>gh release view v0.2.1 --json assets --jq '.assets | length'</automated>
  </verify>
  <done>v0.2.1 Release 已发布（非 Draft），7 个产物完整，aarch64 DMG 安装到 /Applications 可运行</done>
  <what-built>
Phase 12 完整 CI/CD 流水线 + /ship 发版技能。本任务验证首次真实发版链路：
/ship patch -> git tag v0.2.1 -> CI 双架构构建 -> Release 自动发布 -> 产物完整性 -> DMG 安装
  </what-built>
  <how-to-verify>
**步骤 A：执行 /ship patch 发版**

1. 在 Claude Code 中运行 `/ship patch`
2. 确认输出 5 步均为成功（版本 0.2.0 -> 0.2.1）
3. 确认 tag v0.2.1 已推送

**步骤 B：等待 CI 完成（约 10-15 分钟）**

4. 访问 https://github.com/nuts2k/CLIManager/actions 监控 CI
5. 等待 aarch64 和 x86_64 两个 job 均为绿色
   - 如果遇到 DMG 打包报错（Bug #13804）：重跑失败的 job，不修改代码
6. 验证 CI 触发为 tag v0.2.1（确认 CICD-01）

**步骤 C：验证 Release 产物**

7. 运行以下命令验证产物完整性：
```bash
gh release view v0.2.1 --json assets --jq '.assets[].name'
```
期望包含 7 个文件：
- CLIManager_*_aarch64.dmg
- CLIManager_*_x86_64.dmg
- CLIManager_*_aarch64.app.tar.gz
- CLIManager_*_aarch64.app.tar.gz.sig
- CLIManager_*_x86_64.app.tar.gz
- CLIManager_*_x86_64.app.tar.gz.sig
- latest.json

8. 验证 latest.json 可达且内容正确：
```bash
curl -sL "https://github.com/nuts2k/CLIManager/releases/latest/download/latest.json" | python3 -m json.tool
```
期望：version 为 "0.2.1"，platforms 包含 darwin-aarch64 和 darwin-x86_64，各有 signature 和 url 字段

9. 访问 https://github.com/nuts2k/CLIManager/releases 确认：
   - v0.2.1 显示为 Release（非 Draft）
   - Release Notes 包含 Gatekeeper 安装指引（折叠的 xattr 命令块）

**步骤 D：安装 aarch64 DMG 到本机**

10. 下载 aarch64 DMG：
```bash
gh release download v0.2.1 --pattern '*aarch64.dmg' --dir ~/Downloads
```
11. 安装到 /Applications：
```bash
hdiutil attach ~/Downloads/CLIManager_*_aarch64.dmg
cp -R /Volumes/CLIManager/CLIManager.app /Applications/
hdiutil detach /Volumes/CLIManager
xattr -cr /Applications/CLIManager.app
```
12. 启动 /Applications/CLIManager.app，确认可正常运行
13. 检查版本号：
```bash
/usr/libexec/PlistBuddy -c "Print CFBundleShortVersionString" /Applications/CLIManager.app/Contents/Info.plist
```
期望输出 0.2.1
  </how-to-verify>
  <resume-signal>
全部通过输入 "approved"。如有问题描述具体失败步骤和错误信息。

如 CI 因 Bug #13804 随机失败，重跑 job 后通过也算通过。
如需重试（最多 2 次），说明重试原因。
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 发布 v0.2.2 并验证完整更新流</name>
  <files>src/components/updater/useUpdater.ts, src/components/updater/UpdateDialog.tsx</files>
  <action>
用户执行 /ship patch 发布 v0.2.2，然后启动已安装的 v0.2.1 app 验证完整更新流。

覆盖需求：UPD-01（插件集成）、UPD-02（自动检查）、UPD-03（更新 UI）、UPD-04（下载安装重启）

Claude 协助执行验证命令和排查问题。如果 app 检测不到更新，按以下顺序排查：latest.json URL -> endpoints 配置 -> 网络连接。relaunch 失败（os error 1）可降级为手动重启。
  </action>
  <verify>
    <automated>curl -sL "https://github.com/nuts2k/CLIManager/releases/latest/download/latest.json" | python3 -c "import sys,json; d=json.load(sys.stdin); print(d['version'])"</automated>
  </verify>
  <done>v0.2.1 app 检测到 v0.2.2 更新，UpdateDialog 弹出，下载安装成功，新版本正常运行（relaunch 可降级手动重启）；x86_64 CI 成功且 Release 含对应产物</done>
  <what-built>
Phase 12 的 updater 集成（useUpdater hook + UpdateDialog + AppShell 启动检查）。本任务验证端到端更新流：
发布 v0.2.2 -> 已安装的 v0.2.1 检测到更新 -> 弹出 UpdateDialog -> 下载安装 -> 新版本运行
  </what-built>
  <how-to-verify>
**步骤 A：发布 v0.2.2**

1. 在 Claude Code 中运行 `/ship patch`
2. 确认版本 0.2.1 -> 0.2.2
3. 等待 CI 完成（两个 job 均绿色）
   - Bug #13804 随机失败则重跑 job
4. 验证 latest.json 已更新：
```bash
curl -sL "https://github.com/nuts2k/CLIManager/releases/latest/download/latest.json" | python3 -c "import sys,json; d=json.load(sys.stdin); print(f'version: {d[\"version\"]}')"
```
期望输出：version: 0.2.2

**步骤 B：验证更新检测与安装（aarch64）**

5. 启动 /Applications/CLIManager.app（当前为 v0.2.1）
6. 等待 UpdateDialog 弹出（app 启动时自动检查，约 5 秒内）
   - 如未弹出：打开设置页面，手动点击"检查更新"
   - 如仍无反应：用以下命令排查 latest.json 可达性
   ```bash
   curl -sL "https://github.com/nuts2k/CLIManager/releases/latest/download/latest.json" | python3 -m json.tool
   ```
7. 确认 UpdateDialog 显示新版本号（0.2.2）和"立即更新"按钮（验证 UPD-03）
8. 点击"立即更新"，观察进度条显示（验证 UPD-03）
9. 等待下载完成 + 安装成功（验证 UPD-04）
10. relaunch 处理：
    - 如果 app 自动重启：完美通过
    - 如果报 os error 1（Issue #2273）：手动关闭并重新打开 app，这是已知限制，不阻碍通过
11. 确认新版本正常运行，检查版本号：
```bash
/usr/libexec/PlistBuddy -c "Print CFBundleShortVersionString" /Applications/CLIManager.app/Contents/Info.plist
```
期望输出 0.2.2

**步骤 C：x86_64 架构确认（仅检查 CI 产物）**

12. 确认 v0.2.2 Release 中包含 x86_64 产物：
```bash
gh release view v0.2.2 --json assets --jq '.assets[].name' | grep x86_64
```
期望包含 x86_64.dmg、x86_64.app.tar.gz、x86_64.app.tar.gz.sig

13. 确认 latest.json 包含 x86_64 平台信息：
```bash
curl -sL "https://github.com/nuts2k/CLIManager/releases/latest/download/latest.json" | python3 -c "import sys,json; d=json.load(sys.stdin); print('x86_64' in str(d.get('platforms',{})))"
```
期望输出 True
  </how-to-verify>
  <resume-signal>
全部通过输入 "approved"。

通过标准（实用标准）：
- aarch64 完整走通（检测更新 -> 下载 -> 安装 -> 新版本运行）
- x86_64 CI 成功 + Release 有对应产物
- relaunch 可降级为手动重启

如需重试（v0.2.3，最多 1 次额外机会），说明失败原因。
  </resume-signal>
</task>

</tasks>

<verification>
## 整体验证

所有 13 个需求通过端到端验证：

| 需求 | 验证方式 | 通过标准 |
|------|---------|---------|
| CICD-01 | tag 触发 CI | Actions 页面 workflow 运行 |
| CICD-02 | 双架构构建 | aarch64 + x86_64 两个 job 绿色 |
| CICD-03 | 产物上传 | DMG + .tar.gz + .sig + latest.json 完整 |
| SIGN-01 | ad-hoc 签名 | DMG 可安装运行 |
| SIGN-02 | Ed25519 密钥 | latest.json 中 signature 字段非空 |
| SIGN-03 | 密钥配置 | CI 产出 .sig 文件 |
| UPD-01 | 插件集成 | app 启动无 plugin 报错 |
| UPD-02 | 自动检查 | v0.2.1 启动后弹出更新提示 |
| UPD-03 | 更新 UI | 进度条显示、版本号正确 |
| UPD-04 | 下载安装 | 安装完成、新版本运行 |
| REL-01 | 版本来源 | Release 标题含正确 semver |
| REL-02 | /ship 技能 | /ship patch 5 步成功 |
| REL-03 | Gatekeeper 指引 | Release 页面含 xattr 命令 |

失败回退：最多重试 2 次（v0.2.2 和 v0.2.3），超过则记录问题待解决。
</verification>

<success_criteria>
1. release.yml 已改为 releaseDraft: false 并推送
2. v0.2.1 Release 自动发布，双架构 CI 绿色，产物完整（7 个文件）
3. v0.2.1 aarch64 DMG 安装到 /Applications 可正常运行
4. v0.2.2 Release 发布后，v0.2.1 app 检测到更新并弹出 UpdateDialog
5. 点击更新后下载安装成功，v0.2.2 正常运行（relaunch 可降级手动重启）
6. x86_64 CI 成功 + Release 含对应产物
7. latest.json 包含双架构下载 URL 和签名字段
</success_criteria>

<output>
完成后创建 `.planning/phases/13-e2e-verification/13-01-SUMMARY.md`
</output>
