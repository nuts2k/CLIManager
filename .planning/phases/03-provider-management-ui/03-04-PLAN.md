---
phase: 03-provider-management-ui
plan: 04
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - src/components/settings/SettingsPage.tsx
  - src/components/layout/AppShell.tsx
  - src/hooks/useSettings.ts
autonomous: false
requirements: [I18N-01, I18N-02, I18N-03]

must_haves:
  truths:
    - "Settings page accessible via gear icon in header"
    - "Language dropdown switches between Chinese and English, change takes effect immediately"
    - "Language preference persists to LocalSettings via backend"
    - "Test config section allows setting timeout and test model"
    - "About section shows app version"
    - "User can navigate back from settings to main view"
  artifacts:
    - path: "src/components/settings/SettingsPage.tsx"
      provides: "Settings page with language, test config, and about sections"
      contains: "SettingsPage"
      min_lines: 50
  key_links:
    - from: "src/components/settings/SettingsPage.tsx"
      to: "src/hooks/useSettings.ts"
      via: "updateSettings for language and test config persistence"
      pattern: "updateSettings"
    - from: "src/components/settings/SettingsPage.tsx"
      to: "src/i18n/index.ts"
      via: "i18n.changeLanguage() for runtime switch"
      pattern: "changeLanguage"
---

<objective>
Build the Settings page with language switching (Chinese/English), test configuration (timeout, test model), and About section. Wire it into the AppShell navigation. Then verify the complete UI end-to-end.

Purpose: This completes the i18n requirements (I18N-01/02/03) by providing a user-accessible language switch, and adds test configuration from the locked decisions. The checkpoint ensures the full UI works as designed.

Output: Complete settings page with persistent language switching, test config, and verified end-to-end UI flow.
</objective>

<execution_context>
@/Users/kelin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-provider-management-ui/03-CONTEXT.md
@.planning/phases/03-provider-management-ui/03-03-SUMMARY.md

<interfaces>
From src/hooks/useSettings.ts (Plan 03):
```typescript
export function useSettings(): {
  settings: LocalSettings | null;
  loading: boolean;
  refresh: () => Promise<void>;
  updateSettings: (partial: Partial<LocalSettings>) => Promise<void>;
  getActiveProviderId: (cliId: string) => string | null;
}
```

From src/i18n/index.ts (Plan 02):
```typescript
import i18n from "i18next";
// i18n.changeLanguage("en") / i18n.changeLanguage("zh")
```

From src/components/layout/AppShell.tsx (Plan 03):
```typescript
// view state: "main" | "settings"
// Settings gear click sets view to "settings"
// Need to render SettingsPage when view === "settings"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Settings page and wire into AppShell</name>
  <files>src/components/settings/SettingsPage.tsx, src/components/layout/AppShell.tsx, src/hooks/useSettings.ts</files>
  <action>
1. Create `src/components/settings/SettingsPage.tsx`:
   - Header with back button (arrow-left icon) + title (i18n `settings.title`)
   - **Language section:**
     - Label: i18n `settings.language`
     - Select dropdown with two options: "中文" (value "zh"), "English" (value "en")
     - Default value from `settings.language ?? "zh"`
     - On change: call `i18n.changeLanguage(newLang)` for immediate effect AND call `updateSettings({ ...settings, language: newLang })` to persist
   - **Test Config section:**
     - Label: i18n `settings.testConfig`
     - Timeout input (number, label: i18n `settings.timeout`, default 10)
     - Test model input (string, label: i18n `settings.testModel`, placeholder: "optional")
     - On change: debounce and call `updateSettings({ ...settings, test_config: { timeout_secs, test_model } })`
   - **About section:**
     - Label: i18n `settings.about`
     - Version: read from package.json or hardcode "0.1.0" (i18n `settings.version`)
     - App description text
   - Visual: Use Separator between sections, consistent padding, Card or section containers
   - All text uses i18n translations

2. Update `src/components/layout/AppShell.tsx`:
   - Import and render SettingsPage when `view === "settings"`
   - Pass `onBack` callback to SettingsPage that sets view back to "main"
   - Ensure settings changes (language) take effect across the entire app immediately

3. On app startup (in useSettings or AppShell), load persisted language from LocalSettings and call `i18n.changeLanguage(savedLanguage)` if it differs from default "zh". This ensures language preference survives app restarts.
  </action>
  <verify>
    <automated>cd /Users/kelin/Work/CLIManager && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>Settings page renders with language switch, test config, and about sections. Language changes apply immediately to all UI text. Language preference persists via LocalSettings. Back navigation works.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Provider Management UI end-to-end</name>
  <action>Human verifies the complete UI works as designed by testing all provider management workflows and i18n switching.</action>
  <what-built>Complete Provider Management UI with CLI tabs, provider CRUD, one-click switching, i18n, and settings page</what-built>
  <how-to-verify>
    1. Run `cd /Users/kelin/Work/CLIManager && pnpm tauri dev` to launch the app
    2. Verify window opens at ~1000x700 with dark theme
    3. Verify two tabs: "Claude Code" and "Codex" at top
    4. Create a test provider: click "New Provider", fill name/API key/Base URL, save
    5. Verify provider appears as card row with name and base_url
    6. Hover over card: verify action buttons slide in (switch, dropdown with edit/copy/copy-to/test/delete)
    7. Create a second provider, switch to it: verify blue indicator moves, Toast shows success
    8. Edit a provider: verify Dialog pre-fills data, save updates the card
    9. Delete a non-active provider: verify confirmation dialog, provider removed
    10. Delete the active provider: verify auto-switch to remaining provider
    11. Click gear icon: verify settings page opens
    12. Switch language to English: verify ALL text changes to English immediately
    13. Switch back to Chinese: verify all text returns to Chinese
    14. Close and reopen app: verify language preference persisted
    15. Test "Copy" action: verify new provider with "(copy)" suffix appears
    16. Test "Copy to..." action: verify provider appears in other CLI tab
    17. Verify empty state: delete all providers in one tab, verify friendly message + create button
  </how-to-verify>
  <verify>Human verification of UI behavior</verify>
  <done>All 17 verification steps pass. Provider management UI works end-to-end with CRUD, switch+patch, i18n, and settings.</done>
  <resume-signal>Type "approved" if the UI works as designed, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
```bash
cd /Users/kelin/Work/CLIManager && pnpm build && cd src-tauri && cargo test
```
Full build succeeds and all backend tests pass.
</verification>

<success_criteria>
- Settings page has language switch, test config, and about sections
- Language switching is instant and persists across app restarts
- Default language is Chinese (I18N-02)
- All UI text is localized (I18N-01)
- Language switchable in settings (I18N-03)
- Human verifies the complete end-to-end flow
</success_criteria>

<output>
After completion, create `.planning/phases/03-provider-management-ui/03-04-SUMMARY.md`
</output>
