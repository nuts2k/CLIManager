---
phase: 24-claude-settings-overlay-end-to-end
plan: 03
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - src-tauri/src/adapter/json_merge.rs
  - src-tauri/src/adapter/mod.rs
  - src-tauri/src/adapter/claude.rs
autonomous: true
requirements:
  - COVL-05
  - COVL-06
  - COVL-07
  - COVL-13
must_haves:
  truths:
    - "overlay apply 采用深度合并：object 递归合并、array 整体替换、scalar 覆盖"
    - "overlay 支持用 null 删除 key（删除 settings.json 中对应字段）"
    - "无论 overlay 如何设置，env.ANTHROPIC_AUTH_TOKEN 与 env.ANTHROPIC_BASE_URL 最终值始终由 Provider/Proxy patch 决定"
    - "当 ~/.claude/settings.json 不是合法 JSON 时，保持现有策略：拒绝写入并返回可见错误"
  artifacts:
    - path: "src-tauri/src/adapter/json_merge.rs"
      provides: "深度合并 + null 删除 + 保护字段剥离"
      contains: "merge_with_null_delete"
    - path: "src-tauri/src/adapter/claude.rs"
      provides: "patch 流程升级：provider/proxy patch + overlay merge + 最终强制保护字段"
      contains: "patch_claude_json"
  key_links:
    - from: "src-tauri/src/adapter/claude.rs"
      to: "src-tauri/src/adapter/json_merge.rs"
      via: "在 patch_claude_json 内调用"
      pattern: "merge_with_null_delete\("
    - from: "src-tauri/src/adapter/claude.rs"
      to: "src-tauri/src/storage/icloud.rs"
      via: "读取持久化 overlay 文本并解析为 JSON"
      pattern: "read_claude_settings_overlay\("
---

<objective>
实现 Claude settings.json overlay 的核心合并引擎，并将其强制集成到 ClaudeAdapter patch 流程中：在不破坏 surgical patch 核心价值的前提下，让持久化 overlay 的深度合并（含 null 删除）成为端到端 patch 的必经路径，同时保证保护字段始终由 Provider/Proxy 优先。

Purpose: 让“overlay + surgical patch”可共存且真正接到存储层：用户自定义配置会从 overlay 存储读出并生效，但认证与 base_url 不会被覆盖。
Output: json_merge 纯函数模块 + ClaudeAdapter patch 升级 + storage 读取链路接通。
</objective>

<execution_context>
@/Users/kelin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src-tauri/src/adapter/claude.rs
@src-tauri/src/adapter/mod.rs
@src-tauri/src/error.rs
@src-tauri/src/storage/icloud.rs
@.planning/phases/24-claude-settings-overlay-end-to-end/24-01-PLAN.md

<interfaces>
从 src-tauri/src/adapter/mod.rs（适配器接口）：
```rust
pub trait CliAdapter {
  fn patch(&self, provider: &Provider) -> Result<PatchResult, AppError>;
  fn clear(&self) -> Result<PatchResult, AppError>;
}
```

ClaudeAdapter 当前行为（src-tauri/src/adapter/claude.rs）：
- patch: 读取 settings.json（若存在必须是合法 JSON，否则 Validation error）
- 仅修改 env.ANTHROPIC_AUTH_TOKEN / env.ANTHROPIC_BASE_URL

Plan 24-01 必须提供的存储接口（本 Plan 直接依赖并消费，不可绕过）：
```rust
pub fn read_claude_settings_overlay() -> Result<(Option<String>, OverlayStorageInfo), AppError>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新增 adapter/json_merge.rs 实现深度合并 + null 删除 + 保护字段剥离</name>
  <files>src-tauri/src/adapter/json_merge.rs, src-tauri/src/adapter/mod.rs</files>
  <behavior>
- 合并规则：
  - base/object + overlay/object => 递归合并每个 key
  - base/any + overlay/array => overlay 数组整体替换
  - base/any + overlay/scalar => overlay 覆盖
  - overlay 的某个 key 为 null => 从 base object 删除该 key（如果不存在则 noop）
- 保护字段剥离：
  - 给定 overlay Value 与保护路径集合（本阶段固定为 env.ANTHROPIC_AUTH_TOKEN / env.ANTHROPIC_BASE_URL）
  - overlay 中出现上述字段：应从 overlay 中移除（忽略），同时返回被移除的路径列表供 UI 提示（后续 plan 会用）
- 合并函数不应在 root 非 object 时 panic：返回 AppError::Validation
  </behavior>
  <action>
1) 新建 `src-tauri/src/adapter/json_merge.rs`，实现以下 API（尽量小而清晰，便于 Phase 25 测试扩展）：

- `pub const PROTECTED_ENV_KEYS: [&str; 2] = ["ANTHROPIC_AUTH_TOKEN", "ANTHROPIC_BASE_URL"];`
- `pub struct StripResult { pub stripped_paths: Vec<String>, pub overlay: serde_json::Value }`
- `pub fn strip_protected_fields(overlay: &serde_json::Value) -> Result<StripResult, AppError>`
  - 仅处理 overlay root object；否则 Validation
  - 若 overlay.env 不是 object：不做深挖（保持 overlay 原样），但如果能明确找到保护字段则移除
  - stripped_paths 用 "env.ANTHROPIC_AUTH_TOKEN" 形式
- `pub fn merge_with_null_delete(base: &mut serde_json::Value, overlay: &serde_json::Value) -> Result<(), AppError>`
  - 要求 base 为 object；否则 Validation

2) 在 `src-tauri/src/adapter/mod.rs` 导出：`pub mod json_merge;`

3) 为 Task 的 TDD：在 json_merge.rs 内添加 `#[cfg(test)]` 单元测试覆盖上述 behavior（本阶段只需要覆盖到足以保护 Phase 24 的需求；更完整的覆盖会在 Phase 25）。
</action>
  <verify>
    <automated>cargo test -p cli-manager --lib adapter::json_merge -q</automated>
  </verify>
  <done>
- merge_with_null_delete 按规则工作，且有单测覆盖
- strip_protected_fields 能移除保护字段并返回 stripped_paths
- 模块已在 adapter/mod.rs 导出，供 ClaudeAdapter 引用
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 升级 ClaudeAdapter patch：强制读取 overlay 存储并完成 merge + 最终保护字段回写</name>
  <files>src-tauri/src/adapter/claude.rs</files>
  <behavior>
- patch 读取 existing settings.json：若存在但非法 JSON => 返回 Validation（COVL-13）
- patch 必须读取由 Plan 24-01 持久化的 overlay 文件；只要 overlay 存在，就必须走 strip + merge + 最终保护字段回写路径
- patch 仍然只由 Provider/Proxy 决定两个保护字段的最终值
- 若 overlay 存在：
  - 先 strip 掉 overlay 中的保护字段
  - 再将 overlay 深度合并进 root（含 null 删除）
  - 最后再次写回保护字段（保证优先级）
- overlay 不存在：行为应与当前完全一致
  </behavior>
  <action>
在 `src-tauri/src/adapter/claude.rs` 中升级 `patch_claude_json()` 逻辑，但必须保持“surgical patch”核心价值：不要无关字段重写为默认值。

实现要求（这里不允许“可选接入”“后续再接入”之类路径；本 Task 必须完成端到端读取与接线）：
1) 在 patch() / patch_claude_json() 内读取 existing JSON 为 Value（当前已有预校验）。
2) 直接调用 Plan 24-01 提供的 `read_claude_settings_overlay()` 读取 overlay 文本：
   - `None`：跳过 overlay merge，保持当前 patch 行为
   - `Some(text)`：解析为 `serde_json::Value`，且 root 必须为 object；否则返回 `AppError::Validation(...)`
3) 对 root 执行以下固定顺序：
   - 先确保 env object 存在（现有逻辑保留）
   - 调用 `strip_protected_fields()` 得到 stripped overlay
   - 调用 `merge_with_null_delete(&mut root, &stripped_overlay)`
   - 无论 overlay 是否存在，都在最后强制写回：
     - `env.ANTHROPIC_AUTH_TOKEN = provider.api_key`
     - `env.ANTHROPIC_BASE_URL = provider.base_url`
4) pretty 输出（serde_json::to_string_pretty）保持现有格式。
5) 为本 Task 补充/更新 adapter::claude 相关测试，明确证明：
   - overlay 中普通 env 字段会写入最终 settings.json
   - overlay 中 null 会删除目标 key
   - overlay 试图覆盖保护字段时最终仍以 provider 值为准
   - overlay 文件非法 JSON 或 root 非 object 时 patch 返回 Validation

注意：本 Task 不负责前端提示 stripped_paths，但必须把存储层读取、merge、保护字段优先这条链路接通，供 24-04 的 apply 事件复用。
</action>
  <verify>
    <automated>cargo test -p cli-manager --lib adapter::claude -q</automated>
  </verify>
  <done>
- ClaudeAdapter patch 已强制接入 overlay 存储读取路径，不再是可选/占位方案
- overlay 深度合并 + null 删除在真实 patch 流程中生效
- 保护字段最终值永远与 provider/proxy patch 一致
- overlay 文件非法 JSON/root 非 object、以及 settings.json 非法 JSON 时均拒绝写入并返回 Validation
- 相关单测通过
  </done>
</task>

</tasks>

<verification>
- `cargo test -p cli-manager --lib` 通过（至少 adapter 相关测试）
</verification>

<success_criteria>
- overlay 深度合并规则与 null 删除可在真实 patch 流程中生效
- ClaudeAdapter 会从持久化 overlay 存储读取内容并 merge，而不是保留未接线占位实现
- overlay 无法覆盖保护字段，且非法 settings.json 不会被静默覆盖
</success_criteria>

<output>
After completion, create `.planning/phases/24-claude-settings-overlay-end-to-end/24-03-SUMMARY.md`
</output>
