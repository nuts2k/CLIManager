---
phase: 24-claude-settings-overlay-end-to-end
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/settings/SettingsPage.tsx
  - src/lib/tauri.ts
  - src/i18n/locales/zh.json
  - src/i18n/locales/en.json
  - src/components/ui/textarea.tsx
autonomous: false
requirements:
  - COVL-01
  - COVL-02
  - COVL-04
  - COVL-08
must_haves:
  truths:
    - "用户在 Settings → Advanced → Claude 小节可编辑多行 JSON overlay"
    - "保存时 overlay JSON 会被校验：非法 JSON 或 root 非 object 会被拒绝且提示错误"
    - "用户能看到 overlay 当前存放位置（iCloud/本地降级）并知道是否跨设备同步"
    - "UI 明确提示保护字段（env.ANTHROPIC_AUTH_TOKEN / env.ANTHROPIC_BASE_URL）不可被 overlay 覆盖"
  artifacts:
    - path: "src/components/settings/SettingsPage.tsx"
      provides: "Advanced Tab 中 Claude overlay 编辑 UI"
    - path: "src/lib/tauri.ts"
      provides: "get/set overlay 的 invoke 封装"
    - path: "src/components/ui/textarea.tsx"
      provides: "统一风格的多行输入组件"
  key_links:
    - from: "src/components/settings/SettingsPage.tsx"
      to: "src/lib/tauri.ts"
      via: "调用 getClaudeSettingsOverlay / setClaudeSettingsOverlay"
      pattern: "getClaudeSettingsOverlay\(|setClaudeSettingsOverlay\("
---

<objective>
在 Settings → Advanced 增加 Claude overlay 编辑器：可多行编辑、保存前 JSON 校验、显示存储位置（iCloud/本地降级），并提示保护字段不可覆盖。

Purpose: 让用户在 UI 层完成 overlay 的编辑/校验/保存与状态感知，为后续 apply/自动对齐提供入口。
Output: Claude overlay UI + i18n 文案 + Tauri 前端封装。
</objective>

<execution_context>
@/Users/kelin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/settings/SettingsPage.tsx
@src/lib/tauri.ts
@src/components/ui/input.tsx

<interfaces>
从 src/components/settings/SettingsPage.tsx（现有结构）：
```tsx
<TabsContent value="advanced" className="flex-1 overflow-auto p-6 space-y-6">
  <section className="space-y-3">...</section>
  <Separator />
  ...
</TabsContent>
```

从 src/lib/tauri.ts（invoke 封装模式）：
```ts
export async function getLocalSettings(): Promise<LocalSettings> {
  return invoke("get_local_settings");
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 增加 UI Textarea 组件（风格对齐 Input）</name>
  <files>src/components/ui/textarea.tsx</files>
  <action>
新增 `Textarea` 组件，风格尽量与现有 `Input` 保持一致（border/bg/focus/aria-invalid 等），但适配多行：
- 基于 React.ComponentProps<"textarea"> 封装
- 默认 className 包含：min-h、w-full、rounded-md、border-input、dark:bg-input/30、focus-visible ring 等
- 支持 `aria-invalid` 样式与 Input 对齐

原因：Settings overlay 是多行 JSON 编辑，需要统一 UI 视觉与验证红框能力。
</action>
  <verify>
    <automated>pnpm -s build</automated>
  </verify>
  <done>
- Textarea 组件可在 SettingsPage 使用
- build 通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 前端增加 get/set Claude overlay 的 Tauri 调用封装 + 类型</name>
  <files>src/lib/tauri.ts</files>
  <action>
在 `src/lib/tauri.ts` 新增两个函数（与后端命令名一致）：
- `getClaudeSettingsOverlay(): Promise<{ overlay_json: string | null; storage: { location: "icloud" | "local_fallback"; file_path: string; sync_enabled: boolean; } }>`
- `setClaudeSettingsOverlay(overlayJson: string): Promise<{ storage: { ...同上... } }>`

注意：
- 仅做 invoke 封装与 TS 类型，不做 JSON 校验（校验由 UI + 后端双保险）。
- 命令名必须匹配 Rust：`get_claude_settings_overlay` / `set_claude_settings_overlay`。
</action>
  <verify>
    <automated>pnpm -s build</automated>
  </verify>
  <done>
- src/lib/tauri.ts 暴露 get/set overlay 函数，供 UI 调用
- TypeScript 编译通过
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 人工验证 Claude overlay 编辑/校验/保存/位置显示</name>
  <files>src/components/settings/SettingsPage.tsx, src/lib/tauri.ts</files>
  <action>
验证内容：Settings → Advanced 新增 Claude overlay 编辑器（多行 JSON + 校验 + 保存 + 存储位置显示 + 保护字段提示）。

操作步骤：
1. 启动应用：`pnpm tauri dev`
2. 打开 Settings → Advanced
3. 确认出现 Claude 小节：
   - 多行输入框（JSON overlay）
   - 显示“存储位置：iCloud / 本地（不同步）”之一，并展示文件路径
   - 有保护字段说明：ANTHROPIC_AUTH_TOKEN / ANTHROPIC_BASE_URL 不可覆盖
4. 输入非法 JSON（例如 `{`），点击保存：应出现错误 toast 且不保存
5. 输入 root 非 object 的合法 JSON（例如 `[]`），点击保存：应出现错误 toast
6. 输入合法 object JSON（例如 `{ "env": { "FOO": "BAR" } }`），点击保存：应成功 toast；重启应用或重新进入页面后内容仍存在
</action>
  <verify>
    <automated>pnpm -s build</automated>
  </verify>
  <done>
- 上述 6 步全部通过
- UI 对存储位置（iCloud/本地降级）与保护字段限制有清晰提示
  </done>
  <resume-signal>输入 "approved" 或描述你看到的问题（含截图/错误信息）</resume-signal>
</task>

</tasks>

<verification>
- `pnpm build` 通过
- 人工验证通过后，overlay UI 可用且错误提示明确
</verification>

<success_criteria>
- 用户可编辑/保存 overlay，保存时有 JSON 校验（非法 JSON/root 非 object 直接拒绝）
- UI 显示 overlay 存放位置（iCloud/本地降级）并说明是否同步
- UI 明示保护字段不可覆盖
</success_criteria>

<output>
After completion, create `.planning/phases/24-claude-settings-overlay-end-to-end/24-02-SUMMARY.md`
</output>
