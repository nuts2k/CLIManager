---
phase: 16-responses-api-provider-ui
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/provider.ts
  - src/components/provider/ProviderDialog.tsx
  - src/components/provider/ProviderTabs.tsx
  - src/hooks/useProviders.ts
  - src/i18n/locales/en.json
  - src/i18n/locales/zh.json
autonomous: false
requirements: [MODL-04, RAPI-01]
must_haves:
  truths:
    - "ProtocolType 下拉三个平级选项：Anthropic / OpenAI Chat Completions / OpenAI Responses"
    - "仅选择 OpenAI 类型时显示默认模型和模型映射 UI，Anthropic 时隐藏"
    - "默认目标模型：单独输入框，placeholder 如 gpt-4o"
    - "模型映射对：动态行列表，每行两个输入框 + 删除按钮，底部添加按钮"
    - "保存后 upstream_model 和 upstream_model_map 正确传递到 Rust 后端"
    - "旧 open_ai_compatible 值加载时自动映射为 open_ai_chat_completions"
    - "编辑已有 Provider 时正确回显 upstream_model 和 upstream_model_map"
  artifacts:
    - path: "src/types/provider.ts"
      provides: "ProtocolType 三变体 + Provider 接口扩展"
      contains: "open_ai_chat_completions"
    - path: "src/components/provider/ProviderDialog.tsx"
      provides: "协议选项三选 + 模型映射 UI"
      min_lines: 300
    - path: "src/components/provider/ProviderTabs.tsx"
      provides: "保存逻辑传递 upstream_model 和 upstream_model_map"
  key_links:
    - from: "src/components/provider/ProviderDialog.tsx"
      to: "src/types/provider.ts"
      via: "ProtocolType 和 Provider 类型导入"
      pattern: "import.*ProtocolType.*from.*provider"
    - from: "src/components/provider/ProviderTabs.tsx"
      to: "src/lib/tauri.ts"
      via: "updateProvider 传递新字段"
      pattern: "upstream_model|upstream_model_map"
---

<objective>
扩展 Provider 编辑界面，支持三协议选择和模型映射配置。

Purpose: 用户需要在创建/编辑 Provider 时选择目标 API 格式（Anthropic / Chat Completions / Responses），并配置模型名映射规则（默认模型 + 精确映射对），这些配置直接影响代理转换行为。
Output: 前端类型更新 + ProviderDialog UI 扩展 + 保存逻辑扩展
</objective>

<execution_context>
@/Users/kelin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-responses-api-provider-ui/16-CONTEXT.md
@.planning/phases/16-responses-api-provider-ui/16-RESEARCH.md

<interfaces>
<!-- 现有前端类型（需要扩展） -->
From src/types/provider.ts:
```typescript
export type ProtocolType = "anthropic" | "open_ai_compatible";

export interface Provider {
  id: string;
  name: string;
  cli_id: string;
  protocol_type: ProtocolType;
  api_key: string;
  base_url: string;
  model: string;
  model_config?: ModelConfig | null;
  notes?: string | null;
  created_at: number;
  updated_at: number;
  schema_version: number;
}

export interface CreateProviderInput {
  name: string;
  protocolType: ProtocolType;
  apiKey: string;
  baseUrl: string;
  model: string;
  cliId: string;
}
```

<!-- 现有 ProviderFormData（需要扩展） -->
From src/components/provider/ProviderDialog.tsx:
```typescript
export interface ProviderFormData {
  name: string;
  apiKey: string;
  baseUrl: string;
  model: string;
  protocolType: ProtocolType;
  notes: string;
  haikuModel: string;
  sonnetModel: string;
  opusModel: string;
  reasoningEffort: string;
}
```

<!-- 保存逻辑（需要扩展） -->
From src/components/provider/ProviderTabs.tsx handleSave:
```typescript
// 编辑模式下调用 updateProvider，传递 Provider 对象
await updateProvider({
  ...editingProvider,
  name: data.name,
  // ... 其他字段
  model_config: modelConfig,
});
```

<!-- Rust 后端 Provider struct（已支持新字段） -->
From src-tauri/src/provider.rs:
```rust
pub struct Provider {
  // ... 现有字段 ...
  pub upstream_model: Option<String>,
  pub upstream_model_map: Option<HashMap<String, String>>,
}
```

<!-- 后端 updateProvider 接收完整 Provider 对象，已支持 upstream_model/upstream_model_map -->
From src/lib/tauri.ts:
```typescript
export async function updateProvider(provider: Provider): Promise<Provider>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 前端类型更新 + ProviderDialog UI 扩展 + 保存逻辑</name>
  <files>src/types/provider.ts, src/components/provider/ProviderDialog.tsx, src/components/provider/ProviderTabs.tsx, src/hooks/useProviders.ts, src/i18n/locales/en.json, src/i18n/locales/zh.json</files>
  <action>
    1. **provider.ts 类型更新**：
       - `ProtocolType` 更新为 `"anthropic" | "open_ai_chat_completions" | "open_ai_responses"`（删除旧 `"open_ai_compatible"`）
       - `Provider` 接口新增：`upstream_model?: string | null;` 和 `upstream_model_map?: Record<string, string> | null;`
       - `CreateProviderInput` 保持不变（创建时不需要 upstream 字段，创建后通过 updateProvider 设置）

    2. **ProviderDialog.tsx 扩展**：
       - `ProviderFormData` 新增：`upstreamModel: string` 和 `upstreamModelMap: Array<{ source: string; target: string }>`
       - 表单初始状态新增 `upstreamModel: ""` 和 `upstreamModelMap: []`
       - 编辑模式初始化：从 `provider.upstream_model`、`provider.upstream_model_map`（Record → Array 转换）回显
       - 旧值兼容：表单初始化时若 `provider.protocol_type === "open_ai_compatible"`，映射为 `"open_ai_chat_completions"`
       - Select 组件更新为三个选项：
         ```
         <SelectItem value="anthropic">Anthropic</SelectItem>
         <SelectItem value="open_ai_chat_completions">OpenAI Chat Completions</SelectItem>
         <SelectItem value="open_ai_responses">OpenAI Responses</SelectItem>
         ```
       - 条件显示模型映射区域（protocol_type 为 open_ai_chat_completions 或 open_ai_responses 时）：
         - 默认目标模型输入框（placeholder: "gpt-4o"），Label 用 t("provider.upstreamModel")
         - 模型映射对动态列表：每行 source 输入框（placeholder: "claude-sonnet-4-20250514"）+ target 输入框（placeholder: "gpt-4o"）+ X 删除按钮
         - 底部 "+ 添加映射" 按钮
       - 辅助函数：`addModelMapEntry()`、`removeModelMapEntry(idx)`、`updateModelMapEntry(idx, field, value)`
       - 注意：模型映射 UI 放在 Collapsible 高级设置区域内，protocol_type Select 下方

    3. **ProviderTabs.tsx 保存逻辑扩展**：
       - handleSave 中处理 upstream 字段：
         ```typescript
         const upstreamModel = data.upstreamModel || null;
         const upstreamModelMap = data.upstreamModelMap.length > 0
           ? Object.fromEntries(data.upstreamModelMap.filter(p => p.source && p.target).map(p => [p.source, p.target]))
           : null;
         ```
       - 创建模式：createProvider 后 updateProvider 时传入 upstream_model 和 upstream_model_map
       - 编辑模式：updateProvider 时传入 upstream_model 和 upstream_model_map
       - 注意：updateProvider 接收完整 Provider 对象，直接添加新字段即可

    4. **useProviders.ts 扩展**：
       - copyProvider 和 copyProviderTo 中 updateProvider 调用时传递 upstream_model 和 upstream_model_map

    5. **i18n 更新**：
       - en.json 新增：
         - protocol.openAiChatCompletions: "OpenAI Chat Completions"
         - protocol.openAiResponses: "OpenAI Responses"
         - provider.upstreamModel: "Default Target Model"
         - provider.modelMapping: "Model Name Mapping"
         - provider.addMapping: "Add Mapping"
         - provider.sourceModel: "Source Model"
         - provider.targetModel: "Target Model"
       - zh.json 新增：
         - protocol.openAiChatCompletions: "OpenAI Chat Completions"
         - protocol.openAiResponses: "OpenAI Responses"
         - provider.upstreamModel: "默认目标模型"
         - provider.modelMapping: "模型名映射"
         - provider.addMapping: "添加映射"
         - provider.sourceModel: "源模型"
         - provider.targetModel: "目标模型"
       - 删除旧的 protocol.openAiCompatible key（前端不再使用）

    关键注意事项：
    - 直接使用技术名称 "OpenAI Chat Completions" / "OpenAI Responses"，中英文相同（用户决策）
    - 保存反馈复用现有 Provider 保存 toast，无需额外提示（用户决策）
    - 映射数据随 Provider 一起保存，前端 Array → Record 转换后传递（用户决策 + Pitfall 5 防范）
    - lucide-react 中导入 X 图标用于删除按钮（已有 Plus 可不导入，用文本 "+ 添加映射"）
  </action>
  <verify>
    <automated>cd /Users/kelin/Workspace/CLIManager && pnpm tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>
    - provider.ts 类型更新完成：ProtocolType 三变体 + Provider 新增 upstream_model/upstream_model_map
    - ProviderDialog 显示三个协议选项，OpenAI 类型时显示模型映射 UI
    - ProviderTabs 保存逻辑正确传递新字段到后端
    - i18n 中英文新增完成
    - TypeScript 编译无错误
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 验证 Provider UI 三协议选择 + 模型映射交互</name>
  <files>src/components/provider/ProviderDialog.tsx</files>
  <action>人工验证 Provider 编辑界面三协议选择 + 模型映射 UI 的交互效果和数据持久化</action>
  <verify>
    <automated>cd /Users/kelin/Workspace/CLIManager && pnpm tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>用户确认 UI 交互和数据保存符合预期</done>
  <what-built>Provider 编辑界面三协议选择 + 模型映射 UI</what-built>
  <how-to-verify>
    1. 启动应用：`cd /Users/kelin/Workspace/CLIManager && pnpm tauri dev`
    2. 打开 Provider 编辑对话框（创建或编辑任意 Provider）
    3. 展开高级设置，确认 Protocol Type 下拉显示三个选项：Anthropic / OpenAI Chat Completions / OpenAI Responses
    4. 选择 "OpenAI Chat Completions" 或 "OpenAI Responses"，确认下方出现：
       - "默认目标模型" 输入框（placeholder: gpt-4o）
       - "模型名映射" 区域
    5. 点击 "+ 添加映射"，确认新增一行映射（两个输入框 + 删除按钮）
    6. 填写映射对，保存 Provider
    7. 重新打开编辑，确认映射数据回显正确
    8. 选择 "Anthropic"，确认模型映射区域隐藏
    9. （如有旧 open_ai_compatible Provider）打开编辑，确认自动显示为 OpenAI Chat Completions
  </how-to-verify>
  <resume-signal>输入 "approved" 或描述发现的问题</resume-signal>
</task>

</tasks>

<verification>
- pnpm tsc --noEmit 无错误
- 应用 UI 正常显示三协议选项
- 模型映射增删改查 + 保存回显正常
</verification>

<success_criteria>
- Protocol Type 下拉三选项正确显示
- OpenAI 类型时模型映射 UI 可见，Anthropic 时隐藏
- 保存后数据正确传递到后端（upstream_model + upstream_model_map）
- 编辑旧 Provider 时 open_ai_compatible 自动映射为 open_ai_chat_completions
</success_criteria>

<output>
After completion, create `.planning/phases/16-responses-api-provider-ui/16-03-SUMMARY.md`
</output>
