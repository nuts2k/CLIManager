---
phase: 19-provider
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/provider/ProviderDialog.tsx
  - src/i18n/locales/zh.json
  - src/i18n/locales/en.json
autonomous: true
requirements: [EDIT-01, EDIT-02, EDIT-03]

must_haves:
  truths:
    - "编辑对话框宽度明显大于当前版本（max-w-xl ~576px），长表单可纵向滚动"
    - "表单字段分为基础信息、协议设置、模型配置三个视觉分区，分区之间有标题+分割线"
    - "Collapsible 折叠已移除，三个分区全部平铺展开"
    - "DialogHeader 和 DialogFooter 固定，中间表单区域独立滚动"
    - "必填字段验证失败时，错误提示红色小字显示在字段下方，输入框有红色边框"
    - "所有字段有合理的 placeholder 提示，ModelConfig placeholder 已国际化"
  artifacts:
    - path: "src/components/provider/ProviderDialog.tsx"
      provides: "重构后的 Provider 编辑对话框（加宽+滚动+分组+验证优化）"
      contains: "max-w-xl"
    - path: "src/i18n/locales/zh.json"
      provides: "新增分区标题和 placeholder 翻译 key"
      contains: "section.basic"
    - path: "src/i18n/locales/en.json"
      provides: "新增分区标题和 placeholder 翻译 key 英文版"
      contains: "section.basic"
  key_links:
    - from: "src/components/provider/ProviderDialog.tsx"
      to: "src/i18n/locales/zh.json"
      via: "t() 函数引用新增翻译 key"
      pattern: "t\\(\"section\\."
    - from: "src/components/provider/ProviderDialog.tsx"
      to: "src/components/ui/dialog.tsx"
      via: "DialogContent className 覆盖宽度"
      pattern: "max-w-xl"
---

<objective>
重构 ProviderDialog 布局：对话框加宽至 max-w-xl、移除 Collapsible 改为三分区平铺、固定 Header/Footer 中间表单可滚动、字段 placeholder 国际化、验证错误红色边框。

Purpose: 让 Provider 编辑体验更宽敞清晰，字段分组一目了然，验证反馈直观明确。
Output: 重构后的 ProviderDialog.tsx + 更新的 i18n 文件。
</objective>

<execution_context>
@/Users/kelin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-provider/19-CONTEXT.md

@src/components/provider/ProviderDialog.tsx
@src/components/ui/dialog.tsx
@src/i18n/locales/zh.json
@src/i18n/locales/en.json

<interfaces>
<!-- ProviderDialog 当前接口 — 保持不变，只改内部布局 -->

From src/components/provider/ProviderDialog.tsx:
```typescript
interface ProviderDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  mode: "create" | "edit";
  provider?: Provider | null;
  cliId: string;
  onSave: (data: ProviderFormData) => Promise<void>;
}

export interface ProviderFormData {
  name: string;
  apiKey: string;
  baseUrl: string;
  model: string;
  testModel: string;
  protocolType: ProtocolType;
  notes: string;
  haikuModel: string;
  sonnetModel: string;
  opusModel: string;
  reasoningEffort: string;
  upstreamModel: string;
  upstreamModelMap: ModelMapEntry[];
}
```

From src/components/ui/dialog.tsx:
```typescript
// DialogContent 支持 className 覆盖
function DialogContent({ className, children, showCloseButton, ...props })
// DialogHeader / DialogFooter 为普通 div wrapper
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 对话框加宽 + 移除 Collapsible + 三分区平铺 + 固定 Header/Footer 滚动</name>
  <files>src/components/provider/ProviderDialog.tsx, src/i18n/locales/zh.json, src/i18n/locales/en.json</files>
  <action>
重构 ProviderDialog.tsx 布局结构，按以下步骤进行：

**1. 对话框加宽（EDIT-01）：**
- DialogContent className 从 `sm:max-w-md` 改为 `max-w-xl`

**2. 固定 Header/Footer，中间表单滚动（EDIT-01）：**
- 将 DialogContent 内部结构改为 flex flex-col，限制最大高度（max-h-[85vh]）
- DialogHeader 保持在顶部（flex-shrink-0）
- 中间表单区域用 div 包裹，加 `overflow-y-auto flex-1 min-h-0`，内部保持 `flex flex-col gap-4 px-1`（px-1 为滚动条留空间）
- DialogFooter 保持在底部（flex-shrink-0），加上 `border-t pt-4` 分隔

**3. 移除 Collapsible，改三分区平铺（EDIT-02）：**
- 删除 Collapsible/CollapsibleContent/CollapsibleTrigger 的 import 和使用
- 删除 `advancedOpen` state 和 `setAdvancedOpen`
- 删除 handleSave 中的 `setAdvancedOpen(true)` 行
- 删除 ChevronDown import
- 将所有字段从 Collapsible 中解放出来，平铺到三个分区

**4. 三分区标题 + 分割线（EDIT-02）：**
按 CONTEXT.md 字段归属组织三个分区：

分区 1 — 基础信息（t("section.basic")）：
- name, apiKey, baseUrl（这三个已在 Collapsible 外部，保持位置不变）

分区 2 — 协议设置（t("section.protocol")）：
- protocolType, upstreamModel（条件渲染）, upstreamModelMap（条件渲染）

分区 3 — 模型配置（t("section.model")）：
- model, testModel, haikuModel/sonnetModel/opusModel/reasoningEffort（2x2 grid 保持），notes（放最末尾）

每个分区前加标题和分割线：
```tsx
{/* 分区标题 */}
<div className="flex items-center gap-2 pt-2">
  <span className="text-sm font-semibold text-muted-foreground">{t("section.basic")}</span>
  <div className="flex-1 border-t border-border" />
</div>
```
第一个分区（基础信息）不需要 pt-2，因为它是顶部。

**5. 字段布局调整（EDIT-02）：**
- model config 的 4 个字段（haiku/sonnet/opus/reasoningEffort）保持 2x2 grid 紧凑布局
- 其余字段单列全宽（已有）
- 移除原有的 `<Label className="text-muted-foreground text-xs">{t("provider.modelConfig")}</Label>` 分组标签（由分区标题替代）
- 同理移除模型映射的旧分组标签

**6. 添加 i18n 翻译 key：**
在 zh.json 和 en.json 的 `provider` 或新增 `section` 命名空间中添加：
- `section.basic`: 中文 "基础信息" / 英文 "Basic Info"
- `section.protocol`: 中文 "协议设置" / 英文 "Protocol"
- `section.model`: 中文 "模型配置" / 英文 "Model Config"
  </action>
  <verify>
    <automated>cd /Users/kelin/Workspace/CLIManager && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
- 对话框使用 max-w-xl 宽度
- Collapsible 完全移除（无 import、无 state、无 JSX）
- 三个分区（基础信息/协议设置/模型配置）各有标题+分割线
- DialogHeader 和 DialogFooter 固定，中间表单可纵向滚动
- 字段归属正确：name/apiKey/baseUrl → 基础信息，protocolType/upstreamModel/upstreamModelMap → 协议设置，model/testModel/haikuModel/sonnetModel/opusModel/reasoningEffort/notes → 模型配置
- 中英文翻译 key 已添加
- `npm run build` 编译通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 字段 placeholder 国际化 + 验证错误红色边框</name>
  <files>src/components/provider/ProviderDialog.tsx, src/i18n/locales/zh.json, src/i18n/locales/en.json</files>
  <action>
**1. 字段 placeholder 国际化（EDIT-03）：**
为以下字段添加有意义的 placeholder（通过 t() 翻译）：

- name: placeholder={t("placeholder.name")} — 中文 "如：Claude Pro" / 英文 "e.g. Claude Pro"
- apiKey: placeholder={t("placeholder.apiKey")} — 中文 "sk-..." / 英文 "sk-..."
- baseUrl: placeholder={t("placeholder.baseUrl")} — 中文 "https://api.anthropic.com" / 英文 "https://api.anthropic.com"
- model: placeholder={t("placeholder.model")} — 中文 "如：claude-sonnet-4-6" / 英文 "e.g. claude-sonnet-4-6"
- notes: placeholder={t("placeholder.notes")} — 中文 "可选备注信息" / 英文 "Optional notes"

ModelConfig 的 4 个字段 placeholder 改为国际化（当前是硬编码英文 "haiku_model" 等）：
- haikuModel: placeholder={t("placeholder.haikuModel")} — 中文 "Haiku 模型名" / 英文 "Haiku model name"
- sonnetModel: placeholder={t("placeholder.sonnetModel")} — 中文 "Sonnet 模型名" / 英文 "Sonnet model name"
- opusModel: placeholder={t("placeholder.opusModel")} — 中文 "Opus 模型名" / 英文 "Opus model name"
- reasoningEffort: placeholder={t("placeholder.reasoningEffort")} — 中文 "推理力度（low/medium/high）" / 英文 "low / medium / high"

在 zh.json 和 en.json 中新增 `placeholder` 命名空间，包含以上所有 key。

**2. 验证错误红色边框（EDIT-03）：**
当前 name/apiKey/baseUrl 输入框已有 `aria-invalid={!!errors.xxx}`，但需要确保视觉上有红色边框。
- 检查 Input 组件是否已针对 `aria-invalid` 应用 border-destructive 样式
- 如果 Input 组件未内置 aria-invalid 样式，则在 ProviderDialog.tsx 中为这些输入框添加条件 className：
  `className={cn(errors.xxx && "border-destructive")}` 或类似方式
- 同样处理 upstreamModel 输入框的验证错误红色边框
- 确保红色错误文字（已有 `text-xs text-destructive`）和红色边框同时出现
  </action>
  <verify>
    <automated>cd /Users/kelin/Workspace/CLIManager && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
- 所有字段都有通过 t() 引用的 placeholder 提示
- ModelConfig 的 4 个 placeholder 从硬编码英文改为国际化
- 验证失败时输入框显示红色边框（border-destructive）
- 中英文翻译 key 完整
- `npm run build` 编译通过
  </done>
</task>

</tasks>

<verification>
- `npm run build` 编译通过（无 TypeScript/import 错误）
- ProviderDialog.tsx 中无 Collapsible 相关 import 或使用
- ProviderDialog.tsx 中包含 `max-w-xl`
- ProviderDialog.tsx 中包含三个分区标题（section.basic / section.protocol / section.model）
- ProviderDialog.tsx 中包含 `overflow-y-auto`（中间表单滚动）
- zh.json 和 en.json 包含 section.* 和 placeholder.* 翻译 key
- 验证错误显示红色边框（border-destructive）
</verification>

<success_criteria>
1. 对话框宽度 max-w-xl（~576px），明显宽于原来的 sm:max-w-md（~448px）
2. 三个分区（基础信息/协议设置/模型配置）各有标题+分割线，视觉分隔清晰
3. Collapsible 完全移除，所有字段平铺可见
4. Header/Footer 固定，中间表单区域可纵向滚动
5. 必填字段验证失败时红色边框+红色文字同时出现
6. 所有字段有国际化的 placeholder 提示
</success_criteria>

<output>
After completion, create `.planning/phases/19-provider/19-01-SUMMARY.md`
</output>
