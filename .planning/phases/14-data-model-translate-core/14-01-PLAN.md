---
phase: 14-data-model-translate-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/src/provider.rs
  - src-tauri/src/proxy/state.rs
  - src-tauri/src/proxy/mod.rs
  - src-tauri/src/proxy/error.rs
  - src-tauri/src/proxy/handler.rs
  - src-tauri/src/proxy/translate/mod.rs
autonomous: true
requirements: [MODL-01, MODL-02]

must_haves:
  truths:
    - "Provider JSON 新增 upstream_model 和 upstream_model_map 字段，旧文件（无这两个字段）反序列化不崩溃，字段取默认值 None"
    - "ProtocolType 扩展为三变体（Anthropic, OpenAiChatCompletions, OpenAiResponses），旧 JSON 中的 open_ai_compatible 通过 alias 正确反序列化为 OpenAiChatCompletions"
    - "ProtocolType::OpenAiChatCompletions 序列化为 open_ai_chat_completions（snake_case）"
    - "ProxyError 新增 TranslateError variant，translate 模块骨架（mod.rs）可编译"
    - "Cargo.toml 新增 bytes、futures、async-stream 显式依赖，cargo check 通过"
  artifacts:
    - path: "src-tauri/src/provider.rs"
      provides: "ProtocolType 三变体 + Provider upstream_model/upstream_model_map 字段"
      contains: "upstream_model"
    - path: "src-tauri/src/proxy/translate/mod.rs"
      provides: "translate 子模块声明骨架"
      min_lines: 3
    - path: "src-tauri/src/proxy/error.rs"
      provides: "TranslateError variant"
      contains: "TranslateError"
    - path: "src-tauri/Cargo.toml"
      provides: "bytes + futures + async-stream 显式依赖"
      contains: "async-stream"
  key_links:
    - from: "src-tauri/src/proxy/mod.rs"
      to: "src-tauri/src/proxy/translate/mod.rs"
      via: "pub mod translate"
      pattern: "pub mod translate"
    - from: "src-tauri/src/proxy/handler.rs"
      to: "src-tauri/src/provider.rs"
      via: "ProtocolType match arms"
      pattern: "ProtocolType::"
---

<objective>
Provider 数据模型扩展 + 转换模块基础设施搭建

Purpose: 为 Wave 2 的三路并行转换模块（request/response/stream）奠定 schema 基础和模块骨架。MODL-01/MODL-02 定义了代理转换使用的模型映射字段，ProtocolType 三变体是后续协议路由的判断依据。
Output: provider.rs 新字段 + ProtocolType 扩展、translate/ 模块骨架、Cargo.toml 新依赖、ProxyError::TranslateError variant
</objective>

<execution_context>
@/Users/kelin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src-tauri/src/provider.rs
@src-tauri/src/proxy/mod.rs
@src-tauri/src/proxy/state.rs
@src-tauri/src/proxy/error.rs
@src-tauri/src/proxy/handler.rs
@src-tauri/Cargo.toml

<interfaces>
<!-- 当前 provider.rs 关键类型（将被修改） -->

From src-tauri/src/provider.rs:
```rust
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
#[serde(rename_all = "snake_case")]
pub enum ProtocolType {
    Anthropic,
    OpenAiCompatible,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
pub struct Provider {
    pub id: String,
    pub cli_id: String,
    pub name: String,
    pub protocol_type: ProtocolType,
    pub api_key: String,
    pub base_url: String,
    pub model: String,
    pub model_config: Option<ModelConfig>,
    pub notes: Option<String>,
    pub created_at: i64,
    pub updated_at: i64,
    pub schema_version: u32,
}
```

From src-tauri/src/proxy/state.rs:
```rust
pub struct UpstreamTarget {
    pub api_key: String,
    pub base_url: String,
    pub protocol_type: ProtocolType,
}
```

From src-tauri/src/proxy/error.rs:
```rust
pub enum ProxyError {
    AlreadyRunning,
    NotRunning,
    BindFailed(String),
    StopTimeout,
    StopFailed(String),
    UpstreamUnreachable(String),
    NoUpstreamConfigured,
    HealthCheckFailed(String),
    Internal(String),
}
```

From src-tauri/src/proxy/handler.rs (line 92-99, 凭据注入 match):
```rust
match upstream.protocol_type {
    ProtocolType::Anthropic => {
        req_builder = req_builder.header("x-api-key", &upstream.api_key);
    }
    ProtocolType::OpenAiCompatible => {
        req_builder = req_builder.header("Authorization", format!("Bearer {}", upstream.api_key));
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Provider 数据模型扩展（ProtocolType 三变体 + upstream 映射字段）</name>
  <files>src-tauri/src/provider.rs, src-tauri/src/proxy/state.rs, src-tauri/src/proxy/handler.rs</files>
  <behavior>
    - Test: ProtocolType::OpenAiChatCompletions 序列化为 "open_ai_chat_completions"
    - Test: "open_ai_compatible" JSON 字符串反序列化为 ProtocolType::OpenAiChatCompletions（serde alias 向前兼容）
    - Test: ProtocolType::OpenAiResponses 序列化为 "open_ai_responses"
    - Test: Provider 新增 upstream_model: Option<String> 字段，含值时序列化输出，None 时 skip
    - Test: Provider 新增 upstream_model_map: Option<HashMap<String, String>> 字段，含值时序列化输出，None 时 skip
    - Test: 不含 upstream_model/upstream_model_map 的旧 JSON 反序列化成功，两字段为 None
    - Test: Provider round-trip（含新字段）序列化-反序列化一致
  </behavior>
  <action>
    1. 修改 `src-tauri/src/provider.rs`:
       - 在文件顶部添加 `use std::collections::HashMap;`
       - 将 `ProtocolType::OpenAiCompatible` 重命名为 `OpenAiChatCompletions`，添加 `#[serde(alias = "open_ai_compatible")]`
       - 新增 `ProtocolType::OpenAiResponses` 变体
       - 在 `Provider` struct 中新增两个字段（在 `notes` 之后、`created_at` 之前）：
         ```rust
         #[serde(default, skip_serializing_if = "Option::is_none")]
         pub upstream_model: Option<String>,
         #[serde(default, skip_serializing_if = "Option::is_none")]
         pub upstream_model_map: Option<HashMap<String, String>>,
         ```
       - 更新 `Provider::new()` 初始化两个新字段为 None
       - 更新现有测试 `test_protocol_type_openai_compatible_serde`：改为验证 alias 向前兼容（"open_ai_compatible" 反序列化为 OpenAiChatCompletions）
       - 新增测试覆盖上述 behavior 列表

    2. 修改 `src-tauri/src/proxy/handler.rs`:
       - handler.rs 第 96 行 `ProtocolType::OpenAiCompatible` 改为 `ProtocolType::OpenAiChatCompletions`
       - 对 `ProtocolType::OpenAiResponses` 暂时也使用 Bearer token 认证（与 OpenAiChatCompletions 相同逻辑），Phase 16 再细化

    3. 修改 `src-tauri/src/proxy/state.rs`:
       - 测试中引用 `ProtocolType::OpenAiCompatible` 的地方改为 `ProtocolType::OpenAiChatCompletions`

    注意：不修改 `normalize_origin_base_url()`（不在此 task 范围，新增的端点重写函数在 Plan 02 request.rs 中实现）。
  </action>
  <verify>
    <automated>cd /Users/kelin/Workspace/CLIManager/src-tauri && cargo test --package cli-manager provider::tests -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>ProtocolType 三变体编译通过，旧 "open_ai_compatible" JSON 可反序列化，Provider 新字段 serde round-trip 正确，handler.rs 和 state.rs 中所有 OpenAiCompatible 引用已更新，全套现有测试通过</done>
</task>

<task type="auto">
  <name>Task 2: 转换模块骨架 + 依赖声明 + ProxyError 扩展</name>
  <files>src-tauri/Cargo.toml, src-tauri/src/proxy/translate/mod.rs, src-tauri/src/proxy/error.rs, src-tauri/src/proxy/mod.rs</files>
  <action>
    1. 修改 `src-tauri/Cargo.toml`，在 `[dependencies]` 中新增：
       ```toml
       bytes = "1"
       futures = "0.3"
       async-stream = "0.3"
       ```
       注意：`futures` 同时保留在 `[dev-dependencies]` 中（现有代码已在测试中使用）。

    2. 创建 `src-tauri/src/proxy/translate/mod.rs`：
       ```rust
       //! 协议转换模块：Anthropic Messages API <-> OpenAI Chat Completions API
       //!
       //! 三个子模块：
       //! - request: Anthropic 请求 → OpenAI 请求（anthropic_to_openai）
       //! - response: OpenAI 非流式响应 → Anthropic 响应（openai_to_anthropic）
       //! - stream: OpenAI SSE 流 → Anthropic SSE 流（create_anthropic_sse_stream）

       pub mod request;
       pub mod response;
       pub mod stream;
       ```
       同时创建空的占位文件（确保编译通过）：
       - `src-tauri/src/proxy/translate/request.rs` — 空文件或仅含注释
       - `src-tauri/src/proxy/translate/response.rs` — 空文件或仅含注释
       - `src-tauri/src/proxy/translate/stream.rs` — 空文件或仅含注释

    3. 修改 `src-tauri/src/proxy/mod.rs`，在 `pub mod state;` 之后添加 `pub mod translate;`

    4. 修改 `src-tauri/src/proxy/error.rs`，在 `ProxyError` enum 中新增：
       ```rust
       #[error("转换错误: {0}")]
       TranslateError(String),
       ```
       在 `IntoResponse` impl 的 match 中，为 `TranslateError` 返回 `(StatusCode::BAD_REQUEST, self.to_string())`（转换失败意味着请求内容无法处理，400 最合适）。

    5. 运行 `cargo check` 和 `cargo test` 确认全部编译通过、全套测试通过。
  </action>
  <verify>
    <automated>cd /Users/kelin/Workspace/CLIManager/src-tauri && cargo test --package cli-manager 2>&1 | tail -5</automated>
  </verify>
  <done>Cargo.toml 含 bytes/futures/async-stream 三个新依赖，translate/ 目录含 mod.rs + 三个占位子模块，proxy/mod.rs 声明 pub mod translate，ProxyError 含 TranslateError variant，cargo check 和全套测试通过（含现有 221+ 个测试）</done>
</task>

</tasks>

<verification>
1. `cargo check --package cli-manager` — 编译通过，无 warning（除已知的 dead_code）
2. `cargo test --package cli-manager` — 全套测试通过（221+ 个）
3. `cargo test --package cli-manager provider::tests` — 含新增的 ProtocolType 三变体测试和 upstream 字段测试
4. 验证 serde alias 向前兼容：旧 "open_ai_compatible" JSON 字符串可正确反序列化
</verification>

<success_criteria>
- ProtocolType 有三个变体：Anthropic, OpenAiChatCompletions, OpenAiResponses
- Provider struct 含 upstream_model 和 upstream_model_map 两个 Option 字段
- 旧 Provider JSON（无新字段）反序列化成功，新字段默认为 None
- translate/ 模块骨架就绪（mod.rs + 三个空子模块），编译通过
- Cargo.toml 含 bytes + futures + async-stream 依赖
- ProxyError::TranslateError 可用
- 全套现有测试通过，无回归
</success_criteria>

<output>
After completion, create `.planning/phases/14-data-model-translate-core/14-01-SUMMARY.md`
</output>
