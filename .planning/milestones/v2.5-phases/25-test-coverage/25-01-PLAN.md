---
phase: 25-test-coverage
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/adapter/json_merge.rs
  - src-tauri/src/adapter/claude.rs
autonomous: true
requirements: [COVL-14, COVL-15, COVL-16]

must_haves:
  truths:
    - "cargo test adapter::json_merge 全部通过，且覆盖空对象无副作用、嵌套 null 删除两个边界用例"
    - "cargo test adapter::claude 全部通过，且覆盖保护字段+自定义字段共存、序贯 patch 保护字段始终正确、overlay+clear 交互三个场景"
    - "全量 cargo test --lib 无回归"
  artifacts:
    - path: "src-tauri/src/adapter/json_merge.rs"
      provides: "深度合并规则单元测试（含边界用例补充）"
      contains: "test_merge_empty_overlay_no_side_effects"
    - path: "src-tauri/src/adapter/claude.rs"
      provides: "保护字段优先 + ClaudeAdapter overlay 集成测试（含边界用例补充）"
      contains: "test_patch_sequential_different_providers"
  key_links:
    - from: "src-tauri/src/adapter/json_merge.rs"
      to: "merge_with_null_delete"
      via: "单元测试直接调用"
      pattern: "merge_with_null_delete"
    - from: "src-tauri/src/adapter/claude.rs"
      to: "ClaudeAdapter::patch"
      via: "集成测试通过 TempDir + overlay_path_override 注入"
      pattern: "new_with_paths_and_overlay"
---

<objective>
审计并补充 overlay 相关测试，确保 COVL-14/15/16 三项测试需求完整覆盖，防止深度合并/保护字段优先级/ClaudeAdapter surgical patch 回归。

Purpose: Phase 24 已内嵌约 33 个 overlay 相关测试（json_merge 17 个 + claude 16 个），但 CONTEXT.md 中明确的几个边界用例尚未覆盖。本 plan 逐条审计后仅补充缺失用例。
Output: json_merge.rs 和 claude.rs 中新增约 5-7 个边界测试，全量 cargo test 通过。
</objective>

<execution_context>
@/Users/kelin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-test-coverage/25-CONTEXT.md
@.planning/phases/24-claude-settings-overlay-end-to-end/24-03-SUMMARY.md

<interfaces>
<!-- json_merge.rs 公开接口 -->
```rust
pub const PROTECTED_ENV_KEYS: [&str; 2] = ["ANTHROPIC_AUTH_TOKEN", "ANTHROPIC_BASE_URL"];

pub struct StripResult {
    pub stripped_paths: Vec<String>,
    pub overlay: Value,
}

pub fn strip_protected_fields(overlay: &Value) -> Result<StripResult, AppError>;
pub fn merge_with_null_delete(base: &mut Value, overlay: &Value) -> Result<(), AppError>;
```

<!-- claude.rs 测试辅助 -->
```rust
fn test_provider() -> Provider { ... }  // 返回固定测试 Provider
fn write_overlay(config_dir: &Path, content: &str) -> PathBuf;  // 写入临时 overlay 文件
ClaudeAdapter::new_with_paths(config_dir, backup_dir) -> Self;
ClaudeAdapter::new_with_paths_and_overlay(config_dir, backup_dir, overlay_path) -> Self;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 补充 json_merge.rs 边界用例（COVL-14 深度合并）</name>
  <files>src-tauri/src/adapter/json_merge.rs</files>
  <behavior>
    - test_merge_empty_overlay_no_side_effects: base = 含多个 key 的 object，overlay = `{}`，合并后 base 不变（COVL-14 边界：空 overlay 无副作用）
    - test_merge_nested_null_deletes_deep_key: base = `{"a":{"b":"val","c":"keep"}}`，overlay = `{"a":{"b":null}}`，合并后 `a.b` 被删除、`a.c` 保留（COVL-14 边界：嵌套路径 null 删除）
  </behavior>
  <action>
在 json_merge.rs 的 `mod tests` 块末尾追加两个测试函数：

1. `test_merge_empty_overlay_no_side_effects`：
   - base = `json!({"permissions": {"allow": ["Bash"]}, "env": {"TOKEN": "val"}, "scalar": 42})`
   - overlay = `json!({})`
   - 调用 `merge_with_null_delete(&mut base, &overlay).unwrap()`
   - 断言 base 与原始值完全相等（`assert_eq!`）

2. `test_merge_nested_null_deletes_deep_key`：
   - base = `json!({"a": {"b": "val", "c": "keep"}, "top": true})`
   - overlay = `json!({"a": {"b": null}})`
   - 调用 `merge_with_null_delete(&mut base, &overlay).unwrap()`
   - 断言 `base["a"]` 中无 "b" key（`base["a"].as_object().unwrap().get("b").is_none()`）
   - 断言 `base["a"]["c"]` == "keep"
   - 断言 `base["top"]` == true

沿用现有 `serde_json::json!` 宏和测试命名惯例（test_ 前缀）。
  </action>
  <verify>
    <automated>cd /Users/kelin/Work/CLIManager/src-tauri && cargo test --lib adapter::json_merge -- --show-output 2>&1 | tail -30</automated>
  </verify>
  <done>json_merge.rs 新增 2 个测试，19 个总测试全部通过，覆盖空 overlay 无副作用和嵌套 null 删除两个 COVL-14 边界场景</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 补充 claude.rs 边界用例（COVL-15 保护字段 + COVL-16 集成）</name>
  <files>src-tauri/src/adapter/claude.rs</files>
  <behavior>
    - test_patch_overlay_protected_and_custom_coexist: overlay 同时含保护字段和自定义 env 字段，保护字段被剥离但自定义字段写入成功（COVL-15 边界）
    - test_patch_sequential_different_providers: 先用 provider A patch（含 overlay），再用 provider B patch（含同一 overlay），两次保护字段分别为 A 和 B 的值（COVL-15+16 边界：序贯 patch 保护字段始终正确）
    - test_patch_then_clear_overlay_fields_survive: patch with overlay 注入自定义 env 字段后，clear 只删除保护字段，overlay 注入的自定义字段保留在 settings.json 中（COVL-16 边界：overlay+clear 交互）
    - test_patch_with_overlay_adds_top_level_keys: overlay 在 env 之外添加顶层 key（如 permissions），与 surgical patch 行为共存（COVL-16 边界：overlay 顶层字段不干扰 patch）
    - test_patch_with_empty_overlay_object: overlay 文件内容为 `{}`，patch 行为与无 overlay 完全一致（COVL-16 边界：空 overlay 对象）
  </behavior>
  <action>
在 claude.rs 的 `mod tests` 块末尾追加五个测试函数，全部沿用现有 `test_provider()`、`write_overlay()`、`TempDir` 模式：

1. `test_patch_overlay_protected_and_custom_coexist`：
   - overlay = `{"env": {"ANTHROPIC_AUTH_TOKEN": "HACKED", "MY_VAR": "custom"}}`
   - 断言最终 `ANTHROPIC_AUTH_TOKEN` == provider 值，`MY_VAR` == "custom"

2. `test_patch_sequential_different_providers`：
   - 创建 provider_a（api_key="key-a", base_url="url-a"）和 provider_b（api_key="key-b", base_url="url-b"）
   - overlay = `{"env": {"EXTRA": "shared"}}`
   - 先 adapter.patch(&provider_a)，读取 settings 验证保护字段 == a 值且 EXTRA 存在
   - 再 adapter.patch(&provider_b)，读取 settings 验证保护字段 == b 值且 EXTRA 仍存在
   - 注意：两次 patch 使用同一 adapter（同一 overlay_path），模拟切换 provider 场景

3. `test_patch_then_clear_overlay_fields_survive`：
   - overlay = `{"env": {"MY_OVERLAY_VAR": "injected"}}`
   - 先 adapter.patch(&provider)，验证 MY_OVERLAY_VAR 和保护字段都在
   - 再 adapter.clear()，读取 settings 验证保护字段被删除，但 MY_OVERLAY_VAR 仍保留

4. `test_patch_with_overlay_adds_top_level_keys`：
   - original settings = `{"env":{}}`
   - overlay = `{"permissions": {"allow": ["Bash","Read"]}, "env": {"EXTRA": "val"}}`
   - 断言 patch 后 permissions.allow 来自 overlay，env.EXTRA 来自 overlay，保护字段来自 provider

5. `test_patch_with_empty_overlay_object`：
   - overlay = `{}`
   - 断言 patch 后与无 overlay 的 patch 结果一致（仅保护字段和原有字段）

构造 provider_a/provider_b 时直接在测试函数内创建 Provider struct（复制 test_provider 改 api_key/base_url）。
  </action>
  <verify>
    <automated>cd /Users/kelin/Work/CLIManager/src-tauri && cargo test --lib adapter::claude -- --show-output 2>&1 | tail -30</automated>
  </verify>
  <done>claude.rs 新增 5 个测试，21 个总测试全部通过，覆盖保护字段+自定义字段共存、序贯 patch、overlay+clear 交互、顶层 key 合并、空 overlay 五个边界场景</done>
</task>

</tasks>

<verification>
全量测试无回归：
```bash
cd /Users/kelin/Work/CLIManager/src-tauri && cargo test --lib 2>&1 | tail -5
```
预期：所有测试通过，0 failures。

专项验证：
```bash
cd /Users/kelin/Work/CLIManager/src-tauri && cargo test --lib adapter::json_merge -- --show-output
cd /Users/kelin/Work/CLIManager/src-tauri && cargo test --lib adapter::claude -- --show-output
```
预期：json_merge 19 通过，claude 21 通过。
</verification>

<success_criteria>
1. json_merge.rs 新增 2 个测试（总 19 个），覆盖 COVL-14 的空 overlay 无副作用和嵌套 null 删除边界
2. claude.rs 新增 5 个测试（总 21 个），覆盖 COVL-15 保护字段+自定义字段共存与序贯 patch 正确性，以及 COVL-16 overlay+clear 交互与顶层 key 合并
3. 全量 `cargo test --lib` 无回归，0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/25-test-coverage/25-01-SUMMARY.md`
</output>
