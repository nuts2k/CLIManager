---
phase: 29-traffic-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/traffic.ts
  - src/lib/tauri.ts
  - src/hooks/useTrafficLogs.ts
  - src/components/layout/AppShell.tsx
  - src/components/layout/Header.tsx
  - src/i18n/locales/zh.json
  - src/i18n/locales/en.json
autonomous: true
requirements: [LOG-02, LOG-03]

must_haves:
  truths:
    - "Header 右侧出现 Traffic 图标按钮，与 Settings 齿轮按钮并排"
    - "点击 Traffic 按钮进入 traffic 视图，再点一次回到 main（toggle 行为）"
    - "Traffic 视图使用 opacity + pointer-events 过渡模式（150ms），始终渲染不卸载"
    - "useTrafficLogs hook 启动时拉取 100 条历史，监听 traffic-log 事件增量追加"
    - "内存最多保持 500 条日志，超出时丢弃最旧的"
    - "type=update 事件正确替换已有日志条目"
  artifacts:
    - path: "src/types/traffic.ts"
      provides: "TrafficLog TypeScript 接口（与后端 TrafficLogPayload 19 字段一一对应 + type 字段）"
      exports: ["TrafficLog"]
    - path: "src/hooks/useTrafficLogs.ts"
      provides: "双轨数据 hook（command 初始拉取 + event 增量追加）"
      exports: ["useTrafficLogs"]
    - path: "src/lib/tauri.ts"
      provides: "getRecentLogs Tauri command 封装"
      exports: ["getRecentLogs"]
    - path: "src/components/layout/AppShell.tsx"
      provides: "三视图（main | traffic | settings）切换框架"
      contains: "traffic"
    - path: "src/components/layout/Header.tsx"
      provides: "Traffic 图标按钮 + currentView prop"
      contains: "traffic"
  key_links:
    - from: "src/hooks/useTrafficLogs.ts"
      to: "src/lib/tauri.ts"
      via: "getRecentLogs() 调用"
      pattern: "getRecentLogs"
    - from: "src/hooks/useTrafficLogs.ts"
      to: "@tauri-apps/api/event"
      via: "listen('traffic-log')"
      pattern: "listen.*traffic-log"
    - from: "src/components/layout/AppShell.tsx"
      to: "src/components/layout/Header.tsx"
      via: "currentView prop + onNavigate 回调"
      pattern: "currentView"
---

<objective>
为 Phase 29 TrafficPage 建立基础设施：类型定义、Tauri command 封装、数据 hook、导航层扩展和国际化 key。

Purpose: 后续 Plan 02 的 UI 组件需要类型、数据源和导航框架才能实现。本 plan 先把这些"骨架"全部就位。
Output: TrafficLog 类型、useTrafficLogs hook、AppShell 三视图切换、Header Traffic 按钮、traffic i18n keys。
</objective>

<execution_context>
@/Users/kelin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-traffic-page/29-CONTEXT.md
@.planning/phases/29-traffic-page/29-RESEARCH.md

@src/components/layout/AppShell.tsx
@src/components/layout/Header.tsx
@src/lib/tauri.ts
@src/hooks/useSyncListener.ts
@src/i18n/locales/zh.json
@src/i18n/locales/en.json
@src/types/provider.ts

<interfaces>
<!-- 后端 TrafficLogPayload 字段定义（来自 src-tauri/src/traffic/log.rs:38-60） -->
```rust
pub struct TrafficLogPayload {
    #[serde(rename = "type")]
    pub event_type: String, // "new" / "update" / "history"
    pub id: i64,
    pub created_at: i64,
    pub provider_name: String,
    pub cli_id: String,
    pub method: String,
    pub path: String,
    pub status_code: Option<i64>,
    pub is_streaming: i64,
    pub request_model: Option<String>,
    pub upstream_model: Option<String>,
    pub protocol_type: String,
    pub input_tokens: Option<i64>,
    pub output_tokens: Option<i64>,
    pub cache_creation_tokens: Option<i64>,
    pub cache_read_tokens: Option<i64>,
    pub ttfb_ms: Option<i64>,
    pub duration_ms: Option<i64>,
    pub stop_reason: Option<String>,
    pub error_message: Option<String>,
}
```

<!-- 后端 Tauri command（来自 src-tauri/src/commands/traffic.rs） -->
```rust
#[tauri::command]
pub async fn get_recent_logs(
    traffic_db: tauri::State<'_, crate::traffic::TrafficDb>,
    limit: Option<i64>,
) -> Result<Vec<crate::traffic::log::TrafficLogPayload>, String>
```

<!-- 现有 AppShell.tsx 关键类型和模式 -->
```typescript
type AppView = "main" | "settings";  // 需扩展为 "main" | "traffic" | "settings"
const VIEW_TRANSITION_MS = 150;
// handleNavigate(nextView: AppView) — 已有完整的 exitingView 过渡逻辑
```

<!-- 现有 Header.tsx 接口 -->
```typescript
interface HeaderProps {
  onNavigate: (view: "main" | "settings") => void;
  // 需新增 currentView prop 才能实现 toggle 行为
}
```

<!-- 现有 tauri.ts 模式 -->
```typescript
import { invoke } from "@tauri-apps/api/core";
// 所有 invoke 封装均为 export async function XXX(): Promise<T> { return invoke("command_name", { args }); }
```

<!-- 现有 event 监听模式（来自 useSyncListener.ts） -->
```typescript
const unlisten = listen<PayloadType>("event-name", (event) => {
  const payload = event.payload;
  // ...
});
return () => { unlisten.then((fn) => fn()); };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 类型定义 + Tauri 封装 + 数据 Hook</name>
  <files>src/types/traffic.ts, src/lib/tauri.ts, src/hooks/useTrafficLogs.ts</files>
  <action>
1. **创建 `src/types/traffic.ts`**：定义 `TrafficLog` 接口，与后端 `TrafficLogPayload` 19 个字段 + type 字段一一对应。字段使用 snake_case（与后端 JSON 序列化一致）。`type` 字段类型为 `"new" | "update" | "history"`，`is_streaming` 为 `number`（0 或 1），所有 `Option<T>` 对应 `T | null`。

2. **扩展 `src/lib/tauri.ts`**：在文件末尾新增 `getRecentLogs` 函数。导入 `TrafficLog` 类型，函数签名 `getRecentLogs(limit?: number): Promise<TrafficLog[]>`，内部调用 `invoke("get_recent_logs", { limit })`。

3. **创建 `src/hooks/useTrafficLogs.ts`**：
   - 导入 `useState, useEffect, useRef` from react，`listen` from `@tauri-apps/api/event`，`getRecentLogs` from `@/lib/tauri`，`TrafficLog` from `@/types/traffic`
   - 常量 `MAX_LOGS = 500`
   - state: `logs: TrafficLog[]`（初始空数组），`loading: boolean`（初始 true）
   - 初始拉取 effect：调用 `getRecentLogs(100)`，成功后 setLogs，finally setLoading(false)。catch 只 console.error，不阻断。
   - 实时监听 effect：`listen<TrafficLog>("traffic-log", callback)`
     - `type === "new"`：置顶插入，超过 MAX_LOGS 时 slice(0, MAX_LOGS) 丢弃最旧
     - `type === "update"`：map 替换同 id 条目；若找不到则静默忽略（不新增，避免竞态问题——见 Research Pitfall 2）
   - cleanup：unlisten.then(fn => fn())
   - 返回 `{ logs, loading }`
  </action>
  <verify>
    <automated>cd /Users/kelin/Workspace/CLIManager && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - src/types/traffic.ts 导出 TrafficLog 接口，20 个字段与后端完全对应
    - src/lib/tauri.ts 导出 getRecentLogs 函数
    - src/hooks/useTrafficLogs.ts 导出 useTrafficLogs hook，包含初始拉取 + 实时监听 + 500 条上限逻辑
    - TypeScript 编译无错误
  </done>
</task>

<task type="auto">
  <name>Task 2: 导航层扩展 + 国际化 key</name>
  <files>src/components/layout/AppShell.tsx, src/components/layout/Header.tsx, src/i18n/locales/zh.json, src/i18n/locales/en.json</files>
  <action>
1. **修改 `src/components/layout/AppShell.tsx`**：
   - 将 `type AppView = "main" | "settings"` 改为 `type AppView = "main" | "traffic" | "settings"`
   - 在文件顶部 import 中添加 `TrafficPage` 的 lazy import（TrafficPage 组件在 Plan 02 创建，此处先写 import 路径 `@/components/traffic/TrafficPage`，并创建一个极简占位组件以保证编译通过）
   - 新增 `showTrafficView` 计算：`const showTrafficView = view === "traffic" || exitingView === "traffic";`
   - 在 `<main>` 中 `showSettingsView` 块之前添加 traffic 视图渲染块，完全复制 settings 模式（inert、aria-hidden、opacity 过渡 150ms、absolute inset-0）。内部渲染 `<TrafficPage />`
   - 将 `currentView={view}` 传给 `<Header>`
   - 注意：`handleNavigate` 已有 `if (nextView === view) return;` 的防重复逻辑，无需修改

2. **创建占位 `src/components/traffic/TrafficPage.tsx`**：
   - 极简占位组件：`export function TrafficPage() { return <div className="p-6">TrafficPage placeholder</div>; }`
   - Plan 02 会替换为完整实现

3. **修改 `src/components/layout/Header.tsx`**：
   - 新增 import `Activity` from lucide-react（用作 Traffic 图标——Activity 图标形似脉冲线，契合"流量监控"语义）
   - HeaderProps 扩展为 `{ currentView: "main" | "traffic" | "settings"; onNavigate: (view: "main" | "traffic" | "settings") => void; }`
   - Settings 按钮 onClick 改为 toggle 逻辑：`onClick={() => onNavigate(currentView === "settings" ? "main" : "settings")}`
   - 在 Settings 按钮前添加 Traffic 按钮（与 Settings 按钮并排，都在右侧）：
     ```
     <Button variant="ghost" size="icon" onClick={() => onNavigate(currentView === "traffic" ? "main" : "traffic")}>
       <Activity className="size-4" />
     </Button>
     ```
   - 两个按钮放在同一个 `<div className="flex items-center gap-1">` 容器中

4. **扩展 `src/i18n/locales/zh.json`**：在根对象中新增 `"traffic"` 键，包含以下翻译：
   ```json
   "traffic": {
     "title": "流量监控",
     "stats": {
       "totalRequests": "请求数",
       "inputTokens": "输入 Token",
       "outputTokens": "输出 Token",
       "successRate": "成功率",
       "cacheHitRate": "缓存命中率"
     },
     "filter": {
       "allProviders": "全部 Provider",
       "provider": "Provider"
     },
     "table": {
       "time": "时间",
       "provider": "Provider",
       "model": "模型",
       "statusCode": "状态码",
       "tokens": "Token",
       "duration": "耗时",
       "secondsAgo": "{{count}}秒前",
       "minutesAgo": "{{count}}分前",
       "cacheRead": "cache read",
       "ttfb": "TTFB",
       "tps": "t/s",
       "streaming": "流式",
       "nonStreaming": "非流式",
       "protocol": "协议",
       "upstreamModel": "上游模型",
       "cli": "CLI",
       "stopReason": "终止原因",
       "errorMessage": "错误信息",
       "placeholder": "--"
     },
     "empty": {
       "title": "暂无流量记录",
       "description": "开启代理并发送请求后，流量日志将在此处实时显示"
     },
     "statsBasis": "基于当前缓存"
   }
   ```

5. **扩展 `src/i18n/locales/en.json`**：添加对应英文翻译，key 结构与 zh.json 完全一致：
   ```json
   "traffic": {
     "title": "Traffic Monitor",
     "stats": {
       "totalRequests": "Requests",
       "inputTokens": "Input Tokens",
       "outputTokens": "Output Tokens",
       "successRate": "Success Rate",
       "cacheHitRate": "Cache Hit Rate"
     },
     "filter": {
       "allProviders": "All Providers",
       "provider": "Provider"
     },
     "table": {
       "time": "Time",
       "provider": "Provider",
       "model": "Model",
       "statusCode": "Status",
       "tokens": "Tokens",
       "duration": "Duration",
       "secondsAgo": "{{count}}s ago",
       "minutesAgo": "{{count}}m ago",
       "cacheRead": "cache read",
       "ttfb": "TTFB",
       "tps": "t/s",
       "streaming": "Streaming",
       "nonStreaming": "Non-streaming",
       "protocol": "Protocol",
       "upstreamModel": "Upstream Model",
       "cli": "CLI",
       "stopReason": "Stop Reason",
       "errorMessage": "Error",
       "placeholder": "--"
     },
     "empty": {
       "title": "No traffic records",
       "description": "Enable proxy and send requests to see traffic logs here"
     },
     "statsBasis": "Based on current cache"
   }
   ```
  </action>
  <verify>
    <automated>cd /Users/kelin/Workspace/CLIManager && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - AppView 类型扩展为 "main" | "traffic" | "settings"
    - Header 有 Traffic 图标按钮（Activity 图标），支持 toggle 行为
    - Header 接收 currentView prop，Settings 按钮同样改为 toggle 逻辑
    - Traffic 视图使用 always-render + opacity 150ms 过渡
    - zh.json 和 en.json 均包含 traffic.* 翻译 key
    - TypeScript 编译无错误
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` 编译通过
2. 应用启动后 Header 右侧可见 Activity 图标按钮
3. 点击 Traffic 按钮进入 traffic 视图（显示占位文字），再点回 main
4. Settings 按钮 toggle 行为不受影响
</verification>

<success_criteria>
- TrafficLog 类型导出且字段与后端 TrafficLogPayload 完全对应
- useTrafficLogs hook 导出且包含初始拉取 + 实时监听逻辑
- getRecentLogs 函数导出且可调用
- 三视图切换正常工作（main/traffic/settings），过渡动画一致
- i18n 中英文翻译 key 齐全
</success_criteria>

<output>
After completion, create `.planning/phases/29-traffic-page/29-01-SUMMARY.md`
</output>
