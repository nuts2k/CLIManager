---
phase: 31-tech-debt-fix
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/traffic/rollup.rs
  - src/types/traffic.ts
  - src/components/traffic/CacheLeaderboard.tsx
  - src/i18n/locales/zh.json
  - src/i18n/locales/en.json
  - .planning/phases/30-stats-rollup/30-03-SUMMARY.md
autonomous: true
requirements: [DEBT-01, DEBT-03]

must_haves:
  truths:
    - "ProviderStat API 返回 total_cache_creation_tokens 字段且数值正确"
    - "前端 CacheLeaderboard 展示缓存创建 Token 列"
    - "30-03-SUMMARY.md 中路径已修正为 src/i18n/locales/"
  artifacts:
    - path: "src-tauri/src/traffic/rollup.rs"
      provides: "ProviderStat 含 total_cache_creation_tokens 字段"
      contains: "total_cache_creation_tokens"
    - path: "src/types/traffic.ts"
      provides: "TS ProviderStat 接口含 total_cache_creation_tokens"
      contains: "total_cache_creation_tokens"
    - path: "src/components/traffic/CacheLeaderboard.tsx"
      provides: "缓存创建 Token 列展示"
      contains: "total_cache_creation_tokens"
  key_links:
    - from: "src-tauri/src/traffic/rollup.rs"
      to: "src/types/traffic.ts"
      via: "Rust ProviderStat 序列化 → TS ProviderStat 反序列化"
      pattern: "total_cache_creation_tokens"
    - from: "src/types/traffic.ts"
      to: "src/components/traffic/CacheLeaderboard.tsx"
      via: "ProviderStat.total_cache_creation_tokens 传入组件"
      pattern: "total_cache_creation_tokens"
---

<objective>
暴露 `total_cache_creation_tokens` 到 ProviderStat API 和前端，并修正 30-03-SUMMARY 文档路径

Purpose: 补全 Phase 30 遗漏的缓存创建 Token 字段，使数据链路完整（DB 已存储但 API/前端未暴露）；修正文档中的错误路径防止后续误导
Output: 后端 ProviderStat 含新字段 + 前端 CacheLeaderboard 展示缓存创建列 + 文档路径修正
</objective>

<execution_context>
@/Users/kelin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-tech-debt-fix/31-RESEARCH.md

<interfaces>
<!-- 执行者需要的关键接口定义，无需探索代码库 -->

From src-tauri/src/traffic/rollup.rs (当前，需修改):
```rust
#[derive(Debug, serde::Serialize)]
pub struct ProviderStat {
    pub provider_name: String,
    pub request_count: i64,
    pub success_count: i64,
    pub total_input_tokens: i64,
    pub total_output_tokens: i64,
    // 缺少 total_cache_creation_tokens — 本 plan 补上
    pub total_cache_read_tokens: i64,
    pub cache_triggered_count: i64,
    pub cache_hit_count: i64,
    pub sum_ttfb_ms: i64,
    pub sum_duration_ms: i64,
}
```

From src/types/traffic.ts (当前，需修改):
```typescript
export interface ProviderStat {
  provider_name: string;
  request_count: number;
  success_count: number;
  total_input_tokens: number;
  total_output_tokens: number;
  // 缺少 total_cache_creation_tokens — 本 plan 补上
  cache_triggered_count: number;
  cache_hit_count: number;
  total_cache_read_tokens: number;
  sum_ttfb_ms: number;
  sum_duration_ms: number;
}
```

From src/components/traffic/CacheLeaderboard.tsx:
```typescript
type SortKey =
  | "provider_name"
  | "cache_triggered_count"
  | "cache_hit_rate"
  | "total_cache_read_tokens"
  | "total_tokens";
// 需新增 "total_cache_creation_tokens" 排序键和对应表列
```

CacheLeaderboard 列结构 (gridTemplateColumns 5列):
```
Provider | 缓存触发数 | 命中率 | 缓存读取 Token | 总 Token
```
修改后应为 6 列，在"缓存读取 Token"前插入"缓存创建 Token"列。

i18n 键前缀: `traffic.analysis.colCacheCreationTokens`（zh: "缓存创建 Token", en: "Cache Creation Tokens"）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 后端 ProviderStat 新增 total_cache_creation_tokens + 前端类型同步 + CacheLeaderboard 展示</name>
  <files>src-tauri/src/traffic/rollup.rs, src/types/traffic.ts, src/components/traffic/CacheLeaderboard.tsx, src/i18n/locales/zh.json, src/i18n/locales/en.json</files>
  <behavior>
    - 测试 test_query_provider_stats_24h: 插入带 cache_creation_tokens 的记录后，返回的 ProviderStat.total_cache_creation_tokens 值正确聚合
    - 测试 test_query_provider_stats_7d: 合并 daily_rollups + 24h 明细后 total_cache_creation_tokens 值正确
    - tsc --noEmit 通过（前端类型完整）
  </behavior>
  <action>
    **Rust 后端 — src-tauri/src/traffic/rollup.rs:**

    1. `ProviderStat` 结构体新增字段 `pub total_cache_creation_tokens: i64`，插入到 `total_output_tokens` 和 `total_cache_read_tokens` 之间

    2. `query_provider_stats` "24h" 分支：
       - SQL SELECT 中在 `total_output_tokens` 行之后新增 `COALESCE(SUM(cache_creation_tokens), 0) AS total_cache_creation_tokens`
       - 这将使列索引变为：0=provider_name, 1=request_count, 2=success_count, 3=total_input_tokens, 4=total_output_tokens, **5=total_cache_creation_tokens**, 6=total_cache_read_tokens, 7=cache_triggered_count, 8=cache_hit_count, 9=sum_ttfb_ms, 10=sum_duration_ms
       - 更新 `query_map` 闭包中所有 `row.get(N)` 索引：原 5→6, 6→7, 7→8, 8→9, 9→10，新增 `total_cache_creation_tokens: row.get(5)?`

    3. `query_provider_stats` "7d" 分支：
       - 外层 SELECT 新增 `SUM(total_cache_creation_tokens) AS total_cache_creation_tokens`
       - 内层 daily_rollups 子查询新增 `total_cache_creation_tokens` 列
       - 内层 request_logs 子查询新增 `COALESCE(SUM(cache_creation_tokens), 0) AS total_cache_creation_tokens`
       - 更新 `query_map` 闭包索引，与 24h 分支保持一致

    4. 更新测试辅助函数 `insert_log`：将 `cache_creation_tokens: None` 改为显式值（如保持 None 即可，但新增一组测试数据带 cache_creation_tokens = Some(50)）
    5. 扩展现有测试 `test_query_provider_stats_24h` 和 `test_query_provider_stats_7d`：增加对 `total_cache_creation_tokens` 字段值的断言
    6. 确保 `test_rollup_aggregates_old_logs` 等涉及 daily_rollups 手工 INSERT 的测试中 `total_cache_creation_tokens` 列值正确

    **特别注意 row.get(N) 索引偏移：** 这是最容易出 bug 的地方（Research 陷阱 1）。修改后务必逐一核对每个 row.get(N) 对应的 SQL 列序号。

    **TypeScript — src/types/traffic.ts:**

    `ProviderStat` 接口新增 `total_cache_creation_tokens: number`，放在 `total_output_tokens` 和 `cache_triggered_count` 之间

    **React — src/components/traffic/CacheLeaderboard.tsx:**

    1. SortKey union type 新增 `"total_cache_creation_tokens"`
    2. sorted 函数的 switch 中新增对应 case
    3. columns 数组在 `colCacheReadTokens` 前插入 `{ key: "total_cache_creation_tokens", label: t("traffic.analysis.colCacheCreationTokens"), align: "right" }`
    4. 数据行渲染区新增 `formatTokenCount(stat.total_cache_creation_tokens)` 单元格
    5. gridTemplateColumns 从 5 列扩展为 6 列：`"minmax(80px,1.5fr) minmax(80px,1fr) minmax(60px,1fr) minmax(100px,1fr) minmax(100px,1fr) minmax(80px,1fr)"`

    **i18n — src/i18n/locales/zh.json 和 en.json:**

    在 `analysis` 节内 `colCacheReadTokens` 之前新增：
    - zh: `"colCacheCreationTokens": "缓存创建 Token"`
    - en: `"colCacheCreationTokens": "Cache Creation"`
  </action>
  <verify>
    <automated>cd /Users/kelin/Workspace/CLIManager/src-tauri && cargo test --lib traffic -- --nocapture 2>&1 | tail -30 && cd /Users/kelin/Workspace/CLIManager && npx tsc --noEmit</automated>
  </verify>
  <done>
    - cargo test traffic 全部通过，含 total_cache_creation_tokens 字段断言
    - tsc --noEmit 零错误
    - CacheLeaderboard 展示 6 列（含"缓存创建 Token"列）
  </done>
</task>

<task type="auto">
  <name>Task 2: 修正 30-03-SUMMARY.md 文档路径</name>
  <files>.planning/phases/30-stats-rollup/30-03-SUMMARY.md</files>
  <action>
    编辑 `.planning/phases/30-stats-rollup/30-03-SUMMARY.md`，将 key-files.modified 节中的两行错误路径修正：

    - 第 32 行 `src/locales/zh.json` → `src/i18n/locales/zh.json`
    - 第 33 行 `src/locales/en.json` → `src/i18n/locales/en.json`

    同时检查文件第 86-87 行的 Files Created/Modified 节中相同的错误路径并修正。

    这是纯文档修正，不涉及任何代码变更。
  </action>
  <verify>
    <automated>grep -n "src/locales/" /Users/kelin/Workspace/CLIManager/.planning/phases/30-stats-rollup/30-03-SUMMARY.md | grep -v "i18n" && echo "FAIL: 仍有未修正路径" || echo "PASS: 所有路径已修正为 src/i18n/locales/"</automated>
  </verify>
  <done>30-03-SUMMARY.md 中所有 i18n 文件路径均为 src/i18n/locales/（无 src/locales/ 残留）</done>
</task>

</tasks>

<verification>
- `cd src-tauri && cargo test --lib traffic` — 全部通过，新字段有断言
- `npx tsc --noEmit` — 零错误
- `grep "total_cache_creation_tokens" src-tauri/src/traffic/rollup.rs src/types/traffic.ts src/components/traffic/CacheLeaderboard.tsx` — 三个文件均有匹配
- `grep "src/locales/" .planning/phases/30-stats-rollup/30-03-SUMMARY.md | grep -v i18n` — 无输出（无残留错误路径）
</verification>

<success_criteria>
1. ProviderStat 从后端到前端完整包含 total_cache_creation_tokens 字段
2. CacheLeaderboard 展示缓存创建 Token 列，支持排序
3. 30-03-SUMMARY.md 路径已修正
4. 所有现有测试 + 新增断言通过
</success_criteria>

<output>
After completion, create `.planning/phases/31-tech-debt-fix/31-01-SUMMARY.md`
</output>
