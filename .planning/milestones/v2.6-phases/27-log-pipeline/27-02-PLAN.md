---
phase: 27-log-pipeline
plan: "02"
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - src-tauri/src/proxy/handler.rs
autonomous: true
requirements: [COLLECT-01, COLLECT-02, COLLECT-04, LOG-01]
must_haves:
  truths:
    - "非流式代理请求完成后，handler 通过 try_send 发送包含完整元数据的 LogEntry"
    - "Anthropic 非流式响应的 input_tokens、output_tokens、cache_creation/read_tokens 被正确提取"
    - "OpenAI Chat Completions 非流式响应的 prompt_tokens、completion_tokens、cached_tokens 被正确提取"
    - "OpenAI Responses 非流式响应的 input_tokens、output_tokens 被正确提取"
    - "stop_reason/finish_reason 保留原始协议值（不做跨协议映射）"
    - "请求失败时（ProxyError）error_message 被填充到 LogEntry"
    - "流式请求写入基础元数据（token 字段留 null），不阻塞流式传输"
    - "handler 开始处记录 Instant::now 和 SystemTime epoch ms 用于计时"
  artifacts:
    - path: "src-tauri/src/proxy/handler.rs"
      provides: "extract_tokens_from_response 函数、proxy_handler 日志埋点"
      contains: "extract_tokens_from_response"
  key_links:
    - from: "src-tauri/src/proxy/handler.rs"
      to: "traffic::log::LogEntry"
      via: "use + try_send"
      pattern: "try_send"
    - from: "src-tauri/src/proxy/handler.rs"
      to: "ProxyState::log_sender"
      via: "state.log_sender()"
      pattern: "log_sender"
---

<objective>
在 proxy_handler 中嵌入日志采集逻辑：请求开始时记录时间戳和 Instant，请求结束时从非流式响应中提取 token 用量和 stop_reason，将完整 LogEntry 通过 try_send 非阻塞发送到 mpsc channel。流式请求只写基础元数据（token 留 null）。错误分支记录 error_message。

Purpose: 连接 handler（数据源）和 Plan 01 建立的写入管道（数据汇），实现 Phase 27 的端到端日志采集。
Output: handler.rs 新增 token 提取函数和各分支的日志发送逻辑。
</objective>

<execution_context>
@/Users/kelin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-log-pipeline/27-CONTEXT.md
@.planning/phases/27-log-pipeline/27-RESEARCH.md
@.planning/phases/27-log-pipeline/27-01-SUMMARY.md

<interfaces>
<!-- Plan 01 创建的接口（executor 需要直接使用） -->

From src-tauri/src/traffic/log.rs (Plan 01 创建):
```rust
#[derive(Debug, Clone)]
pub struct LogEntry {
    pub created_at: i64,
    pub provider_name: String,
    pub cli_id: String,
    pub method: String,
    pub path: String,
    pub status_code: Option<i64>,
    pub is_streaming: i64,
    pub request_model: Option<String>,
    pub upstream_model: Option<String>,
    pub protocol_type: String,
    pub input_tokens: Option<i64>,
    pub output_tokens: Option<i64>,
    pub cache_creation_tokens: Option<i64>,
    pub cache_read_tokens: Option<i64>,
    pub ttfb_ms: Option<i64>,
    pub duration_ms: Option<i64>,
    pub stop_reason: Option<String>,
    pub error_message: Option<String>,
}
```

From src-tauri/src/proxy/state.rs (Plan 01 修改):
```rust
pub struct UpstreamTarget {
    // ...existing fields...
    pub provider_name: String,
}

pub struct ProxyState {
    // ...existing fields...
    log_tx: Option<tokio::sync::mpsc::Sender<LogEntry>>,
    cli_id: String,
}

impl ProxyState {
    pub fn log_sender(&self) -> Option<&Sender<LogEntry>>;
    pub fn cli_id(&self) -> &str;
}
```

From src-tauri/src/proxy/handler.rs (当前结构):
```rust
enum ResponseTranslationMode {
    Passthrough,
    AnthropicPassthrough { request_model: String },
    OpenAiChatCompletions { request_model: String },
    OpenAiResponses { request_model: String },
}

pub async fn proxy_handler(
    State(state): State<ProxyState>,
    req: axum::extract::Request,
) -> Result<Response, ProxyError>
```

handler.rs 响应处理分支（步骤 J）:
- OpenAiChatCompletions 非流式: resp_bytes -> resp_value -> openai_to_anthropic(resp_value) -> anthropic_resp
- OpenAiResponses 非流式: resp_bytes -> resp_value -> responses_to_anthropic(resp_value) -> anthropic_resp
- AnthropicPassthrough 非流式: resp_bytes -> resp_value -> reverse_model_in_response(resp_value)
- Passthrough: Body::from_stream(upstream_resp.bytes_stream()) — 不读取 body
- 所有协议 SSE 流式: Body::from_stream(各自的 stream)
- 所有协议 4xx/5xx: 直接透传 Body::from_stream
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: token 提取函数 + handler 各分支日志采集埋点</name>
  <files>src-tauri/src/proxy/handler.rs</files>
  <action>
    **A. 添加 use 声明：**
    ```rust
    use crate::traffic::log::LogEntry;
    ```

    **B. 创建 token 提取辅助函数（放在 proxy_handler 前）：**

    ```rust
    /// 从原始上游响应中提取 token 用量和 stop_reason
    ///
    /// 根据 ResponseTranslationMode 判断协议类型，从原始（转换前）响应 JSON 中提取字段。
    /// 返回 (input_tokens, output_tokens, cache_creation, cache_read, stop_reason)。
    /// 任何字段提取失败时返回 None（不 panic）。
    fn extract_tokens_from_response(
        resp_value: &Value,
        response_mode: &ResponseTranslationMode,
    ) -> (Option<i64>, Option<i64>, Option<i64>, Option<i64>, Option<String>) {
        match response_mode {
            ResponseTranslationMode::AnthropicPassthrough { .. } => {
                // Anthropic 原生格式
                extract_anthropic_tokens(resp_value)
            }
            ResponseTranslationMode::OpenAiChatCompletions { .. } => {
                // OpenAI Chat Completions 原始格式（转换前）
                extract_openai_chat_tokens(resp_value)
            }
            ResponseTranslationMode::OpenAiResponses { .. } => {
                // OpenAI Responses API 原始格式（转换前）
                extract_responses_tokens(resp_value)
            }
            ResponseTranslationMode::Passthrough => {
                // 透传请求（非 /v1/messages），尝试 Anthropic 格式
                extract_anthropic_tokens(resp_value)
            }
        }
    }
    ```

    ```rust
    /// Anthropic 协议 token 提取
    fn extract_anthropic_tokens(v: &Value) -> (Option<i64>, Option<i64>, Option<i64>, Option<i64>, Option<String>) {
        let usage = v.get("usage");
        let input = usage.and_then(|u| u.get("input_tokens")).and_then(|v| v.as_i64());
        let output = usage.and_then(|u| u.get("output_tokens")).and_then(|v| v.as_i64());
        let cache_creation = usage.and_then(|u| u.get("cache_creation_input_tokens")).and_then(|v| v.as_i64());
        let cache_read = usage.and_then(|u| u.get("cache_read_input_tokens")).and_then(|v| v.as_i64());
        let stop_reason = v.get("stop_reason").and_then(|v| v.as_str()).map(|s| s.to_string());
        (input, output, cache_creation, cache_read, stop_reason)
    }

    /// OpenAI Chat Completions token 提取（从原始 OpenAI 响应，非转换后的 Anthropic 格式）
    fn extract_openai_chat_tokens(v: &Value) -> (Option<i64>, Option<i64>, Option<i64>, Option<i64>, Option<String>) {
        let usage = v.get("usage");
        let input = usage.and_then(|u| u.get("prompt_tokens")).and_then(|v| v.as_i64());
        let output = usage.and_then(|u| u.get("completion_tokens")).and_then(|v| v.as_i64());
        // OpenAI 缓存 token: usage.prompt_tokens_details.cached_tokens
        let cache_read = usage
            .and_then(|u| u.get("prompt_tokens_details"))
            .and_then(|d| d.get("cached_tokens"))
            .and_then(|v| v.as_i64());
        // OpenAI Chat Completions 无 cache_creation 概念
        let cache_creation = None;
        // stop_reason 从 choices[0].finish_reason 提取原始值（不做映射）
        let stop_reason = v.get("choices")
            .and_then(|c| c.as_array())
            .and_then(|arr| arr.first())
            .and_then(|choice| choice.get("finish_reason"))
            .and_then(|v| v.as_str())
            .map(|s| s.to_string());
        (input, output, cache_creation, cache_read, stop_reason)
    }

    /// OpenAI Responses API token 提取
    fn extract_responses_tokens(v: &Value) -> (Option<i64>, Option<i64>, Option<i64>, Option<i64>, Option<String>) {
        let usage = v.get("usage");
        let input = usage.and_then(|u| u.get("input_tokens")).and_then(|v| v.as_i64());
        let output = usage.and_then(|u| u.get("output_tokens")).and_then(|v| v.as_i64());
        // Responses API 缓存字段: usage.input_token_details.cached_tokens（不确定，Phase 27 留 null）
        let cache_creation = None;
        let cache_read = None;
        // Responses API 无统一 stop_reason 字段，留 None
        // output[0].status 通常是 "completed"，不等价于 stop_reason
        let stop_reason = None;
        (input, output, cache_creation, cache_read, stop_reason)
    }
    ```

    **C. proxy_handler 函数头部添加计时（步骤 A 之后）：**
    ```rust
    let start_time = std::time::Instant::now();
    let request_start_ms = std::time::SystemTime::now()
        .duration_since(std::time::UNIX_EPOCH)
        .unwrap_or_default()
        .as_millis() as i64;
    ```

    **D. 步骤 B 之后提取 request method 和 path 的 clone（用于日志）：**
    注意：method 和 path 在步骤 B 已经有了（`let method = req.method().clone();` 和 `let path = req.uri().path().to_string();`）。handler 结尾需要再用一次，但 method 是 Clone 的，path 是 String 也可以再引用。
    另外，从请求 body 中提取 request_model（在步骤 C 的各协议分支中已有）。需要在各分支中记录 request_model 供日志使用。当前 ResponseTranslationMode 已携带 request_model，可直接使用。

    **E. 步骤 J 各分支：在非流式分支中 token 提取 + 构建 LogEntry：**

    **关键策略：** 在每个非流式分支中，token 提取必须从**原始上游响应**（`resp_value` 变量，转换前）而非转换后的 Anthropic 格式提取。原因：CONTEXT.md 要求 stop_reason 保留原始协议值。

    对于 `OpenAiChatCompletions` 非流式分支（当前代码流程：`resp_bytes -> resp_value -> openai_to_anthropic(resp_value)`）：
    - 在 `openai_to_anthropic(resp_value)` 调用**之前**，调用 `extract_tokens_from_response(&resp_value, &response_mode)` 提取 token
    - 注意：`resp_value` 在传给 `openai_to_anthropic` 时被 move，所以必须先提取 token
    - 修改方式：先 clone resp_value 用于 token 提取，或先提取 token 再 move

    对于 `OpenAiResponses` 非流式分支（同理）：先提取 token 再调用 `responses_to_anthropic`。

    对于 `AnthropicPassthrough` 非流式分支（`resp_value -> reverse_model_in_response`）：
    - 在 reverse_model_in_response 前提取 token（resp_value 也被 move）

    对于 `Passthrough` 分支：
    - 当前是 `Body::from_stream(upstream_resp.bytes_stream())`，**不**读取 body
    - Phase 27 对 Passthrough 分支不提取 token（留 null），只写基础元数据
    - 但 Passthrough 包含两类：(1) Anthropic 非 /v1/messages 请求 (2) Anthropic /v1/messages 但无模型映射（纯透传）
    - 纯透传情况下可能有 SSE 也可能非 SSE。对非 SSE 场景，如果要提取 token 需改动透传逻辑，复杂度高——Phase 27 对 Passthrough 全部留 null

    **F. 在步骤 J 结束后、return builder.body(body) 前，发送 LogEntry：**

    将日志发送逻辑集中在 builder.body(body) 前：

    ```rust
    // 日志采集：非流式请求的 token 已在各分支中提取
    // 流式和 Passthrough 的 token = None
    if let Some(tx) = state.log_sender() {
        let entry = LogEntry {
            created_at: request_start_ms,
            provider_name: upstream.provider_name.clone(),
            cli_id: state.cli_id().to_string(),
            method: method.to_string(),
            path: path.clone(),
            status_code: Some(status.as_u16() as i64),
            is_streaming: if upstream_is_sse { 1 } else { 0 },
            request_model: log_request_model,   // 从 response_mode 提取
            upstream_model: upstream.upstream_model.clone(),
            protocol_type: format!("{:?}", upstream.protocol_type),
            input_tokens: log_input_tokens,
            output_tokens: log_output_tokens,
            cache_creation_tokens: log_cache_creation,
            cache_read_tokens: log_cache_read,
            ttfb_ms: None,  // Phase 27 非流式不计 TTFB，Phase 28 处理
            duration_ms: if upstream_is_sse { None } else { Some(start_time.elapsed().as_millis() as i64) },
            stop_reason: log_stop_reason,
            error_message: None,
        };
        if let Err(e) = tx.try_send(entry) {
            log::warn!("日志 channel 发送失败（可能已满）: {}", e);
        }
    }
    ```

    其中 `log_request_model`、`log_input_tokens` 等变量需要在步骤 J 各分支中设置。实现方式：

    在步骤 J 的 match 之前声明 mutable 变量：
    ```rust
    let mut log_input_tokens: Option<i64> = None;
    let mut log_output_tokens: Option<i64> = None;
    let mut log_cache_creation: Option<i64> = None;
    let mut log_cache_read: Option<i64> = None;
    let mut log_stop_reason: Option<String> = None;
    let mut log_request_model: Option<String> = None;
    ```

    在每个非流式分支中（token 提取后）赋值：
    ```rust
    // 在 OpenAiChatCompletions 非流式分支内
    let (it, ot, cc, cr, sr) = extract_tokens_from_response(&resp_value, &response_mode);
    log_input_tokens = it;
    log_output_tokens = ot;
    // ... 以此类推
    ```

    在流式和 Passthrough 分支中保持 None。

    对于 `log_request_model`：从 `response_mode` 提取。在步骤 J match 之前：
    ```rust
    let log_request_model = match &response_mode {
        ResponseTranslationMode::AnthropicPassthrough { request_model } => Some(request_model.clone()),
        ResponseTranslationMode::OpenAiChatCompletions { request_model } => Some(request_model.clone()),
        ResponseTranslationMode::OpenAiResponses { request_model } => Some(request_model.clone()),
        ResponseTranslationMode::Passthrough => {
            // 尝试从请求 body 中提取 model（如果能解析的话）
            serde_json::from_slice::<Value>(&body_bytes).ok()
                .and_then(|v| v.get("model").and_then(|m| m.as_str()).map(|s| s.to_string()))
        }
    };
    ```
    注意 body_bytes 在步骤 C 中已读取，但在 ProtocolType 分支中 body_bytes 可能已 move。检查代码：步骤 C 结尾的 match 中，Passthrough 分支返回 `(url, body_bytes, ResponseTranslationMode::Passthrough)`，body_bytes 被 move 到 `final_body_bytes`。但 `body_bytes` 在 move 前可以提取 model。解决：在步骤 C match 之前就提取 request_model（不依赖 response_mode）：
    ```rust
    let log_request_model: Option<String> = serde_json::from_slice::<Value>(&body_bytes).ok()
        .and_then(|v| v.get("model").and_then(|m| m.as_str()).map(|s| s.to_string()));
    ```
    这样在任何分支都能用。

    **G. 错误处理日志：**
    当前 proxy_handler 返回 `Result<Response, ProxyError>`。错误情况下函数直接返回 Err，不经过步骤 J 的日志发送点。

    解决方案：将整个 handler 逻辑包在内部函数或闭包中，然后在外层 catch 错误并发送日志。但这会使代码过于复杂。

    **更简洁的方案：** 在每个 `?` 或 `.map_err` 返回 Err 之前，用 `log_error_and_return` 辅助：
    ```rust
    // 辅助宏或函数：在返回 ProxyError 前发送错误日志
    fn send_error_log(
        state: &ProxyState,
        request_start_ms: i64,
        start_time: &std::time::Instant,
        upstream: &UpstreamTarget,
        method: &axum::http::Method,
        path: &str,
        request_model: &Option<String>,
        is_streaming: bool,
        error: &ProxyError,
    ) {
        if let Some(tx) = state.log_sender() {
            let entry = LogEntry {
                created_at: request_start_ms,
                provider_name: upstream.provider_name.clone(),
                cli_id: state.cli_id().to_string(),
                method: method.to_string(),
                path: path.to_string(),
                status_code: None,
                is_streaming: if is_streaming { 1 } else { 0 },
                request_model: request_model.clone(),
                upstream_model: upstream.upstream_model.clone(),
                protocol_type: format!("{:?}", upstream.protocol_type),
                input_tokens: None,
                output_tokens: None,
                cache_creation_tokens: None,
                cache_read_tokens: None,
                ttfb_ms: None,
                duration_ms: Some(start_time.elapsed().as_millis() as i64),
                stop_reason: None,
                error_message: Some(format!("{}", error)),
            };
            let _ = tx.try_send(entry);
        }
    }
    ```

    但 handler 中大部分错误发生在 upstream 获取之前（NoUpstreamConfigured）或 body 读取阶段，此时 upstream 变量可能还未获取。

    **最终方案（务实）：**
    - 对 NoUpstreamConfigured 错误：不记录日志（无 provider_name 等信息）
    - 对 body 读取失败、转换失败、upstream 不可达等错误：在上游请求发送后（步骤 H）的 `.map_err` 中发送错误日志
    - 对 4xx/5xx 响应（非 ProxyError，是正常 HTTP 响应）：按正常流程记录，status_code 为 4xx/5xx
    - 具体实现：在步骤 H 的 `UpstreamUnreachable` 错误返回前调用 `send_error_log`

    对于 4xx/5xx 响应分支（`!status.is_success()`）：当前代码直接 `Body::from_stream(upstream_resp.bytes_stream())`。这些分支不读取 body，token 留 null。但它们会经过步骤 J 后面的日志发送点，status_code 会记录 4xx/5xx。

    **H. protocol_type 字段格式化：**
    `ProtocolType` 是 enum，需确认 `format!("{:?}", upstream.protocol_type)` 的输出。检查 ProtocolType 定义——它可能有 Display 或 Debug trait。如果输出是 `Anthropic`、`OpenAiChatCompletions` 等，直接使用 Debug 格式即可。如果需要更友好的格式（如 `anthropic`、`open_ai_chat_completions`），可以用 match 映射。推荐用小写字符串保持与 DB 一致：
    ```rust
    fn protocol_type_str(pt: &ProtocolType) -> &'static str {
        match pt {
            ProtocolType::Anthropic => "anthropic",
            ProtocolType::OpenAiChatCompletions => "open_ai_chat_completions",
            ProtocolType::OpenAiResponses => "open_ai_responses",
        }
    }
    ```

    **注意事项：**
    - 非流式分支中 token 提取必须在 `resp_value` 被 move 之前完成
    - try_send 失败只 log::warn，不 panic
    - 流式请求的 duration_ms 留 None（Phase 28 在 stream EOF 后 UPDATE）
    - 测试代码无需修改（handler tests 中 ProxyState 无 log_tx 时不发送日志）
  </action>
  <verify>
    <automated>cd /Users/kelin/Work/CLIManager/src-tauri && cargo test 2>&1 | tail -20</automated>
  </verify>
  <done>handler.rs 中所有非流式分支正确提取 token 并发送 LogEntry，流式分支发送基础 LogEntry（token=null），错误分支发送 error_message，cargo test 全部通过，cargo build 无 warning</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: token 提取函数单元测试</name>
  <files>src-tauri/src/proxy/handler.rs</files>
  <behavior>
    - Test: extract_anthropic_tokens 从含 usage 的 JSON 正确提取 input_tokens、output_tokens、cache_creation/read、stop_reason
    - Test: extract_anthropic_tokens 对缺少 usage 的 JSON 返回全 None
    - Test: extract_openai_chat_tokens 正确提取 prompt_tokens、completion_tokens、cached_tokens、finish_reason
    - Test: extract_openai_chat_tokens 对缺少 prompt_tokens_details 的 JSON，cache_read 返回 None
    - Test: extract_responses_tokens 正确提取 input_tokens、output_tokens，cache 字段返回 None
    - Test: protocol_type_str 返回正确的小写字符串
  </behavior>
  <action>
    在 handler.rs 的 `#[cfg(test)] mod tests` 中添加以下测试：

    1. `test_extract_anthropic_tokens_full` — 传入完整 Anthropic 响应 JSON（含 usage + stop_reason），验证 5 个返回值
    2. `test_extract_anthropic_tokens_no_usage` — 传入无 usage 字段的 JSON，验证全部返回 None
    3. `test_extract_openai_chat_tokens_full` — 传入完整 OpenAI Chat Completions 响应（含 prompt_tokens_details.cached_tokens + choices[0].finish_reason），验证提取正确
    4. `test_extract_openai_chat_tokens_no_cache` — 传入无 prompt_tokens_details 的 OpenAI 响应，cache_read = None
    5. `test_extract_responses_tokens` — 传入 Responses API 响应，验证 input/output 正确，cache 字段为 None
    6. `test_protocol_type_str` — 验证三种 ProtocolType 的字符串转换

    这些是纯函数测试，不需要网络或 Tauri 运行时。
  </action>
  <verify>
    <automated>cd /Users/kelin/Work/CLIManager/src-tauri && cargo test proxy::handler::tests::test_extract -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>6 个 token 提取单元测试全部通过，覆盖三种协议的正常和缺失字段场景</done>
</task>

</tasks>

<verification>
1. `cd src-tauri && cargo test proxy::handler::tests::test_extract` — token 提取测试通过
2. `cd src-tauri && cargo test` — 完整测试套件通过
3. `cd src-tauri && cargo build` — 编译无 error
</verification>

<success_criteria>
- handler.rs 含 extract_anthropic_tokens、extract_openai_chat_tokens、extract_responses_tokens 三个提取函数
- proxy_handler 顶部记录 Instant::now 和 SystemTime epoch ms
- 非流式 OpenAiChatCompletions 分支：从原始 resp_value 提取 token 后再调用 openai_to_anthropic
- 非流式 OpenAiResponses 分支：从原始 resp_value 提取 token 后再调用 responses_to_anthropic
- 非流式 AnthropicPassthrough 分支：从 resp_value 提取 token 后再 reverse_model
- 流式分支：token 字段全部为 None，is_streaming = 1
- Passthrough 分支：token 字段全部为 None
- 4xx/5xx 分支：token 为 None，status_code 记录实际状态码
- UpstreamUnreachable 错误：发送 error_message 日志
- try_send 失败时仅 warn 日志
- 6 个 token 提取单元测试通过
- cargo test 全套通过
</success_criteria>

<output>
完成后创建 `.planning/phases/27-log-pipeline/27-02-SUMMARY.md`
</output>
