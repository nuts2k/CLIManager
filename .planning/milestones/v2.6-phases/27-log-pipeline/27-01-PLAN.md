---
phase: 27-log-pipeline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/traffic/log.rs
  - src-tauri/src/traffic/mod.rs
  - src-tauri/src/commands/traffic.rs
  - src-tauri/src/commands/mod.rs
  - src-tauri/src/proxy/state.rs
  - src-tauri/src/proxy/mod.rs
  - src-tauri/src/proxy/server.rs
  - src-tauri/src/proxy/handler.rs
  - src-tauri/src/commands/proxy.rs
  - src-tauri/src/commands/provider.rs
  - src-tauri/src/watcher/mod.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements: [STORE-03, COLLECT-01, LOG-01]
must_haves:
  truths:
    - "LogEntry 结构包含 request_logs 表 18 个数据字段（不含 id）"
    - "insert_request_log 能将 LogEntry 写入 SQLite 并返回自增 id"
    - "log_worker 从 mpsc receiver 消费 LogEntry，写入 DB 后 emit traffic-log 事件"
    - "get_recent_logs Tauri command 能查询最近 N 条日志"
    - "UpstreamTarget 携带 provider_name 字段，所有构造点已填充"
    - "ProxyState 携带 log_tx 和 cli_id 字段"
    - "lib.rs setup 闭包中创建 mpsc channel 并注入 sender 到 ProxyService"
  artifacts:
    - path: "src-tauri/src/traffic/log.rs"
      provides: "LogEntry, TrafficLogPayload, insert_request_log, query_recent_logs, log_worker"
      min_lines: 100
    - path: "src-tauri/src/commands/traffic.rs"
      provides: "get_recent_logs Tauri command"
      exports: ["get_recent_logs"]
    - path: "src-tauri/src/proxy/state.rs"
      provides: "UpstreamTarget.provider_name, ProxyState.log_tx, ProxyState.cli_id"
      contains: "provider_name"
  key_links:
    - from: "src-tauri/src/lib.rs"
      to: "traffic::log::log_worker"
      via: "tauri::async_runtime::spawn"
      pattern: "log_worker"
    - from: "src-tauri/src/lib.rs"
      to: "proxy::ProxyService"
      via: "set_log_sender"
      pattern: "set_log_sender"
    - from: "src-tauri/src/traffic/log.rs"
      to: "traffic::TrafficDb"
      via: "app_handle.try_state"
      pattern: "try_state::<TrafficDb>"
---

<objective>
建立日志写入管道的全部基础设施：LogEntry 数据结构、SQLite 写入方法、mpsc channel 后台写入 worker、Tauri 事件推送、查询 command。同时扩展 UpstreamTarget（加 provider_name）和 ProxyState（加 log_tx + cli_id），在 lib.rs setup 闭包中完成 channel 创建和注入。

Purpose: 为 Plan 02 的 handler 埋点提供所有必需的基础设施——数据结构、写入通道、状态字段。
Output: traffic/log.rs、commands/traffic.rs 两个新文件；state.rs、mod.rs、lib.rs 等多个文件的扩展。
</objective>

<execution_context>
@/Users/kelin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-log-pipeline/27-CONTEXT.md
@.planning/phases/27-log-pipeline/27-RESEARCH.md
@.planning/phases/26-sqlite/26-01-SUMMARY.md

<interfaces>
<!-- Phase 26 已建立的 traffic 模块接口 -->

From src-tauri/src/traffic/mod.rs:
```rust
pub struct TrafficDb {
    pub conn: Mutex<rusqlite::Connection>,
}
pub fn init_traffic_db() -> Option<TrafficDb>
```

From src-tauri/src/traffic/schema.rs:
```rust
// request_logs 表 19 列: id, created_at, provider_name, cli_id, method, path,
// status_code, is_streaming, request_model, upstream_model, protocol_type,
// input_tokens, output_tokens, cache_creation_tokens, cache_read_tokens,
// ttfb_ms, duration_ms, stop_reason, error_message
pub const MIGRATIONS: Migrations<'static>;
```

From src-tauri/src/proxy/state.rs (当前):
```rust
pub struct UpstreamTarget {
    pub api_key: String,
    pub base_url: String,
    pub protocol_type: ProtocolType,
    pub upstream_model: Option<String>,
    pub upstream_model_map: Option<HashMap<String, String>>,
}

pub struct ProxyState {
    upstream: Arc<RwLock<Option<UpstreamTarget>>>,
    pub http_client: reqwest::Client,
}
```

From src-tauri/src/proxy/mod.rs:
```rust
pub struct ProxyService {
    servers: Mutex<HashMap<String, ProxyServer>>,
    http_client: reqwest::Client,
}
// 方法: start, stop, stop_all, status, update_upstream
```

From src-tauri/src/proxy/server.rs:
```rust
pub struct ProxyServer {
    shutdown_tx: Option<oneshot::Sender<()>>,
    server_handle: Option<JoinHandle<()>>,
    state: ProxyState,
    port: u16,
}
impl ProxyServer {
    pub fn new(port: u16, client: reqwest::Client) -> Self;
    pub fn state(&self) -> &ProxyState;
}
```

From src-tauri/src/watcher/mod.rs (emit 模式):
```rust
// 行 199: app_handle.emit("providers-changed", &payload)
// 行 296-298: tauri::async_runtime::spawn(async move { ... })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 traffic/log.rs + commands/traffic.rs（日志写入 + 查询基础）</name>
  <files>src-tauri/src/traffic/log.rs, src-tauri/src/traffic/mod.rs, src-tauri/src/commands/traffic.rs, src-tauri/src/commands/mod.rs</files>
  <behavior>
    - Test: insert_request_log 写入 LogEntry 后 last_insert_rowid > 0
    - Test: insert_request_log 写入后 query_recent_logs 能查到该记录，字段值匹配
    - Test: query_recent_logs limit=1 只返回 1 条（多条数据时）
    - Test: query_recent_logs 按 created_at 降序排列（最新的在前）
    - Test: LogEntry 所有 18 个字段均可正确写入和读取（含 Option 字段为 None 的情况）
    - Test: TrafficLogPayload 能从 LogEntry + id + type 构建，derive Serialize 正确
  </behavior>
  <action>
    1. 创建 `src-tauri/src/traffic/log.rs`，包含：

    **LogEntry 结构**（不含 id，对应 request_logs 表的 18 个数据列）：
    ```rust
    #[derive(Debug, Clone)]
    pub struct LogEntry {
        pub created_at: i64,       // epoch ms
        pub provider_name: String,
        pub cli_id: String,
        pub method: String,
        pub path: String,
        pub status_code: Option<i64>,
        pub is_streaming: i64,     // 0 或 1
        pub request_model: Option<String>,
        pub upstream_model: Option<String>,
        pub protocol_type: String,
        pub input_tokens: Option<i64>,
        pub output_tokens: Option<i64>,
        pub cache_creation_tokens: Option<i64>,
        pub cache_read_tokens: Option<i64>,
        pub ttfb_ms: Option<i64>,
        pub duration_ms: Option<i64>,
        pub stop_reason: Option<String>,
        pub error_message: Option<String>,
    }
    ```

    **TrafficLogPayload 结构**（用于 Tauri emit，含 id + type + 19 列）：
    ```rust
    #[derive(Debug, Clone, serde::Serialize)]
    pub struct TrafficLogPayload {
        #[serde(rename = "type")]
        pub event_type: String,    // "new" 或 "update"
        pub id: i64,
        pub created_at: i64,
        pub provider_name: String,
        pub cli_id: String,
        pub method: String,
        pub path: String,
        pub status_code: Option<i64>,
        pub is_streaming: i64,
        pub request_model: Option<String>,
        pub upstream_model: Option<String>,
        pub protocol_type: String,
        pub input_tokens: Option<i64>,
        pub output_tokens: Option<i64>,
        pub cache_creation_tokens: Option<i64>,
        pub cache_read_tokens: Option<i64>,
        pub ttfb_ms: Option<i64>,
        pub duration_ms: Option<i64>,
        pub stop_reason: Option<String>,
        pub error_message: Option<String>,
    }
    ```
    提供 `TrafficLogPayload::from_entry(id, entry, event_type)` 构造方法。

    **TrafficDb 方法扩展**（在 log.rs 中 `impl super::TrafficDb`）：
    - `insert_request_log(&self, entry: &LogEntry) -> rusqlite::Result<i64>`：INSERT 18 个字段，返回 `conn.last_insert_rowid()`
    - `query_recent_logs(&self, limit: i64) -> rusqlite::Result<Vec<TrafficLogPayload>>`：SELECT * FROM request_logs ORDER BY created_at DESC LIMIT ?，返回 TrafficLogPayload 列表（event_type 设为 "history"）

    **log_worker 异步函数**：
    ```rust
    pub async fn log_worker(
        mut rx: tokio::sync::mpsc::Receiver<LogEntry>,
        app_handle: tauri::AppHandle,
    ) {
        use tauri::Emitter;
        while let Some(entry) = rx.recv().await {
            if let Some(db) = app_handle.try_state::<super::TrafficDb>() {
                match db.insert_request_log(&entry) {
                    Ok(id) => {
                        let payload = TrafficLogPayload::from_entry(id, &entry, "new");
                        if let Err(e) = app_handle.emit("traffic-log", &payload) {
                            log::warn!("emit traffic-log 失败: {}", e);
                        }
                    }
                    Err(e) => log::warn!("写入 request_logs 失败: {}", e),
                }
            }
        }
    }
    ```

    2. 修改 `src-tauri/src/traffic/mod.rs`：添加 `pub mod log;` 声明

    3. 创建 `src-tauri/src/commands/traffic.rs`：
    ```rust
    #[tauri::command]
    pub async fn get_recent_logs(
        traffic_db: tauri::State<'_, crate::traffic::TrafficDb>,
        limit: Option<i64>,
    ) -> Result<Vec<crate::traffic::log::TrafficLogPayload>, String> {
        let limit = limit.unwrap_or(100).min(1000);
        traffic_db.query_recent_logs(limit).map_err(|e| e.to_string())
    }
    ```

    4. 修改 `src-tauri/src/commands/mod.rs`：添加 `pub mod traffic;`

    注意：log_worker 函数由于依赖 tauri::AppHandle 无法在单元测试中直接测试，但 insert_request_log 和 query_recent_logs 可用 in-memory DB 充分测试。单元测试写在 traffic/log.rs 的 `#[cfg(test)] mod tests` 中。
  </action>
  <verify>
    <automated>cd /Users/kelin/Work/CLIManager/src-tauri && cargo test traffic::log::tests -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>traffic/log.rs 包含 LogEntry、TrafficLogPayload、insert_request_log、query_recent_logs、log_worker；commands/traffic.rs 包含 get_recent_logs command；所有单元测试通过</done>
</task>

<task type="auto">
  <name>Task 2: 扩展 UpstreamTarget + ProxyState + ProxyService + lib.rs mpsc 管道注入</name>
  <files>src-tauri/src/proxy/state.rs, src-tauri/src/proxy/mod.rs, src-tauri/src/proxy/server.rs, src-tauri/src/proxy/handler.rs, src-tauri/src/commands/proxy.rs, src-tauri/src/commands/provider.rs, src-tauri/src/watcher/mod.rs, src-tauri/src/lib.rs</files>
  <action>
    **A. UpstreamTarget 加 provider_name（state.rs）：**
    ```rust
    pub struct UpstreamTarget {
        pub api_key: String,
        pub base_url: String,
        pub protocol_type: ProtocolType,
        pub upstream_model: Option<String>,
        pub upstream_model_map: Option<HashMap<String, String>>,
        pub provider_name: String,  // 新增 Phase 27
    }
    ```

    **B. ProxyState 加 log_tx 和 cli_id（state.rs）：**
    ```rust
    pub struct ProxyState {
        upstream: Arc<RwLock<Option<UpstreamTarget>>>,
        pub http_client: reqwest::Client,
        log_tx: Option<tokio::sync::mpsc::Sender<crate::traffic::log::LogEntry>>,
        cli_id: String,
    }
    ```
    - `ProxyState::new` 签名改为 `new(client, cli_id, log_tx)` 三个参数
    - 新增 `pub fn log_sender(&self) -> Option<&tokio::sync::mpsc::Sender<LogEntry>>` 方法
    - 新增 `pub fn cli_id(&self) -> &str` 方法
    注意：log_tx 不需要 Arc<RwLock>——ProxyState 已经是 Clone（通过 axum State 共享），Sender 本身是 Clone 的，可直接持有 Option<Sender>。

    **C. ProxyServer::new 签名扩展（server.rs）：**
    `pub fn new(port: u16, client: reqwest::Client, cli_id: String, log_tx: Option<Sender<LogEntry>>) -> Self`
    内部传递给 `ProxyState::new(client, cli_id, log_tx)`。

    **D. ProxyService 扩展（mod.rs）：**
    - 新增字段 `log_tx: std::sync::RwLock<Option<tokio::sync::mpsc::Sender<LogEntry>>>`（用 std::sync::RwLock，因为只在 start 时读取）
    - `ProxyService::new()` 中 `log_tx: std::sync::RwLock::new(None)`
    - 新增 `pub fn set_log_sender(&self, tx: Sender<LogEntry>)` 方法：写入 log_tx
    - `start()` 方法中 `ProxyServer::new(port, self.http_client.clone(), cli_id.to_string(), self.log_tx.read().unwrap().clone())`

    **E. 生产代码 UpstreamTarget 构造点加 provider_name（5 处）：**
    1. `commands/proxy.rs::build_upstream_target` — 函数签名加 `provider_name: String` 参数，构造中加 `provider_name`。同时修改调用方传入 provider_name。
    2. `commands/proxy.rs::build_upstream_target_from_provider` — 加 `provider_name: provider.name.clone()`
    3. `commands/provider.rs` 第 635 行 — 加 `provider_name: result.name.clone()`（update_provider 联动）
    4. `commands/provider.rs` 第 718 行 — 加 `provider_name: provider.name.clone()`（_set_active_provider_in_proxy_mode）
    5. `watcher/mod.rs` 第 286 行 — 加 `provider_name: provider.name.clone()`

    **F. 测试代码 UpstreamTarget 构造点加 provider_name（约 30 处）：**
    在以下文件中所有 `UpstreamTarget { ... }` 构造中加 `provider_name: "test".to_string()`：
    - proxy/state.rs tests（make_target + 2 处内联构造）
    - proxy/handler.rs tests（make_upstream_responses_target, make_upstream_openai_target, 以及约 12 处内联构造）
    - proxy/server.rs tests（4 处 .update_upstream(UpstreamTarget {...})）
    - proxy/mod.rs tests（make_upstream, make_upstream_openai, make_upstream_responses, 以及约 3 处内联构造）

    **G. ProxyServer::new 调用点修复（server.rs tests）：**
    所有 `ProxyServer::new(port, client)` 改为 `ProxyServer::new(port, client, "test".to_string(), None)`。

    **H. ProxyService::start 内部调用 ProxyServer::new 修复（mod.rs）：**
    `ProxyServer::new(port, self.http_client.clone(), cli_id.to_string(), self.log_tx.read().unwrap().clone())`

    **I. lib.rs setup 闭包扩展：**
    在 TrafficDb 初始化之后、proxy 恢复之前：
    ```rust
    // 创建 mpsc channel 用于流量日志写入
    let (log_tx, log_rx) = tokio::sync::mpsc::channel::<crate::traffic::log::LogEntry>(1024);

    // 注入 log sender 到 ProxyService
    let proxy_service = app.state::<proxy::ProxyService>();
    proxy_service.set_log_sender(log_tx);

    // 启动后台日志写入 worker
    let app_handle_for_log = app.handle().clone();
    tauri::async_runtime::spawn(async move {
        crate::traffic::log::log_worker(log_rx, app_handle_for_log).await;
    });
    ```

    **J. lib.rs invoke_handler 注册 get_recent_logs：**
    在 `invoke_handler(tauri::generate_handler![...])` 中添加 `commands::traffic::get_recent_logs`。

    **关键注意事项：**
    - `build_upstream_target` 函数被 `proxy_update_upstream` command 调用，需检查调用方是否有 provider_name 可传。如果调用方目前无法提供 provider_name（该 command 接收原始 api_key/base_url），传 `"unknown".to_string()` 即可——该 command 主要用于测试/调试。
    - ProxyState 的 log_tx 使用 Option 而非 Arc<RwLock> 是因为 ProxyState 已 #[derive(Clone)]，Sender 也是 Clone，无需额外包装。但注意 ProxyState 当前没有 derive Clone，而是手动 impl——检查确认：当前 ProxyState 是 `#[derive(Clone)]`，因此直接加 Option<Sender> 字段即可（Sender: Clone）。
  </action>
  <verify>
    <automated>cd /Users/kelin/Work/CLIManager/src-tauri && cargo test 2>&1 | tail -20</automated>
  </verify>
  <done>cargo test 全部通过（420+ tests），UpstreamTarget 含 provider_name，ProxyState 含 log_tx + cli_id，lib.rs 中 mpsc channel 已创建并注入，get_recent_logs 已注册到 invoke_handler</done>
</task>

</tasks>

<verification>
1. `cd src-tauri && cargo test traffic` — traffic 模块所有测试通过
2. `cd src-tauri && cargo test proxy` — proxy 模块所有测试通过（UpstreamTarget 构造修复验证）
3. `cd src-tauri && cargo test` — 完整测试套件通过
4. `cd src-tauri && cargo build` — 编译无 error 无 warning
</verification>

<success_criteria>
- traffic/log.rs 存在且包含 LogEntry、TrafficLogPayload、insert_request_log、query_recent_logs、log_worker
- commands/traffic.rs 存在且包含 get_recent_logs Tauri command
- UpstreamTarget 有 provider_name 字段，所有 5 个生产构造点和约 30 个测试构造点已更新
- ProxyState 有 log_tx 和 cli_id 字段
- lib.rs 中创建 mpsc channel(1024)、注入 sender 到 ProxyService、spawn log_worker
- cargo test 全部通过
</success_criteria>

<output>
完成后创建 `.planning/phases/27-log-pipeline/27-01-SUMMARY.md`
</output>
