---
phase: 26-sqlite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/src/traffic/mod.rs
  - src-tauri/src/traffic/schema.rs
  - src-tauri/src/traffic/db.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements: [STORE-01, STORE-02]

must_haves:
  truths:
    - "应用启动后 ~/Library/Application Support/com.climanager.app/ 下出现 traffic.db 文件"
    - "traffic.db 路径不含 iCloud/Mobile Documents"
    - "traffic.db 以 WAL 模式运行，busy_timeout 已配置"
    - "request_logs 和 daily_rollups 两张表及索引均已创建"
    - "重复启动不会报错或重建表（schema 迁移幂等）"
    - "DB 初始化失败时应用降级运行而非 panic"
  artifacts:
    - path: "src-tauri/src/traffic/mod.rs"
      provides: "TrafficDb 结构体（Mutex<Connection> 包裹）+ 初始化入口"
      exports: ["TrafficDb", "init_traffic_db"]
    - path: "src-tauri/src/traffic/schema.rs"
      provides: "MIGRATIONS 常量定义（request_logs + daily_rollups 建表 SQL）"
      exports: ["MIGRATIONS"]
    - path: "src-tauri/src/traffic/db.rs"
      provides: "路径解析 get_traffic_db_path() + 连接打开 open_traffic_db()"
      exports: ["get_traffic_db_path", "open_traffic_db"]
    - path: "src-tauri/src/lib.rs"
      provides: "setup 闭包中 traffic DB 初始化和 manage 注入"
      contains: "traffic::init_traffic_db"
  key_links:
    - from: "src-tauri/src/lib.rs"
      to: "src-tauri/src/traffic/mod.rs"
      via: "setup 闭包调用 traffic::init_traffic_db()"
      pattern: "traffic::init_traffic_db"
    - from: "src-tauri/src/traffic/mod.rs"
      to: "src-tauri/src/traffic/db.rs"
      via: "init_traffic_db 调用 open_traffic_db"
      pattern: "db::open_traffic_db"
    - from: "src-tauri/src/traffic/db.rs"
      to: "src-tauri/src/traffic/schema.rs"
      via: "open 后执行 MIGRATIONS.to_latest()"
      pattern: "schema::MIGRATIONS"
---

<objective>
搭建 SQLite 基础设施：在非 iCloud 路径初始化 traffic.db，启用 WAL 模式，建立 request_logs 和 daily_rollups 两张表及索引，通过 rusqlite_migration 管理 schema 版本，并将 TrafficDb 注入 Tauri 全局状态。

Purpose: 为后续所有流量监控功能（日志写入、统计查询、前端展示）提供稳固的数据存储基础
Output: traffic/ 模块（3 个文件）+ lib.rs 集成 + 单元测试全部通过
</objective>

<execution_context>
@/Users/kelin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-sqlite/26-CONTEXT.md
@.planning/phases/26-sqlite/26-RESEARCH.md

@src-tauri/Cargo.toml
@src-tauri/src/lib.rs

<interfaces>
<!-- 项目现有的路径获取模式（参考但不复用） -->

From src-tauri/src/storage/local.rs:
```rust
pub fn get_local_settings_path() -> PathBuf {
    let home = dirs::home_dir().expect("Could not determine home directory");
    home.join(".cli-manager").join("local.json")
}
```

From src-tauri/Cargo.toml:
```toml
dirs = "5.0"
thiserror = "2.0"
log = "0.4"
```

Tauri bundle identifier (from tauri.conf.json):
```
"identifier": "com.climanager.app"
```

From src-tauri/src/lib.rs setup 闭包模式:
```rust
// 现有 manage 注入方式
.manage(watcher::SelfWriteTracker::new())
.manage(proxy::ProxyService::new())
// DB 初始化应插入 setup 闭包中，崩溃恢复之前、代理恢复之前
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 traffic 模块 + 依赖安装</name>
  <files>
    src-tauri/Cargo.toml,
    src-tauri/src/traffic/mod.rs,
    src-tauri/src/traffic/schema.rs,
    src-tauri/src/traffic/db.rs
  </files>
  <behavior>
    - Test: MIGRATIONS.validate() 通过（SQL 语法正确）
    - Test: to_latest() 后 request_logs 表存在，含 19 列（id + 18 业务列）
    - Test: to_latest() 后 daily_rollups 表存在，含 14 列（id + provider_name + rollup_date + 10 聚合字段 + UNIQUE 约束）
    - Test: idx_request_logs_created_at、idx_request_logs_provider_name、idx_daily_rollups_date 索引存在
    - Test: 重复执行 to_latest() 不报错（幂等性）
    - Test: configure_connection 后 journal_mode=wal
    - Test: configure_connection 后 busy_timeout=5000
    - Test: get_traffic_db_path() 返回路径包含 "Application Support" 且不包含 "Mobile Documents"
    - Test: open_traffic_db() 首次打开成功返回 Some(Connection)
    - Test: DB 文件损坏时 open_traffic_db() 自动重建并返回 Some
  </behavior>
  <action>
1. 在 Cargo.toml [dependencies] 中添加：
   ```toml
   rusqlite = { version = "0.38", features = ["bundled"] }
   rusqlite_migration = "2.4"
   ```

2. 创建 `src-tauri/src/traffic/schema.rs`：
   - 定义 `const MIGRATIONS_SLICE: &[M<'static>]` 包含一个迁移项（M::up），SQL 内容：
     - CREATE TABLE IF NOT EXISTS request_logs（19 列按 CONTEXT.md 决策：id, created_at INTEGER NOT NULL, provider_name TEXT NOT NULL, cli_id TEXT NOT NULL, method TEXT NOT NULL, path TEXT NOT NULL, status_code INTEGER, is_streaming INTEGER NOT NULL DEFAULT 0, request_model TEXT, upstream_model TEXT, protocol_type TEXT NOT NULL, input_tokens INTEGER, output_tokens INTEGER, cache_creation_tokens INTEGER, cache_read_tokens INTEGER, ttfb_ms INTEGER, duration_ms INTEGER, stop_reason TEXT, error_message TEXT）
     - CREATE INDEX idx_request_logs_created_at ON request_logs(created_at)
     - CREATE INDEX idx_request_logs_provider_name ON request_logs(provider_name)
     - CREATE TABLE IF NOT EXISTS daily_rollups（14 列按 CONTEXT.md 决策：id, provider_name TEXT NOT NULL, rollup_date TEXT NOT NULL, request_count, success_count, total_input_tokens, total_output_tokens, total_cache_creation_tokens, total_cache_read_tokens, cache_triggered_count, cache_hit_count, sum_ttfb_ms, sum_duration_ms，全部 INTEGER NOT NULL DEFAULT 0，UNIQUE(provider_name, rollup_date)）
     - CREATE INDEX idx_daily_rollups_date ON daily_rollups(rollup_date)
   - 定义 `pub const MIGRATIONS: Migrations<'static> = Migrations::from_slice(MIGRATIONS_SLICE);`
   - 添加 `#[cfg(test)] mod tests` 验证 migrations_are_valid、migrations_create_expected_tables、migrations_are_idempotent

3. 创建 `src-tauri/src/traffic/db.rs`：
   - `pub fn get_traffic_db_path() -> PathBuf`：使用 `dirs::data_local_dir()` 拼接 `com.climanager.app/traffic.db`
   - `fn configure_connection(conn: &Connection) -> rusqlite::Result<()>`：执行 PRAGMA journal_mode=WAL; busy_timeout=5000; synchronous=NORMAL; foreign_keys=ON;
   - `fn try_open_and_migrate(path: &std::path::Path) -> Result<Connection, Box<dyn std::error::Error>>`：create_dir_all 父目录 → Connection::open → configure_connection → MIGRATIONS.to_latest
   - `pub fn open_traffic_db() -> Option<Connection>`：调用 try_open_and_migrate，失败则 log::warn 并删除文件重试，重试仍失败则 log::error 返回 None（降级运行）
   - 添加 `#[cfg(test)] mod tests` 验证路径、WAL 模式、busy_timeout、损坏重建

4. 创建 `src-tauri/src/traffic/mod.rs`：
   - `pub mod schema;` 和 `pub mod db;`
   - `pub struct TrafficDb { pub conn: std::sync::Mutex<rusqlite::Connection> }`
   - `pub fn init_traffic_db() -> Option<TrafficDb>`：调用 db::open_traffic_db()，Some 时包裹为 TrafficDb
  </action>
  <verify>
    <automated>cd /Users/kelin/Work/CLIManager/src-tauri && cargo test -p cli-manager-lib traffic -- --nocapture</automated>
  </verify>
  <done>
    - traffic/ 模块 3 个文件创建完毕
    - rusqlite 和 rusqlite_migration 依赖已添加到 Cargo.toml
    - 所有单元测试通过：migrations 验证、表/索引创建、幂等性、WAL 模式、busy_timeout、路径检查、损坏重建
    - cargo build 无 warning
  </done>
</task>

<task type="auto">
  <name>Task 2: 集成到 lib.rs setup 闭包</name>
  <files>src-tauri/src/lib.rs</files>
  <action>
1. 在 lib.rs 顶部添加 `mod traffic;`

2. 在 setup 闭包中（file watcher 启动之后、startup overlay apply 之前），插入 traffic DB 初始化：
   ```rust
   // 初始化 traffic DB（降级运行：失败不阻断启动）
   if let Some(traffic_db) = traffic::init_traffic_db() {
       app.manage(traffic_db);
       log::info!("traffic.db 初始化成功");
   } else {
       log::warn!("traffic.db 不可用，代理将正常工作但不记录流量");
   }
   ```

3. 确保 init_traffic_db() 返回 None 时不调用 manage()，不导致 panic。后续 Phase 27 将使用 `app_handle.try_state::<TrafficDb>()` 安全访问。

注意事项：
- 不使用 `?` 传播 DB 初始化错误（RESEARCH.md Pitfall 3）
- 使用 if let Some 模式而非 match，保持与 setup 闭包现有代码风格一致
- DB 初始化位置选择说明：watcher 启动后（确保文件系统就绪）、overlay apply 之前（尽早可用），但具体位置不严格依赖，只要在 setup 闭包内即可
  </action>
  <verify>
    <automated>cd /Users/kelin/Work/CLIManager/src-tauri && cargo build 2>&1 | tail -5 && cargo test -p cli-manager-lib -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>
    - lib.rs 中 `mod traffic` 声明已添加
    - setup 闭包中 traffic DB 初始化代码已插入
    - cargo build 无错误无 warning
    - 完整测试套件（cargo test -p cli-manager-lib）通过
    - 集成后 TrafficDb 可通过 Tauri state 访问（Phase 27 前置条件就绪）
  </done>
</task>

</tasks>

<verification>
1. `cargo test -p cli-manager-lib traffic -- --nocapture` — 所有 traffic 模块测试通过
2. `cargo test -p cli-manager-lib` — 完整测试套件无回归
3. `cargo build` — 编译成功无 warning
4. 手动验证（可选）：`cargo tauri dev` 启动后检查 `ls ~/Library/Application\ Support/com.climanager.app/traffic.db` 文件存在
</verification>

<success_criteria>
- traffic.db 在正确路径初始化（~/Library/Application Support/com.climanager.app/traffic.db）
- WAL 模式和 busy_timeout pragma 已配置
- request_logs 表含 19 列 + 2 索引，daily_rollups 表含 14 列 + 1 索引 + UNIQUE 约束
- schema 迁移幂等（重复启动不报错）
- DB 初始化失败时降级运行（不 panic）
- 所有单元测试通过
- cargo build 无 warning
</success_criteria>

<output>
完成后创建 `.planning/phases/26-sqlite/26-01-SUMMARY.md`
</output>
