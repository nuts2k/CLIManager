---
phase: 20-tab
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/settings/SettingsPage.tsx
  - src/components/settings/AboutSection.tsx
  - src/i18n/locales/zh.json
  - src/i18n/locales/en.json
autonomous: true
requirements:
  - SETT-01

must_haves:
  truths:
    - "设置页顶部显示'通用'、'高级'、'关于'三个 Tab，点击可切换内容区域"
    - "通用 Tab 包含语言选择"
    - "高级 Tab 包含代理模式开关、测试配置、导入 CLI 配置按钮"
    - "关于 Tab 包含应用 Logo、版本号、更新检查、GitHub Releases 链接"
    - "每次打开设置页默认停留在'通用' Tab"
    - "Tab 栏使用 line variant 下划线风格，居左对齐"
  artifacts:
    - path: "src/components/settings/SettingsPage.tsx"
      provides: "三 Tab 布局设置页"
      contains: "TabsList"
    - path: "src/components/settings/AboutSection.tsx"
      provides: "关于区块含 Logo 展示"
      contains: "icon.png"
    - path: "src/i18n/locales/zh.json"
      provides: "Tab 标签名中文翻译"
      contains: "tabGeneral"
    - path: "src/i18n/locales/en.json"
      provides: "Tab 标签名英文翻译"
      contains: "tabGeneral"
  key_links:
    - from: "src/components/settings/SettingsPage.tsx"
      to: "src/components/ui/tabs.tsx"
      via: "import Tabs/TabsList/TabsTrigger/TabsContent"
      pattern: "from.*components/ui/tabs"
    - from: "src/components/settings/SettingsPage.tsx"
      to: "src/i18n/locales/zh.json"
      via: "t('settings.tabGeneral') 等翻译 key"
      pattern: "t\\(.*tabGeneral"
---

<objective>
将设置页从单列滚动长页重构为三 Tab 分组布局（通用/高级/关于），使内容按功能归类、更清晰易找。

Purpose: SETT-01 要求设置页 Tab 化，告别一眼望不到头的滚动长页。
Output: SettingsPage.tsx 使用 Tabs 组件实现三 Tab 切换，各 Tab 内容独立；关于页增加应用 Logo。
</objective>

<execution_context>
@/Users/kelin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-tab/20-CONTEXT.md

@src/components/settings/SettingsPage.tsx
@src/components/settings/AboutSection.tsx
@src/components/ui/tabs.tsx
@src/i18n/locales/zh.json
@src/i18n/locales/en.json

<interfaces>
<!-- 已有 Tabs 组件的导出接口 -->
From src/components/ui/tabs.tsx:
```typescript
export { Tabs, TabsList, TabsTrigger, TabsContent, tabsListVariants }
// Tabs: Root 容器，接受 defaultValue 指定默认 Tab
// TabsList: Tab 栏容器，接受 variant="line" 实现下划线风格
// TabsTrigger: 单个 Tab 按钮，value 属性标识
// TabsContent: Tab 内容面板，value 属性与 Trigger 对应
```

From src/components/settings/SettingsPage.tsx:
```typescript
interface SettingsPageProps {
  onBack: () => void;
  onShowImport?: () => void;
}
```

From src/components/settings/AboutSection.tsx:
```typescript
interface AboutSectionProps {
  onCheckUpdate: () => void;
  updateStatus: UpdateStatus;
  currentVersion: string;
  newVersion: string | null;
  progress: number;
  error: string | null;
  onUpdate?: () => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加 Tab 翻译 key + 关于页 Logo</name>
  <files>src/i18n/locales/zh.json, src/i18n/locales/en.json, src/components/settings/AboutSection.tsx</files>
  <action>
1. 在 zh.json 和 en.json 的 "settings" 对象中添加三个 Tab 标签名翻译 key：
   - "tabGeneral": "通用" / "General"
   - "tabAdvanced": "高级" / "Advanced"
   - "tabAbout": "关于" / "About"

2. 修改 AboutSection.tsx，在内容顶部添加应用 Logo 展示：
   - 使用 `<img src="/icon.png" />` 引用 public 目录下的应用图标（Tauri 项目 public 目录对应 src-tauri/icons）
   - 如果 public 目录下没有 icon.png，则将 src-tauri/icons/icon.png 复制到 public/icon.png
   - Logo 尺寸：64x64px（w-16 h-16），圆角 rounded-lg
   - Logo 下方紧跟应用名 "CLIManager" 使用 text-base font-semibold
   - 版本号紧跟应用名下方
   - 整体垂直居中排列（flex flex-col items-center），Logo+名称+版本为一组，与下方更新区域间距 space-y-4
  </action>
  <verify>
    <automated>cd /Users/kelin/Workspace/CLIManager && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>zh.json 和 en.json 各包含 tabGeneral/tabAdvanced/tabAbout 三个翻译 key；AboutSection 顶部展示 64px 应用 Logo + 应用名 + 版本号</done>
</task>

<task type="auto">
  <name>Task 2: SettingsPage 重构为三 Tab 布局</name>
  <files>src/components/settings/SettingsPage.tsx</files>
  <action>
重构 SettingsPage.tsx，将滚动长页改为 Tabs 布局：

1. 新增 import：
   - `import { Tabs, TabsList, TabsTrigger, TabsContent } from "@/components/ui/tabs";`
   - 移除 `Separator` import（不再需要作为分区分隔符）

2. Header 保持不变（返回按钮 + "设置" 标题），在 Header 下方放置 Tabs 组件：
   ```
   <Tabs defaultValue="general" className="flex-1 flex flex-col overflow-hidden">
     <div className="px-6 pt-4">
       <TabsList variant="line">
         <TabsTrigger value="general">{t("settings.tabGeneral")}</TabsTrigger>
         <TabsTrigger value="advanced">{t("settings.tabAdvanced")}</TabsTrigger>
         <TabsTrigger value="about">{t("settings.tabAbout")}</TabsTrigger>
       </TabsList>
     </div>
     <!-- TabsContent 区域 -->
   </Tabs>
   ```

3. 通用 Tab（value="general"）：
   - 包含语言选择 section（现有代码直接迁移）
   - TabsContent 设 className="flex-1 overflow-auto p-6 space-y-6"

4. 高级 Tab（value="advanced"）：
   - 包含三个区块，区块之间用 Separator 分隔：
     a. 代理模式 section（现有代码直接迁移）
     b. 测试配置 section（现有代码直接迁移）
     c. 导入 CLI 配置 section（现有代码直接迁移，保留 onShowImport 条件判断）
   - 高级 Tab 内的 Separator 保留（用于区块间分隔），需保留 Separator import

5. 关于 Tab（value="about"）：
   - 包含 AboutSection 组件（现有代码直接迁移，props 不变）

6. 外层结构保持 `<div className="flex h-full flex-col">`，Tabs 占据 flex-1。
   每个 TabsContent 自带 overflow-auto 支持内容滚动。

7. 确保所有状态逻辑（proxyEnabled, timeout, testModel 等）保留在 SettingsPage 中不变。
   只是将渲染输出从一个长列表拆分到三个 TabsContent 中。

注意：
- Tab 栏居左对齐，使用 variant="line" 下划线风格（根据用户决策）
- Tab 切换无过渡动效，内容直接替换（Phase 21 可后续追加）
- defaultValue="general" 确保每次打开默认停留「通用」Tab
  </action>
  <verify>
    <automated>cd /Users/kelin/Workspace/CLIManager && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>SettingsPage 展示三个 Tab（通用/高级/关于），各 Tab 内容独立且正确渲染对应设置项；默认停留通用 Tab；Tab 栏为 line variant 下划线风格居左对齐</done>
</task>

</tasks>

<verification>
1. TypeScript 编译无错误：`npx tsc --noEmit`
2. 应用启动后设置页顶部可见三个 Tab（通用/高级/关于）
3. 点击各 Tab 切换显示对应内容：
   - 通用：语言选择
   - 高级：代理模式开关 + 测试配置 + 导入按钮
   - 关于：Logo + 版本 + 更新检查 + GitHub 链接
4. 每次进入设置页默认停留「通用」Tab
5. Tab 栏使用下划线风格，居左对齐
</verification>

<success_criteria>
- 设置页三个 Tab 可正常切换，内容正确归类
- 关于页展示应用 Logo（64px），视觉正式
- Tab 下划线风格与暗色主题融合
- 所有原有设置功能（语言/代理/测试/导入/更新）完整保留
- 中英文翻译完整
</success_criteria>

<output>
After completion, create `.planning/phases/20-tab/20-01-SUMMARY.md`
</output>
