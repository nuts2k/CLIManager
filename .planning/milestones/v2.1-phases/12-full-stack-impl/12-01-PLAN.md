---
phase: 12-full-stack-impl
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/tauri.conf.json
  - src-tauri/capabilities/default.json
  - src-tauri/src/lib.rs
  - package.json
  - pnpm-lock.yaml
autonomous: true
requirements: [REL-01, SIGN-02, SIGN-03]

must_haves:
  truths:
    - "Cargo.toml 是版本号唯一来源，tauri.conf.json 无 version 字段"
    - "Ed25519 密钥对已生成，私钥存入 GitHub Secrets，公钥写入 tauri.conf.json"
    - "updater 和 process 插件在 Rust 端注册、前端依赖安装、capability 权限声明完毕"
  artifacts:
    - path: "src-tauri/tauri.conf.json"
      provides: "updater 配置（pubkey + endpoint）、createUpdaterArtifacts、ad-hoc 签名"
      contains: "plugins.*updater"
    - path: "src-tauri/Cargo.toml"
      provides: "唯一版本来源 + updater/process Rust 依赖"
      contains: "tauri-plugin-updater"
    - path: "src-tauri/capabilities/default.json"
      provides: "updater 和 process 权限声明"
      contains: "updater:default"
    - path: "src-tauri/src/lib.rs"
      provides: "Rust 端插件注册"
      contains: "tauri_plugin_updater"
  key_links:
    - from: "src-tauri/tauri.conf.json"
      to: "src-tauri/Cargo.toml"
      via: "省略 version 字段后 Tauri 自动回退读 Cargo.toml"
      pattern: "无 version 字段"
    - from: "src-tauri/tauri.conf.json plugins.updater.pubkey"
      to: "~/.tauri/climanager.key.pub"
      via: "生成的公钥写入配置"
      pattern: "pubkey"
---

<objective>
完成 Phase 12 的基础层：版本来源统一、Ed25519 签名密钥生成与配置、updater/process 插件依赖安装与注册。

Purpose: Wave 2 的三个并行 plan（CI/CD、Updater UI、发版脚本）都依赖此 plan 产出的配置和依赖。这是整个发版基础设施的地基。
Output: 可编译通过的项目，包含 updater 配置、签名公钥、插件注册。
</objective>

<execution_context>
@/Users/kelin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-full-stack-impl/12-RESEARCH.md

<interfaces>
<!-- 当前 lib.rs 插件注册链 -->
From src-tauri/src/lib.rs:
```rust
let builder = tauri::Builder::default()
    .plugin(tauri_plugin_opener::init())
    .manage(watcher::SelfWriteTracker::new())
    // ... invoke_handler, setup 等
```

<!-- 当前 tauri.conf.json 结构 -->
From src-tauri/tauri.conf.json:
```json
{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "CLIManager",
  "version": "0.1.0",
  "identifier": "com.climanager.app",
  "bundle": {
    "active": true,
    "targets": "all",
    "icon": ["icons/32x32.png", "icons/128x128.png", "icons/128x128@2x.png", "icons/icon.icns", "icons/icon.ico"]
  }
}
```

<!-- 当前 capabilities/default.json -->
From src-tauri/capabilities/default.json:
```json
{
  "permissions": ["core:default", "opener:default"]
}
```

<!-- 当前 Cargo.toml 依赖区 -->
From src-tauri/Cargo.toml:
```toml
[package]
name = "cli-manager"
version = "0.1.0"

[dependencies]
tauri = { version = "2", features = ["tray-icon", "image-png"] }
tauri-plugin-opener = "2"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 版本来源统一 + 依赖安装 + 插件注册</name>
  <files>src-tauri/Cargo.toml, src-tauri/tauri.conf.json, src-tauri/capabilities/default.json, src-tauri/src/lib.rs, package.json</files>
  <action>
1. **Cargo.toml 版本升级（REL-01）：**
   - 修改 `version = "0.1.0"` 为 `version = "0.2.0"`（用户决策：首次 CI 发布版本 v0.2.0）
   - 在 `[dependencies]` 区添加两行：
     ```toml
     tauri-plugin-updater = "2"
     tauri-plugin-process = "2"
     ```

2. **tauri.conf.json 配置（REL-01、SIGN-01）：**
   - 删除顶级 `"version": "0.1.0"` 字段（Tauri 自动回退读 Cargo.toml）
   - 在 `bundle` 中添加：
     ```json
     "createUpdaterArtifacts": true,
     "macOS": {
       "signingIdentity": "-"
     }
     ```
   - 在顶级添加 `plugins` 对象（pubkey 暂时留空字符串占位，Task 2 生成密钥后填入）：
     ```json
     "plugins": {
       "updater": {
         "pubkey": "",
         "endpoints": [
           "https://github.com/nuts2k/CLIManager/releases/latest/download/latest.json"
         ]
       }
     }
     ```

3. **capabilities/default.json 添加权限：**
   - 在 `permissions` 数组中追加 `"updater:default"` 和 `"process:default"`

4. **lib.rs 注册插件：**
   - 在 `.plugin(tauri_plugin_opener::init())` 之后插入：
     ```rust
     .plugin(tauri_plugin_updater::Builder::new().build())
     .plugin(tauri_plugin_process::init())
     ```

5. **前端依赖安装：**
   - 运行 `pnpm add @tauri-apps/plugin-updater @tauri-apps/plugin-process`

6. **编译验证：**
   - 运行 `cd src-tauri && cargo check` 确认 Rust 编译通过
  </action>
  <verify>
    <automated>cd /Users/kelin/Workspace/CLIManager/src-tauri && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>Cargo.toml 版本为 0.2.0，tauri.conf.json 无 version 字段且含 updater/bundle 配置，lib.rs 注册了 updater 和 process 插件，capabilities 声明了权限，前端依赖已安装，cargo check 通过</done>
</task>

<task type="auto">
  <name>Task 2: Ed25519 密钥生成与公钥写入</name>
  <files>src-tauri/tauri.conf.json</files>
  <action>
1. **生成 Ed25519 密钥对（SIGN-02）：**
   - 运行 `pnpm tauri signer generate -w ~/.tauri/climanager.key`
   - 密钥不设密码（直接回车或传空密码），规避已知 Bug #13485
   - 注意：如果命令交互式询问密码，使用 `echo "" | pnpm tauri signer generate -w ~/.tauri/climanager.key` 或等效方式绕过

2. **验证密钥文件生成：**
   - 确认 `~/.tauri/climanager.key` 和 `~/.tauri/climanager.key.pub` 两个文件存在
   - 读取 `~/.tauri/climanager.key.pub` 内容

3. **公钥写入 tauri.conf.json（SIGN-03 部分）：**
   - 将 `.pub` 文件的内容（单行 base64 字符串）写入 `plugins.updater.pubkey` 字段
   - 替换 Task 1 中的空字符串占位

4. **提示用户完成 GitHub Secrets 配置（SIGN-03 剩余部分）：**
   - 输出私钥文件路径和 GitHub Secrets 配置指引
   - 私钥需存入 GitHub repo 的 `Settings > Secrets > Actions` 中，名称为 `TAURI_SIGNING_PRIVATE_KEY`
   - 私钥文件的完整内容（两行：注释行 + base64 密钥行）需整体粘贴为 secret 值
   - 提醒用户备份私钥（STATE.md 记录的风险：私钥丢失风险极高）
  </action>
  <verify>
    <automated>ls ~/.tauri/climanager.key ~/.tauri/climanager.key.pub && grep -c "pubkey" /Users/kelin/Workspace/CLIManager/src-tauri/tauri.conf.json</automated>
  </verify>
  <done>密钥对已生成在 ~/.tauri/ 目录，公钥已写入 tauri.conf.json 的 plugins.updater.pubkey 字段（非空字符串），用户已收到 GitHub Secrets 配置和私钥备份指引</done>
</task>

</tasks>

<verification>
- `cargo check` 编译通过（无 updater/process 相关编译错误）
- `tauri.conf.json` 无 `version` 字段，有 `plugins.updater.pubkey`（非空）和 `endpoints`
- `Cargo.toml` 版本为 `0.2.0`
- `capabilities/default.json` 包含 `updater:default` 和 `process:default`
- `~/.tauri/climanager.key` 和 `~/.tauri/climanager.key.pub` 文件存在
</verification>

<success_criteria>
- REL-01: Cargo.toml 是唯一版本来源（0.2.0），tauri.conf.json 无 version 字段
- SIGN-02: Ed25519 密钥对已生成并存储在 ~/.tauri/
- SIGN-03: 公钥已写入 tauri.conf.json，私钥配置指引已输出给用户
- 项目可编译：cargo check 通过
</success_criteria>

<output>
After completion, create `.planning/phases/12-full-stack-impl/12-01-SUMMARY.md`
</output>
