---
phase: 15-handler
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/proxy/state.rs
  - src-tauri/src/commands/proxy.rs
  - src-tauri/src/commands/provider.rs
  - src-tauri/src/watcher/mod.rs
  - src-tauri/src/proxy/mod.rs
  - src-tauri/src/proxy/server.rs
autonomous: true
requirements: [ROUT-02, MODL-03]

must_haves:
  truths:
    - "UpstreamTarget 携带 upstream_model 和 upstream_model_map 两个 Option 字段"
    - "build_upstream_target_from_provider 直接保留 provider.base_url（不 strip path）"
    - "所有 16 个 UpstreamTarget 构造点编译通过（新字段填 None 或从 Provider 读取）"
    - "现有全部测试继续通过（Anthropic 路径零回归）"
  artifacts:
    - path: "src-tauri/src/proxy/state.rs"
      provides: "UpstreamTarget 扩展后的结构体定义"
      contains: "upstream_model"
    - path: "src-tauri/src/commands/proxy.rs"
      provides: "build_upstream_target_from_provider 新实现（保留完整 base_url + 读取映射字段）"
      contains: "upstream_model_map"
  key_links:
    - from: "src-tauri/src/commands/proxy.rs"
      to: "src-tauri/src/proxy/state.rs"
      via: "build_upstream_target_from_provider 构造 UpstreamTarget"
      pattern: "UpstreamTarget\\s*\\{"
---

<objective>
扩展 UpstreamTarget 结构体以携带模型映射数据，并更新全部 16 个构造点（6 个文件）。同时修复 build_upstream_target_from_provider 中 base_url 被 extract_origin_base_url strip path 的问题，改为直接保留 provider.base_url。

Purpose: 为 Plan 02 的 handler 协议路由分支提供基础设施——handler 需要从 UpstreamTarget 读取映射数据，base_url 需要保留完整路径供 build_proxy_endpoint_url 正确拼接。
Output: 编译通过，所有现有测试继续通过，UpstreamTarget 就绪。
</objective>

<execution_context>
@/Users/kelin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-handler/15-RESEARCH.md

<interfaces>
<!-- UpstreamTarget 当前定义（state.rs L8-12） -->
```rust
#[derive(Debug, Clone)]
pub struct UpstreamTarget {
    pub api_key: String,
    pub base_url: String,
    pub protocol_type: ProtocolType,
}
```

<!-- Provider struct 相关字段（provider.rs L114-117） -->
```rust
pub upstream_model: Option<String>,
pub upstream_model_map: Option<HashMap<String, String>>,
```

<!-- build_upstream_target_from_provider 当前实现（commands/proxy.rs L43-49） -->
```rust
fn build_upstream_target_from_provider(provider: &Provider) -> Result<UpstreamTarget, AppError> {
    Ok(UpstreamTarget {
        api_key: provider.api_key.clone(),
        base_url: extract_origin_base_url(&provider.base_url).map_err(AppError::Validation)?,
        protocol_type: provider.protocol_type.clone(),
    })
}
```

<!-- extract_origin_base_url 行为（provider.rs L72-74）：会 strip path -->
```rust
url.set_path("");
url.set_query(None);
url.set_fragment(None);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 UpstreamTarget + 更新全部构造点</name>
  <files>
    src-tauri/src/proxy/state.rs,
    src-tauri/src/commands/proxy.rs,
    src-tauri/src/commands/provider.rs,
    src-tauri/src/watcher/mod.rs,
    src-tauri/src/proxy/mod.rs,
    src-tauri/src/proxy/server.rs
  </files>
  <action>
1. **state.rs — UpstreamTarget 新增两个字段：**
   ```rust
   use std::collections::HashMap;

   #[derive(Debug, Clone)]
   pub struct UpstreamTarget {
       pub api_key: String,
       pub base_url: String,
       pub protocol_type: ProtocolType,
       pub upstream_model: Option<String>,
       pub upstream_model_map: Option<HashMap<String, String>>,
   }
   ```
   更新 state.rs 内所有测试中的 UpstreamTarget 构造（make_target + 2 处内联），新字段填 `None`。

2. **commands/proxy.rs — 两个构建函数更新：**
   - `build_upstream_target()` (L28-41)：新字段填 `upstream_model: None, upstream_model_map: None`
   - `build_upstream_target_from_provider()` (L43-49)：**关键变更**——不再调用 `extract_origin_base_url`，直接使用 `provider.base_url.clone()`；新增 `upstream_model: provider.upstream_model.clone()` 和 `upstream_model_map: provider.upstream_model_map.clone()`
   - 更新 `test_build_upstream_target_from_provider_strips_legacy_path` 测试：改为验证 base_url 被**保留**而非 strip，断言改为 `assert_eq!(upstream.base_url, "https://api.openai.com/v1/chat/completions")`，测试名改为 `test_build_upstream_target_from_provider_preserves_base_url`
   - 同时检查该测试文件中所有内联 UpstreamTarget 构造（约 5 处测试代码中），统一添加新字段 `None`

3. **commands/provider.rs — 两处内联构造更新（L555, L636）：**
   每处新增 `upstream_model: provider.upstream_model.clone()` 和 `upstream_model_map: provider.upstream_model_map.clone()`（注意 L555 用 `result` 而非 `provider`，字段名是 `result.upstream_model`）

4. **watcher/mod.rs — 一处构造更新（L205）：**
   新增 `upstream_model: provider.upstream_model.clone(), upstream_model_map: provider.upstream_model_map.clone()`

5. **proxy/mod.rs — make_upstream 辅助函数（L206-212）+ 其他测试构造：**
   新字段填 `None`

6. **proxy/server.rs — 四处测试内联构造（L265, L313, L374, L422）：**
   新字段填 `None`

**注意事项：**
- 不要修改 `build_upstream_target()` 中 `normalize_origin_base_url` 的调用——该函数用于用户手动输入场景，base_url 校验保持严格
- 只有 `build_upstream_target_from_provider()` 改为直接保留 `provider.base_url`——该函数用于从 Provider 读取场景，base_url 已在创建 Provider 时校验过
- `commands/provider.rs` 中的两处也应改为直接使用 `result.base_url.clone()` / `provider.base_url.clone()` 而非 `extract_origin_base_url`（因为这些构造的 UpstreamTarget 后续也可能走转换路径）
- `watcher/mod.rs` 中同理，改为直接使用 `provider.base_url.clone()` 而非 `extract_origin_base_url`
  </action>
  <verify>
    <automated>/Users/kelin/.cargo/bin/cargo test --manifest-path /Users/kelin/Workspace/CLIManager/src-tauri/Cargo.toml --package cli-manager proxy::state proxy::mod proxy::server commands::proxy commands::provider 2>&1 | tail -5</automated>
  </verify>
  <done>
    - UpstreamTarget 结构体包含 5 个字段（api_key, base_url, protocol_type, upstream_model, upstream_model_map）
    - 全部 16 个构造点编译通过
    - build_upstream_target_from_provider 直接保留 provider.base_url（不调用 extract_origin_base_url）
    - commands/provider.rs 和 watcher/mod.rs 中的 UpstreamTarget 构造也保留完整 base_url 并携带映射字段
    - 现有测试全部通过（test_build_upstream_target_from_provider_strips_legacy_path 更新为验证新行为）
  </done>
</task>

</tasks>

<verification>
/Users/kelin/.cargo/bin/cargo test --manifest-path /Users/kelin/Workspace/CLIManager/src-tauri/Cargo.toml --package cli-manager 2>&1 | grep -E "test result|FAILED"
</verification>

<success_criteria>
- cargo check 编译通过，无 error
- 全套测试通过（294+ passed，仅已知 UX-01 端口冲突测试除外）
- UpstreamTarget 结构体含 5 个字段
- build_upstream_target_from_provider 不再调用 extract_origin_base_url
</success_criteria>

<output>
After completion, create `.planning/phases/15-handler/15-01-SUMMARY.md`
</output>
