---
phase: 16-responses-api-provider-ui
plan: 04
type: execute
wave: 2
depends_on: ["16-01", "16-02"]
files_modified:
  - src-tauri/src/proxy/handler.rs
  - src-tauri/src/proxy/mod.rs
autonomous: true
requirements: [RAPI-01, RAPI-02, RAPI-03, RAPI-04]
must_haves:
  truths:
    - "handler.rs 中 OpenAiResponses 走独立转换分支（不再与 Anthropic 合并透传）"
    - "OpenAiResponses 分支调用 responses_request::anthropic_to_responses 做请求转换"
    - "OpenAiResponses 分支端点重写为 /responses"
    - "OpenAiResponses 非流式响应调用 responses_response::responses_to_anthropic 转换"
    - "OpenAiResponses 流式响应调用 responses_stream::create_responses_anthropic_sse_stream 转换"
    - "OpenAiResponses 4xx/5xx 错误直接透传"
    - "OpenAiChatCompletions 分支行为不受影响（零回归）"
    - "Anthropic 分支行为不受影响（零回归）"
    - "模型映射 apply_upstream_model_mapping 在 Responses API 请求转换前执行"
  artifacts:
    - path: "src-tauri/src/proxy/handler.rs"
      provides: "OpenAiResponses 独立转换分支"
      contains: "ProtocolType::OpenAiResponses =>"
    - path: "src-tauri/src/proxy/mod.rs"
      provides: "OpenAiResponses 集成测试"
  key_links:
    - from: "src-tauri/src/proxy/handler.rs"
      to: "src-tauri/src/proxy/translate/responses_request.rs"
      via: "步骤 C 后 OpenAiResponses 分支调用"
      pattern: "translate::responses_request::anthropic_to_responses"
    - from: "src-tauri/src/proxy/handler.rs"
      to: "src-tauri/src/proxy/translate/responses_response.rs"
      via: "步骤 J OpenAiResponses 非流式分支调用"
      pattern: "translate::responses_response::responses_to_anthropic"
    - from: "src-tauri/src/proxy/handler.rs"
      to: "src-tauri/src/proxy/translate/responses_stream.rs"
      via: "步骤 J OpenAiResponses 流式分支调用"
      pattern: "translate::responses_stream::create_responses_anthropic_sse_stream"
---

<objective>
将 Responses API 转换模块接入 handler.rs，OpenAiResponses 从透传路径拆出为独立转换分支。

Purpose: Phase 14-15 中 OpenAiResponses 暂时与 Anthropic 合并走透传路径（Phase 16 细化），现在需要拆开为独立分支，调用 Plan 01/02 实现的转换函数，完成端到端协议转换链路。
Output: handler.rs 完整三分支路由 + 集成测试
</objective>

<execution_context>
@/Users/kelin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-responses-api-provider-ui/16-CONTEXT.md
@.planning/phases/16-responses-api-provider-ui/16-RESEARCH.md
@.planning/phases/16-responses-api-provider-ui/16-01-SUMMARY.md
@.planning/phases/16-responses-api-provider-ui/16-02-SUMMARY.md

<interfaces>
<!-- Plan 01 输出（responses_request.rs） -->
```rust
pub fn anthropic_to_responses(body: Value) -> Result<Value, ProxyError>;
// 端点重写复用：request::build_proxy_endpoint_url(&base_url, "/responses")
```

<!-- Plan 02 输出（responses_response.rs / responses_stream.rs） -->
```rust
pub fn responses_to_anthropic(body: Value) -> Result<Value, ProxyError>;

pub fn create_responses_anthropic_sse_stream(
    upstream: impl Stream<Item = Result<Bytes, reqwest::Error>> + Send + 'static,
    request_model: String,
) -> impl Stream<Item = Result<Bytes, std::io::Error>> + Send + 'static;
```

<!-- 现有 handler.rs 关键结构 -->
From src-tauri/src/proxy/handler.rs:
```rust
// 步骤 C 后协议路由（当前）：
match upstream.protocol_type {
    ProtocolType::OpenAiChatCompletions => { /* 完整转换链路 */ }
    ProtocolType::Anthropic | ProtocolType::OpenAiResponses => { /* 透传 */ }
}

// 步骤 J 响应分支（当前）：
match upstream.protocol_type {
    ProtocolType::OpenAiChatCompletions => { /* 非流/流/错误 */ }
    _ => { /* 透传 */ }
}
```

<!-- 现有集成测试模式 -->
From src-tauri/src/proxy/mod.rs:
```rust
// make_upstream_openai 辅助函数（需新增 make_upstream_responses 变体）
// start_mock_upstream_router 启动 mock 上游
// Arc<TokioMutex<Option<Value>>> capture 请求
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: handler.rs OpenAiResponses 独立转换分支</name>
  <files>src-tauri/src/proxy/handler.rs</files>
  <behavior>
    - test_responses_api_routing: 发送 Anthropic 请求到 OpenAiResponses Provider，mock 上游收到 Responses API 格式请求
    - test_responses_api_endpoint: 请求发送到 /v1/responses 端点（不是 /v1/chat/completions）
    - test_responses_api_model_mapping: 模型映射在请求转换前执行
  </behavior>
  <action>
    修改 handler.rs 步骤 C 后的协议路由分支：

    1. 将 `ProtocolType::Anthropic | ProtocolType::OpenAiResponses` 拆开为三个独立分支：
       ```rust
       match upstream.protocol_type {
           ProtocolType::OpenAiChatCompletions => { /* 现有逻辑不变 */ }
           ProtocolType::OpenAiResponses => {
               // 新建独立分支（与 Chat Completions 并列结构）
               let body_value: Value = serde_json::from_slice(&body_bytes)
                   .map_err(|e| ProxyError::TranslateError(format!("无法解析请求体: {}", e)))?;
               let is_streaming = body_value.get("stream").and_then(|v| v.as_bool()).unwrap_or(false);
               let request_model = body_value.get("model").and_then(|m| m.as_str()).unwrap_or("unknown").to_string();

               // 模型映射（在转换前执行）
               let body_value = apply_upstream_model_mapping(body_value, &upstream);

               // 请求转换 + 端点重写
               let responses_body = translate::responses_request::anthropic_to_responses(body_value)?;
               let url = translate::request::build_proxy_endpoint_url(&upstream.base_url, "/responses");
               let new_bytes = serde_json::to_vec(&responses_body)
                   .map_err(|e| ProxyError::Internal(e.to_string()))?;

               (url, Bytes::from(new_bytes), is_streaming, request_model)
           }
           ProtocolType::Anthropic => {
               // 保持透传（单独分支，不再与 OpenAiResponses 合并）
               let url = format!("{}{}{}", upstream.base_url.trim_end_matches('/'), path, query);
               (url, body_bytes, false, String::new())
           }
       }
       ```

    2. 修改步骤 J 响应分支，将 `_ =>` 通配符拆开为三分支：
       ```rust
       match upstream.protocol_type {
           ProtocolType::OpenAiChatCompletions => { /* 现有逻辑不变 */ }
           ProtocolType::OpenAiResponses => {
               if !status.is_success() {
                   Body::from_stream(upstream_resp.bytes_stream())
               } else if is_streaming {
                   Body::from_stream(translate::responses_stream::create_responses_anthropic_sse_stream(
                       upstream_resp.bytes_stream(),
                       request_model,
                   ))
               } else {
                   let resp_bytes = upstream_resp.bytes().await
                       .map_err(|e| ProxyError::Internal(format!("读取上游响应失败: {}", e)))?;
                   let resp_value: Value = serde_json::from_slice(&resp_bytes)
                       .map_err(|e| ProxyError::TranslateError(format!("响应解析失败: {}", e)))?;
                   let anthropic_resp = translate::responses_response::responses_to_anthropic(resp_value)?;
                   let resp_bytes = serde_json::to_vec(&anthropic_resp)
                       .map_err(|e| ProxyError::Internal(e.to_string()))?;
                   Body::from(resp_bytes)
               }
           }
           ProtocolType::Anthropic => {
               Body::from_stream(upstream_resp.bytes_stream())
           }
       }
       ```
  </action>
  <verify>
    <automated>cd /Users/kelin/Workspace/CLIManager/src-tauri && cargo test -p cli-manager-lib -- handler::tests 2>&1 | tail -20</automated>
  </verify>
  <done>handler.rs 三分支路由完整：OpenAiChatCompletions/OpenAiResponses/Anthropic 各走独立路径</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OpenAiResponses 集成测试（非流式 + 流式 + 模型映射）</name>
  <files>src-tauri/src/proxy/mod.rs</files>
  <behavior>
    - test_responses_api_non_streaming: mock 上游返回 Responses API 格式响应，代理层转换为 Anthropic 格式
    - test_responses_api_streaming: mock 上游返回 Responses API SSE 流，代理层转换为 Anthropic SSE 序列
    - test_responses_api_model_mapping: 代理层在请求转换前应用模型映射
  </behavior>
  <action>
    在 mod.rs 的 #[cfg(test)] 块中新增集成测试：

    1. 新增 `make_upstream_responses` 辅助函数：
       ```rust
       fn make_upstream_responses(api_key: &str, base_url: &str) -> UpstreamTarget {
           UpstreamTarget {
               api_key: api_key.to_string(),
               base_url: base_url.to_string(),
               protocol_type: ProtocolType::OpenAiResponses,
               upstream_model: None,
               upstream_model_map: None,
           }
       }
       ```

    2. `test_responses_api_non_streaming_roundtrip`:
       - 启动 mock 上游，POST /v1/responses 返回 Responses API 格式响应：
         ```json
         { "id":"resp_abc", "object":"response", "output":[{"type":"message","role":"assistant","content":[{"type":"output_text","text":"Hello"}],"status":"completed"}], "usage":{"input_tokens":10,"output_tokens":5} }
         ```
       - 发送 Anthropic 请求（带 stream:false）
       - 验证：capture 到的请求体包含 `input`（不是 `messages`）和 `instructions`（不是 system role）
       - 验证：响应体包含 Anthropic 格式（content:[{type:"text"}]，stop_reason:"end_turn"）

    3. `test_responses_api_streaming_roundtrip`:
       - mock 上游返回 Responses API SSE 事件序列
       - 发送 Anthropic 请求（带 stream:true）
       - 验证：响应是 Anthropic SSE 格式（event: message_start, content_block_start 等）

    4. `test_responses_api_model_mapping_roundtrip`:
       - 使用 make_upstream_responses + upstream_model_map
       - 验证：capture 到的请求体 model 字段已被映射替换

    参考现有 test_openai_compat_non_streaming_roundtrip 等测试的 mock 模式和断言结构。
  </action>
  <verify>
    <automated>cd /Users/kelin/Workspace/CLIManager/src-tauri && cargo test -p cli-manager-lib -- proxy::tests::test_responses_api 2>&1 | tail -20</automated>
  </verify>
  <done>
    - 3 个集成测试通过：非流式 roundtrip + 流式 roundtrip + 模型映射
    - cargo test 全套通过（含现有 OpenAiChatCompletions 和 Anthropic 测试，零回归）
  </done>
</task>

</tasks>

<verification>
cd src-tauri && cargo test -p cli-manager-lib 2>&1 | tail -10
全套测试通过，包括：
- 现有 OpenAiChatCompletions 转换测试（零回归）
- 现有 Anthropic 透传测试（零回归）
- 新增 OpenAiResponses 转换测试
</verification>

<success_criteria>
- handler.rs 三分支路由：OpenAiChatCompletions / OpenAiResponses / Anthropic
- OpenAiResponses 请求转换 + 端点重写 /v1/responses 正确执行
- OpenAiResponses 非流式和流式响应均正确转换为 Anthropic 格式
- 模型映射在请求转换前执行
- 现有测试零回归
- 3 个新集成测试全部通过
</success_criteria>

<output>
After completion, create `.planning/phases/16-responses-api-provider-ui/16-04-SUMMARY.md`
</output>
